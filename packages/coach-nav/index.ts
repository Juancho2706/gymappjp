/**
 * @eva/coach-nav — registro compartido del nav del coach (SEAM E7-09).
 * Ver `nav.ts` para el registro (`NAV_MODULES`), los resolvers puros
 * (`getVisibleNavItems` / `groupNavItems`, y las heredadas `splitNavItems` / `splitForSidebar`)
 * y el puente de contexto mobile->web (`coachWorkspaceTypeFromKind`).
 */
export * from './nav'
