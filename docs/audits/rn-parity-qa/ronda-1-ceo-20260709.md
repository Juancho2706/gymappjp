# QA CEO — Ronda 1 (2026-07-09, Xiaomi 14T, APK rama rnmobiledenuevo)

Primera ronda en device real. El CEO no pudo pasar del login del alumno (bug crítico).

| # | Hallazgo | Severidad | Estado |
|---|---|---|---|
| 1 | **Login alumno: no se puede escribir.** Tap en input de email → el foco salta solo a password → el teclado/input desaparece. Nunca deja escribir. Bloquea TODO el QA del alumno. Sospechas: update-loop de re-render (ThemeContext/branding sin memo) o autofill Android (`autoComplete`). | **P0** | **FIX DEFINITIVO 2026-07-09 (2º pase).** Causa raíz = bug RN core #45798 bajo New Architecture/Fabric: un `View` que envuelve al `TextInput` y muta forma/clases al enfocar dispara `onBlur` inmediato (en login encadena next/done y cierra; en register rota el foco). Aplicado: **(1) `Input.tsx`** — borde por `style` (borderColor estable, className del contenedor SIEMPRE la misma) + focus-ring como hermano absoluto SIEMPRE montado que solo varía `opacity` (sin elevation Android); enfocar ya no cambia forma ni clases del árbol. **(2) `ThemeContext.tsx`** — `theme`/`themeVars`/`value` memoizados (antes objetos nuevos por render → Provider re-propagaba identidad a todos los consumidores, amplificador global). **(3) `_layout.tsx`** — quitado el `MotiView` que animaba opacity envolviendo el navegador (vista animada persistente sobre react-native-screens/Fabric); ahora `View` plano, el cross-fade lo hace solo el overlay del BrandedSplash. **(4) `register.tsx`** — banner de error en slot SIEMPRE montado (antes hermano condicional antes de los inputs → remontaba los inputs al aparecer). **(5)** sonda de telemetría en `Input.tsx`: >4 focus en <1.5s ⇒ `Sentry.captureMessage('focus-hop-loop-detected')` 1×/sesión (gateada por DSN, no-op sin Sentry). El fix del 1er pase (autofill HyperOS/MIUI + next/done) SE CONSERVA. **Plan B documentado (no aplicado):** `android:windowSoftInputMode` / `softwareKeyboardLayoutMode: 'pan'` en la config Android (app.json → `expo.android.softwareKeyboardLayoutMode`) — reduce el re-layout del teclado que puede re-disparar el hop; se deja como palanca si el device sigue robando foco tras este build. Pendiente re-test CEO en device. |
| 2 | Splash: rework total — más simple, con estilo (referencia: apps fitness premium) | P1 | Workflow `qa-ceo-ux-rework` U1 |
| 3 | Selector de rol ("¿eres alumno o coach?"): rediseño según mockup aprobado (card azul protagonista alumno + card blanca coach + footer "acceso seguro") | P1 | Workflow U2 |
| 4 | Walkthrough/onboarding: fondo blanco / mejor estilo | P1 | Workflow U3 |
| 5 | Flujo de entrada: con coach recordado (branding cacheado) ir DIRECTO al login brandeado del alumno + botón "elegir otro rol"; con sesión previa ir directo al dashboard | P1 | Workflow U2 |

**Lo que SÍ gustó:** al meter el código del coach (josefit) la app cambia a la estética del coach ✅; al reabrir, el splash brandeado del coach aparece ✅.

Pendiente re-test tras fixes: login alumno completo → dashboard → ejecutor → nutrición → check-in.
