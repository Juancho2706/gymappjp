# Auditoría de fidelidad visual — Equipo (Teams) · 2026-07-01

Área: `/coach/team` (contexto team). Kit desktop: `DesktopTeamEquipo` (docs/design-source/ui_kits/eva-desktop/desktop-coach.jsx:662-788, CSS `.dt-team*`/`.dt-md-*`/`.dt-kpi*`/`.dt-perm*` en eva-desktop/index.html:227-253, 266-275, 518, 685-710, 839-845). Kit mobile: docs/design-source/ui_kits/eva-app/screens/teams-equipo.jsx (574 líneas, VERBATIM).
App: apps/web/src/app/coach/team/page.tsx + \_components/CoachTeamDesktop.tsx, TeamBrandStudio.tsx, TeamMembersManager.tsx, TeamShareLink.tsx.
Nota: área nunca verificada visualmente en vivo (josefit sin team) — auditoría de código con lectura de CSS ground-truth del kit.

No hay P0: el maestro-detalle desktop, los KPIs del pool, la matriz de permisos, el hero de identidad mobile, el Brand Studio y la lista de miembros existen todos con la jerarquía correcta. Los gaps son de estilo/chrome, concentrados en la superficie MOBILE (que debía ser verbatim) y en los diálogos.

---

## [P1] Sticky publish bar del Brand Studio: barra clara genérica en vez de la barra oscura inverse del kit
- Kit: teams-equipo.jsx:461-468 — `position:sticky bottom:12`, `background: var(--surface-inverse)` (oscura), `shadow-xl`, dot pulsante `var(--warning-500)`, texto blanco "Cambios sin publicar", botón Descartar con borde `--border-inverse` sobre oscuro + `Button variant="sport"` "Publicar".
- App: TeamBrandStudio.tsx:563-584 — `sticky bottom-2 ... bg-surface-card/95 backdrop-blur shadow-lg`, dot `bg-amber-500` (raw Tailwind), texto `text-xs text-muted`, Button ghost "Descartar" + Button default "Publicar marca".
- Diferencia visual: el kit usa una barra flotante OSCURA de alto contraste (patrón señalético del DS para "estado sucio"); la app renderiza una barra clara translúcida que se pierde sobre el fondo claro. Aplica a ambos viewports (el componente se monta también dentro del flujo mobile).
- Fix: swap a `bg-[var(--surface-inverse)]` + texto `text-on-dark`, dot `bg-[var(--warning-500)]`, Descartar con `border-[var(--border-inverse)] text-on-dark`, radius `rounded-card`/`--radius-lg` y `shadow-xl`.
- **Verdict:** CONFIRMED — código verificado línea a línea: kit :461-468 = barra `--surface-inverse` (ink-950) + dot `--warning-500` + Descartar `border-inverse` sobre oscuro; app :563-584 = `bg-surface-card/95 backdrop-blur` clara + dot `bg-amber-500` + ghost. Diferencia de superficie (oscura señalética vs clara translúcida) real y visible. Corrección menor: NO aplica a ambos viewports — TeamBrandStudio se monta SOLO en la sección `md:hidden` de page.tsx (CoachTeamDesktop no lo importa), o sea el gap es mobile-only, justo donde el kit es verbatim. El botón Publicar `default` ≈ `sport` (mismo `--cta-fill`, solo falta `--glow-sport`), nit no central.

## [P1] Feedback/estado del Brand Studio y Miembros con paleta Tailwind cruda (emerald/amber/red) en vez de tokens DS success/warning/danger
- Kit: teams-equipo.jsx:359-362 (banner AA: `var(--success-100)`/`var(--success-700)` ↔ `var(--warning-100)`/`var(--warning-700)`), :270 (flash éxito success-100/700 con check-circle), :470-472 (toast "Marca publicada" = pill flotante `var(--success-600)` centrada bottom fixed).
- App: TeamBrandStudio.tsx:361-370 (AA: `border-emerald-500/20 bg-emerald-500/5 text-emerald-600` / `amber-500/30...`), :550-560 (feedback `red-500`/`emerald-500` inline); TeamMembersManager.tsx:80-86 (mismo patrón red/emerald); TeamMembersManager.tsx:257 (`text-emerald-500` en el check de copiado).
- Diferencia visual: los verdes/ámbar/rojos raw de Tailwind NO matchean la rampa DS (`--success-*` #DBF5EA/#0E7A50, `--warning-*` #FDEFD3, `--danger-600` #BE183C, definidos en globals.css:377-383) y no flipean con la rampa dark del tema. Además la confirmación de publicación es un banner inline en el flujo, no la pill flotante success del kit.
- Fix: reemplazar por `bg-[var(--success-100)] text-[var(--success-700)]` / `--warning-100/-700` / `--danger-*`; convertir el mensaje de éxito de publicación en toast flotante `bg-[var(--success-600)] text-white rounded-pill` bottom-center.
- **Verdict:** CONFIRMED — todas las líneas citadas existen tal cual. Los tokens DS verificados en globals.css (:374-383: `--success-100` #DBF5EA sólido, `--warning-100` #FDEFD3, `--danger-600` #BE183C) difieren visiblemente de `emerald-500/5` (tinte casi invisible) / `amber-500/10`; y la rampa DS SÍ flipea en dark (globals.css:612-626 redefine success/warning/danger) mientras la app depende de `dark:text-emerald-400` manual con hues distintos. La pill flotante `--success-600` bottom-fixed del kit (:470-472) vs banner inline en el flujo también es real. No hay @theme remap de emerald/amber que lo salve.

## [P1] Mobile — badges de rol de miembros no siguen el mapping de tonos del kit (aqua/sport/neutral) ni etiquetan a todos
- Kit: teams-equipo.jsx:227 + :430 — `roleTone = { Owner:'sport', 'Co-gestor':'aqua', Miembro:'neutral' }`; CADA fila lleva `Badge tone={roleTone[m.role]} variant="soft"` con el rol textual completo ("Owner"/"Co-gestor"/"Miembro").
- App: TeamMembersManager.tsx:106-110 — Owner = `Badge variant="default"` (legacy → sport soft, OK) con ícono Crown extra; co-gestor = `Badge variant="secondary"` (legacy → neutral soft) con texto **"Gestor"**; miembro raso = **sin badge**.
- Diferencia visual: en mobile se pierde el tono aqua distintivo del co-gestor, el label difiere ("Gestor" vs "Co-gestor") y las filas de miembros quedan sin chip de rol. Irónico: el desktop (CoachTeamDesktop.tsx:31-35) SÍ implementa el mapping verbatim — solo mobile quedó con el legacy shadcn.
- Fix: usar `<Badge tone="aqua" variant="soft" size="sm">Co-gestor</Badge>` y `<Badge tone="neutral" variant="soft" size="sm">Miembro</Badge>`; Owner sin ícono (el kit no lleva Crown en la fila).
- **Verdict:** CONFIRMED — badge.tsx LEGACY_TONE verificado (:109-113): `default`→sport soft (OK para Owner), `secondary`→neutral soft, es decir el co-gestor pierde el aqua (`--aqua-100`/`--aqua-700`) por ink-100/ink-700 — tono distinto real. Kit :430 pone Badge en CADA fila; app :106-110 deja a los miembros rasos sin chip y etiqueta "Gestor" (kit: "Co-gestor"). CoachTeamDesktop.tsx:31-35 confirma que el desktop SÍ tiene el mapping verbatim (ROLE_TONE Owner=sport/Co-gestor=aqua/Miembro=neutral) — la inconsistencia es solo mobile, superficie verbatim.

## [P1] Mobile — diálogos (Agregar coach / Editar especialidad / confirmaciones) como modal centrado shadcn en vez de los bottom-sheets del kit
- Kit: AddCoachDialog (teams-equipo.jsx:497-574) = bottom sheet full-width con drag-handle, título display-font 20px, **segmented control** en `surface-sunken` pill (Cuenta nueva | Coach existente), Inputs del DS con ícono izquierdo, fila-checkbox "Hacerlo co-gestor" con check sport-500, estado de éxito con círculo success + card de credenciales (password copiable en `--sport-600` mono) y CTA `variant="sport" size="lg" fullWidth`. EditRoleDialog (:153-174) = `DirSheet` con chip de ícono `sport-100` + contador `val.length/60`. Confirmaciones (:478-490) = card centrada con círculo de ícono `danger-100`/`sport-100`, título display, botón `danger`.
- App: TeamMembersManager.tsx:205-354 — `Dialog`/`AlertDialog` shadcn centrados (dialog.tsx:56: `top-1/2 left-1/2 -translate-... zoom-in-95`), tabs = dos `<Button>` sueltos (default/outline), Inputs sin íconos, checkbox nativo, éxito como texto plano + fila sunken, sin contador de caracteres, confirmaciones AlertDialog sin chip de ícono.
- Diferencia visual: en <760 el kit presenta TODO flujo modal como sheet desde abajo; la app muestra modales centrados con anatomía shadcn (aunque tokenizados). Es el mismo patrón ya corregido en Nutri/Asignar (commit c45e3d09) pero no en Equipo.
- Fix: presentar Dialog como bottom-sheet en <760 (radius `--radius-sheet` 28px arriba, handle, slide-up), segmented control real, chips de ícono y contador en EditRole; el desktop puede conservar modal centrado (el kit desktop no define diálogos propios).
- **Verdict:** CONFIRMED — dialog.tsx:53-56 verificado: popup SIEMPRE `top-1/2 left-1/2 -translate-x/y-1/2 zoom-in-95`, sin variante responsive de sheet en el primitive ni wrapper alternativo en el módulo. Kit AddCoach :520-522 = `alignItems:flex-end` + `--radius-sheet` solo arriba + drag-handle; segmented en `surface-sunken` (:527-530), Inputs con iconLeft (:534-536), checkbox sport-500 (:539), éxito con círculo success + password `--sport-600` mono copiable (:551-566). EditRole = DirSheet con chip `sport-100` + contador `/60` (:159,167) — app sin chip ni contador (:339-352). Matiz: las CONFIRMACIONES del kit (:479-489) también son centradas (con círculo de ícono danger/sport-100 que la app omite), así que ahí el gap es de anatomía, no de posición — no cambia el veredicto del flujo Agregar/Editar. `--radius-sheet` 28px existe (globals.css:133); md=760px confirmado (globals.css:26).

## [P1] Brand Studio — chrome interno no matchea el kit: cards anidadas con headers de chip, collapsibles sin re-skin y toggle del loader ausente
- Kit: teams-equipo.jsx:320-403 — UN solo `Card` plano: preview arriba (toggle sol/luna en pill `surface-sunken`), nombre, 2 LogoDrop (dropzone 76px alto que muestra el logo centrado + botón "x" para **quitarlo**), presets de color, banner AA, y DOS `TeamCollapsible` (:63-78) = filas borde `--border-subtle` con ícono `--ink-600` + título 13.5/700 + subtítulo + chevron rotatorio; dentro del de "Pantalla de carga" un **switch** (pill 38×22, :379-384) "Loader personalizado" que atenúa y bloquea los campos (`opacity:0.5, pointer-events:none`) cuando está apagado; selector de ícono = 4 botones sueltos con borde 2px del color primario + fondo tintado al seleccionar.
- App: TeamBrandStudio.tsx:279-444 — grid 2 columnas con TRES `Section` cada una `rounded-card border` propia + header con chip `bg-primary/10` (patrón que no existe en el kit), todo dentro de OTRA card del page.tsx:151 → doble borde anidado; "Ajustes avanzados de color" = `<details>` con summary de texto chico ":374" sin borde/ícono; loader = checkbox "Activar loader personalizado" al final (:432-441) que NO gatea visualmente los campos; icon-mode = segmented unido `bg-primary` fill (:411-430); LogoDrop = fila con thumb 48px + texto "Cambiar imagen" y **sin acción de quitar logo**.
- Diferencia visual: la app se ve como un formulario shadcn por secciones, no como la tarjeta única preview-first del kit; el usuario además no puede borrar un logo ya subido (el kit sí, con la "x").
- Fix (mobile primero): aplanar a una card, restaurar TeamCollapsible (borde+ícono+sub+chevron), switch pill que atenúe los campos del loader, LogoDrop tipo dropzone con clear.
- **Verdict:** CONFIRMED — todos los sub-claims verificados en código: Section con chip `bg-primary/10` (:188-196, patrón inexistente en el kit) dentro de la card del page.tsx:151 → doble borde real; `<details>`/summary de texto plano (:372-375) vs TeamCollapsible con borde+ícono+sub+chevron (kit :63-78); checkbox del loader (:432-441) NO atenúa/bloquea campos (kit :385 `opacity:0.5 pointer-events:none`); LogoDrop de la app (:124-180) NO tiene onClear/"x" — el usuario efectivamente no puede quitar un logo subido (kit :34); icon-mode `bg-primary` fill (:420-425) vs botones con borde 2px + tinte `hexA(primary,0.08)` (kit :395). Precisión a favor de la app: en mobile el preview SÍ va primero (`order-1`/`order-2`), pero eso no salva la estructura de 3 cards anidadas vs card única.

## [P2] Desktop — contenedor flotante `rounded-card` con `h-[calc(100dvh-8rem)]` en vez del full-bleed del kit
- Kit: `.dt-team { position:absolute; inset:0; ... }` (index.html:685) — la pantalla llena TODO el main bajo el topbar, sin borde ni radius.
- App: CoachTeamDesktop.tsx:150 — `h-[calc(100dvh-8rem)] min-h-[640px] rounded-card border border-subtle`.
- El hermano `/coach/clients` (CoachRosterMasterDetail.tsx:159) sí es full-bleed `h-[calc(100dvh-60px)]` sin card. Equipo queda como card flotante distinta del kit y del sibling.
- Fix: igualar al patrón de clients (full-bleed hasta el borde del content area, sin borde redondeado).

## [P2] Desktop — semántica de los KPIs del pool: conteos administrativos en vez de salud del pool
- Kit: data.js:470-474 — "Alumnos asignados 5/12", "Adherencia equipo 88% (+5%)", "Check-ins 7d 4/5" (con `dt-kpi-delta` en success).
- App: CoachTeamDesktop.tsx:106-110 — "Alumnos del pool", "Coaches activos N/M" (redundante con el contador del rail justo debajo), "Módulos activos".
- Más que data-mock: el kit pinta KPIs de RENDIMIENTO del pool; la app duplica conteos ya visibles. Discutible (adherencia agregada puede ser cara de computar acá), por eso P2.
- Fix (si se toma): sustituir "Coaches activos" por adherencia media del pool o check-ins 7d cuando la query lo permita; soportar `dt-kpi-delta`.

## [P2] Vista previa del teléfono y splash: detalles del mock difieren del BrandPreview/SplashPreview del kit
- Kit: BrandPreview (teams-equipo.jsx:81-127) — neutrales familia ink (#0F1729/#F6F8FB/#1A2233), SIN bisel de teléfono (card radius 20), header = chip de iniciales del color primario + nombre display-font, CTA "Empezar entrenamiento" con color de texto CALCULADO por contraste (`pickOn`), fila nutrición con chip utensils + pill "82%", simulación de `neutralTint` (:88). SplashPreview (:129-150) — alto 150px, spinner de fallback cuando no hay loader custom, logo EVA real para modo 'eva', caption `eva.app/t/{slug}`.
- App: TeamBrandStudio.tsx:246-250 y 474-545 — neutrales Tailwind `neutral-*` (#FAFAFA/#0A0A0A/#171717), bisel `border-[6px] border-neutral-900 rounded-[28px]`, header = logo en chip tintado del acento + "Hola, Alumna 👋", CTA `text-white` fijo (ilegible con acentos claros), sin simulación de neutralTint; splash `h-20` (80px), sin spinner, Sparkles en vez del logo EVA.
- Fix: alinear neutrales a la familia ink del kit, computar `pickOn(accent)` para el CTA (ya hay helper en @eva/brand-kit), splash más alto con spinner de fallback.

## [P2] Mobile — sección Miembros: encabezado dentro de la card, divisores full-bleed y aviso de cupos sin banner warning
- Kit: teams-equipo.jsx:406-417 — h2 "Miembros (N)" display 17px FUERA de la card + botón pill `--action-primary` "Agregar" 32px; card `padding="none"` con divisores inset (`margin: 0 14px`); si cupos llenos, banner `--warning-100`/`--warning-700` (:413-415).
- App: page.tsx:174-185 envuelve TeamMembersManager en card padded; el h3 queda DENTRO (TeamMembersManager.tsx:66), botón `Button size="sm"` rectangular, `ul divide-y` full-width, y el aviso de cupos es un `<p class="text-xs text-muted">` (:74-78).
- Fix: extraer heading+CTA fuera de la card (como ya hace page.tsx con el heading de Brand Studio), card sin padding con divisores inset, banner warning tokens.

## [P2] Footer "EVA Teams" con glifo Sparkles en vez del logotipo EVA
- Kit: teams-equipo.jsx:455-458 — `eva-logo-ink.png` 26px + "EVA Teams · {team}".
- App: page.tsx:188-194 — ícono `Sparkles` + texto "EVA" en display font.
- Fix: usar el asset real del logo EVA (mismo que usa el sidebar/panel) con `opacity-60`.

---

## Verificado 1:1 (sí matchea)

- **Desktop maestro-detalle completo** (CoachTeamDesktop.tsx): estructura `.dt-team` = KPIs (grid 3→1 en <1001px, padding 20/24, gap 14, chips de tono sport/success/ember con las clases exactas de `.dt-kpi*`), rail `.dt-md-list` con anchos responsivos 320/272/240 y ocultado de `row-sub` bajo 861px espejando las media queries del kit (index.html:839-845), header del rail (título display 18, count pill, botón invitar `--cta-fill` 30px), seat-bar `.dt-team-seat` exacta, filas con estado activo `sport-100` + barra 3px `::before`, perfil del detalle (display 24/900, badges solid+soft), matriz de permisos `.dt-perm-row` (px-16/py-12, íconos circulares 24px success-100/sunken), nota "tu perfil" `.dt-team-note`, card "Acceso del equipo" con copy button `.dt-team-copy`, empty state y toast `.dt-build-toast` (ink-950 pill bottom-center).
- **Tokens**: `--dt-read-text`/`--dt-page-x` existen en globals.css (570-574); rampa aqua/ember/sport/success/warning/danger completa; `rounded-card/control/pill`, `text-on-dark(-muted)`, `eva-metric/mono/press` presentes.
- **Avatares**: initials sobre `--surface-inverse` con `--sport-400` en display font = espejo exacto del DS Avatar (_ds_bundle.js:59-67), tanto en CoachTeamDesktop inline como en ui/avatar.tsx re-skineado (tamaños 36/64 vs 32/72 del kit: nit no reportado).
- **Hero mobile de identidad**: card inverse, chip 56px con iniciales/logo y `onAccent` computado, pill de rol con Crown/Shield/User, copy "Pool compartido…", AccessRows (login + código, copiar/abrir sobre `white/0.06` + `--border-inverse`), anillo SVG de cupos r=22 idéntico, stat tiles y link Módulos con chevron — verbatim del kit.
- **Roles gating**: manager = invitar/editar marca/editar especialidad; owner-only = co-gestor y transferencia; guard extra `!sel.isMemberOwner` en la app (más correcto que el kit). Matriz "Editar marca del equipo" = `m.manage` es divergencia DOCUMENTADA que refleja el RLS real (`team_teams_manager_update`) — no reportada como gap.
- **Textos de confirmación** (sacar/transferir) calcados del kit; heading "Brand Studio"/"Marca del equipo" + badge "Solo lectura" con lock para no-managers; presets de color, campos avanzados (acento claro/oscuro, splash, teñir grises) e hints "Vacío = usa el color principal" todos presentes.
- **Diálogos desktop**: el kit desktop no define diálogos propios (acciones inmediatas demo); los AlertDialog reales de la app son riqueza extra permitida.

---

## Fix log wave 2 (2026-07-01)

- **[P1] Sticky publish bar** → FIXED (`_components/TeamBrandStudio.tsx`). Barra `bg-[var(--surface-inverse)]` + `shadow-[var(--shadow-xl)]`, dot `bg-[var(--warning-500)]` pulsante, Descartar ghost con `border-[var(--border-inverse)] text-on-dark`, Publicar `variant="sport"`. (Aplicado por el intento previo abortado; verificado completo y correcto.)
- **[P1] Feedback/estado/toast → tokens DS** → FIXED (`TeamBrandStudio.tsx` + `_components/TeamMembersManager.tsx`). Brand Studio: banner AA `success-100/700`↔`warning-100/700`, error `danger-100/600`, éxito = pill flotante `bg-[var(--success-600)]` bottom-center (ya aplicado por el intento previo). Miembros: banner de feedback migrado a `success-100/700`/`danger-100/600` con check-circle; check de copiado de credenciales a `text-[var(--sport-600)]`.
- **[P1] Badges de rol mobile** → FIXED (`TeamMembersManager.tsx`). Mapping verbatim del kit en CADA fila: Owner=`tone="sport"`, Co-gestor=`tone="aqua"` (label "Co-gestor", antes "Gestor"), Miembro=`tone="neutral"` (antes sin chip); `variant="soft" size="sm"`, sin ícono Crown.
- **[P1] Diálogos mobile → bottom-sheets** → FIXED (`TeamMembersManager.tsx`). AddCoachDialog y EditRoleDialog ahora responsive vía `useIsTeamDesktop()` (matchMedia 760, split de page.tsx): `<Dialog>` centrado en ≥md / `<Sheet side="bottom">` con drag-handle + `rounded-t-sheet` en <md. AddCoach: segmented control real (surface-sunken pill), Inputs con `iconLeft` (user/mail/tag), checkbox co-gestor con check `sport-500`, estado éxito con círculo `success-100` + credenciales estilizadas (password `sport-600` mono copiable), CTA `variant="sport" size="lg"`. EditRole: chip de ícono `sport-100` + contador `/60`. TODOS los handlers/actions reusados tal cual. Confirmaciones (sacar/transferir): el kit las tiene CENTRADAS (no sheets) → se mantienen `AlertDialog` centrado + se agregó `AlertDialogMedia` círculo de ícono (`danger-100`/`sport-100`) y `variant="danger"` en Sacar para matchear la anatomía del kit.
- **[P1] Brand Studio chrome** → FIXED (`TeamBrandStudio.tsx`, por intento previo abortado; verificado): card única plana (Section+chip `bg-primary/10` eliminados), `TeamCollapsible` re-skineado (borde+ícono+sub+chevron), switch pill del loader que atenúa/bloquea campos (`opacity-50 pointer-events-none`), LogoDrop tipo dropzone 76px con "x" de limpiar el pick LOCAL, icon-mode con borde 2px + tinte. **SKIPPED sub-parte**: quitar un logo YA PERSISTIDO — `updateTeamBrandAction` no tiene handler de remove (`grep` en todo el árbol coach: 0 remove-logo); exige action nueva → fuera de scope presentacional.
- **[P2] Footer EVA logo** → FIXED (`page.tsx`). `Sparkles` reemplazado por `<EvaBrandIcon>` (asset real del logo EVA, mismo del sidebar, claro/oscuro) con `opacity-60`.
- **[P2] Vista previa teléfono/splash** → FIXED (`TeamBrandStudio.tsx`, por intento previo abortado; verificado): neutrales familia ink, sin bisel, CTA con `pickOnColor(accent)`, splash 150px con spinner de fallback + logo EVA para modo 'eva', simulación de `neutralTint`.
- **[P2] Aviso de cupos llenos** → FIXED (`TeamMembersManager.tsx`). `<p text-muted>` → banner `bg-[var(--warning-100)] text-[var(--warning-700)]`.
- **[P2] Desktop full-bleed** → SKIPPED. Cambiar la card flotante a full-bleed exige tocar el height-calc (`100dvh-8rem`→`100dvh-60px`) y el chrome del container; estructural + área nunca verificada en vivo → riesgo > beneficio P2.
- **[P2] Semántica KPIs del pool** → SKIPPED (marcado "discutible"; adherencia agregada requiere query nueva).
- **[P2] Sección Miembros (heading fuera de card / padding none / divisores inset)** → SKIPPED la reestructuración (estructural, toca page.tsx + manager); solo el banner de cupos (arriba) se tokenizó.
