# Auditoría de fidelidad visual — Área: Dashboard coach — 2026-07-01

Fuentes: kit desktop `docs/design-source/ui_kits/eva-desktop/desktop-coach.jsx` (DesktopCoachDashboard L89-193) + CSS `.dt-*` en `eva-desktop/index.html` (L261-308); kit mobile `docs/design-source/ui_kits/eva-app/screens/coach-dashboard.jsx` + `coach-dashboard-sheets.jsx`.
App: `apps/web/src/app/coach/dashboard/**` (page → DashboardContent → DashboardShell; mobile stack + DesktopBento).

---

## [P0] Guía de inicio: componente legacy pre-rediseño — falta el patrón "chip expandible" del kit (ambos viewports)

- **Kit:** `eva-app/screens/coach-dashboard.jsx` L254-292 (P3 del rediseño: "Guía de inicio colapsada a chip expandible (libera el fold)"). Es un chip compacto `sport-100` con borde `sport-200`: icono rocket + label "Guía de inicio" + 4 dots de progreso + contador `n/4` + chevron. Expandido: 4 filas check simples (círculo sport-500 / borde sport-300, texto tachado al completar), hint Pro con sparkles, y link "Saltar guía". Al 100%: card `success-100` "¡Activación lista!" con party-popper y botón X.
- **App:** `DashboardShell.tsx:150` monta `CoachOnboardingChecklist.tsx` (L360-609): un `GlassCard` gigante enteramente pre-rediseño — tokens shadcn legacy (`text-muted-foreground`, `bg-muted`, `text-foreground`, `border-border`, `buttonVariants`), azul `var(--theme-primary)` (#007AFF), botón "Saltar guía" con **gradiente naranja/ámbar pulsante** (motion.button L383-395, ajeno por completo a la paleta EVA), tabs "Tu panel / Tu alumno", carousel de viñetas, jump-nav, bloque "Plan Free" y bloque nutrición emerald. Ocupa varios cientos de px bajo Novedades en mobile Y desktop (el kit desktop ni siquiera tiene guía; el kit mobile la colapsa a ~44px).
- **Diferencia visual:** la jerarquía de página del kit (Pulse → Prioridad → Agenda → Novedades → chip) se rompe: el fold inferior queda dominado por un bloque con look shadcn/theme-primary que no comparte radius, tipografía display, ni paleta con el resto del dashboard rediseñado. Es el único bloque de la página sin re-skin.
- **Fix:** re-skin del contenedor al patrón chip del kit (colapsado por defecto salvo coach nuevo), manteniendo el motor real (señales auto, persistencia server, telemetría, rutas reales). El contenido rico (tabs gemelo, nutrición) puede vivir dentro del expandido o moverse a una vista aparte, pero con tokens EVA DS.
- **Verdict:** CONFIRMED — verificado adversarialmente: `DashboardShell.tsx:150` monta `CoachOnboardingChecklist` FUERA de los wrappers `md:hidden`/`hidden md:block` (visible en ambos viewports); el componente (L362-609) es GlassCard shadcn con `var(--theme-primary)` #007AFF, motion.button naranja/ámbar pulsante (L383-395), tabs/carousel/jump-nav/bloque Free — todo verbatim como se describe. No existe chip reskineado en ninguna parte (`_components/onboarding/*` son sub-piezas del mismo legacy; grep de CoachOnboardingChecklist da solo estos 2 archivos). El kit desktop no tiene guía (grep Guía/guide/onboard en desktop-coach.jsx = 0 hits) y el kit mobile la colapsa a chip (coach-dashboard.jsx L254-292 verificado). No es decisión intencional documentada ni vive en breakpoint alterno. Único bloque sin re-skin de la página → P0 justificado.

## [P1] ClientStatsSheet sin re-skin EVA DS + presentación side-sheet en mobile (kit = bottom sheet)

- **Kit:** `coach-dashboard-sheets.jsx` L61-108 (`ClientStatsSheet` sobre `DirSheet` = bottom sheet). Título display 19/800 + **promedio % grande a la derecha coloreado** por `barColor` (sport ≥75 / warning ≥50 / danger); segmented control estilo EVA (`surface-sunken` + tab activa `surface-card` + `shadow-xs`); caption "Ordenado por menor cumplimiento — los que necesitan ayuda primero."; filas flat con avatar círculo `ink-900`/inicial `sport-400`, % en `eva-mono` color-coded y `ProgressBar` height 5 color-coded.
- **App:** `_components/sheets/ClientStatsSheet.tsx` — `Sheet side="right"` (también en <760: entra lateral en vez de bottom sheet), y el contenido es shadcn legacy: `text-muted-foreground`, `border-border/50`, `bg-background/40`, tabs pill con `bg-primary` + inline `var(--theme-primary, #007AFF)` (L86, azul sistema viejo), `Progress` monocromo sin color-coding, sin promedio %, sin avatar ink/sport, filas como mini-cards con borde. Además el `SheetContent` base trae el gradiente `rgba(0,122,255,…)` legacy. Nit: "Nutricion" sin tilde.
- **Fix:** en <760 renderizar como bottom sheet (radius-sheet 28, drag handle); re-skin de tabs a segmented EVA, agregar avg% y `barColor` en % + barra; reemplazar tokens shadcn por los DS.
- **Verdict:** CONFIRMED — todo verificado línea a línea: `side="right"` sin variante mobile (L40), inline `var(--theme-primary, #007AFF)` exacto en L86, `Progress` shadcn monocromo (L65), sin avg%, sin avatar, filas mini-card `border-border/50 bg-background/40` (L59), "Nutricion" sin tilde (L48); kit L61-108 coincide con lo descrito (DirSheet bottom, avg% coloreado, segmented sunken, barColor). Nuance menor que no refuta: el gradiente `rgba(0,122,255,…)` del SheetContent base (sheet.tsx:63) es la rama dark:; en light es `from-primary/[0.06]` (mismo primary legacy). Detrás de interacción (Sparkles/KPI adherencia), no en el fold → P1 correcto.

## [P1] BillingBanners: tonos legacy de tema oscuro — texto casi invisible en el tema claro del rediseño

- **Kit:** no existe en el árbol del diseño (banner funcional, permitido), pero se pinta ARRIBA de todo el dashboard (`DashboardShell.tsx:59-72`).
- **App:** `_components/banners/BillingBanners.tsx` L95-100 — `text-rose-100` / `text-amber-100` (textos casi blancos pensados para fondo oscuro) sobre `bg-rose-500/10` / `bg-amber-500/10`, y tono info con `bg-primary/10 border-primary/30` (primary shadcn). Sobre la superficie clara del rediseño el texto de estos banners queda ilegible, y ninguno usa tokens EVA (danger-100/700, warning-100/700, radius-card).
- **Fix:** mapear a los tonos soft EVA (`--danger-100`+`--danger-700`, `--warning-100`+`--warning-700`, `--sport-100`+`--sport-700`), `rounded-card`, sin `backdrop-blur`.
- **Verdict:** CONFIRMED — `BillingBanners.tsx:95-100` verificado exacto: `text-rose-100`/`text-amber-100` (casi blancos) sobre tints 10%, info con `bg-primary/10 border-primary/30`, `rounded-2xl` + `backdrop-blur-md`. El tema light es el default del rediseño (`--surface-app: var(--paper)` en globals.css:392; .dark solo por clase) → texto rose-100 sobre paper claro es genuinamente ilegible, no solo infidelidad de tokens. Alcance acotado (solo estados blocked/canceledGrace/trialActive, y el tono info usa `text-foreground` legible), pero el banner danger ilegible es el CTA de reactivar suscripción → P1 correcto, no downgrade.

## [P2] FreeWelcomeModal legacy (shadcn + colores Tailwind crudos)

- **App:** `_components/FreeWelcomeModal.tsx` — `bg-card/border-border/text-foreground` + emerald/teal/sky/violet/amber crudos (`text-emerald-400`, `bg-sky-500/15`…), fuera de la paleta EVA. Es funcional (no está en el kit) y one-shot (`?welcome=free` una vez), por eso P2 y no P1, pero es la primera pantalla que ve un coach free nuevo. Fix: re-skin con tonos DS (sport/success/ember-100/600) y radius-card/sheet.

## [P2] NewsFeed: título de actividad entero en bold (kit: solo el nombre en negrita)

- **Kit:** `coach-dashboard.jsx` L246 — `<b style={{color:'var(--text-strong)'}}>{it.who}</b> {it.what}` (nombre strong-bold, acción en text-body regular).
- **App:** `NewsFeed.tsx` L132-134 — `<b class="text-strong">{item.title}</b>` con el string completo ("María completó una sesión") en bold-strong; se pierde el contraste tipográfico nombre/acción de la fila del kit. Fix: separar `clientName` del verbo en `ActivityItemClient` (o split del title) y aplicar bold solo al nombre.

## [P2] Desktop: columna de lectura 1100px vs 1240px del kit (doble cap)

- **Kit:** `.dt-dash-inner { max-width: var(--dt-read-wide) /*1240*/; padding: 28px 32px 44px }` (`eva-desktop/index.html:261`).
- **App:** `CoachMainWrapper.tsx:56-57` ya da la columna correcta (`md:max-w-[var(--dt-read-wide)]` + `px-[var(--dt-page-x)]`), pero `DashboardShell.tsx:57` agrega un segundo cap `max-w-[1100px]` adentro → el bento desktop queda ~76px más angosto que el resto de las pantallas del shell (y que el kit). Fix: quitar el `max-w-[1100px]` (o subirlo a `--dt-read-wide`).

## [P2] Mobile: gutter lateral 16px vs 20px del kit

- **Kit:** todas las pantallas mobile usan `padding: '0 20px'` (`coach-dashboard.jsx:123`).
- **App:** el gutter lo da `CoachMainWrapper` con `px-4` (16px) (`CoachMainWrapper.tsx:56`; el comentario de `DashboardShell.tsx:55` reconoce el objetivo "~20px" del diseño). 4px por lado en TODO el stack mobile. Fix: `px-5` en <md (verificando que ninguna pantalla ya compense con padding propio).

## [P2] Nits del header mobile y radios

- Avatar de workspace: kit `WorkspaceMenu` usa `Avatar size="lg"` (56px) con glyph `chevrons-up-down` y abre menú de espacios (`coach-core.jsx:252-255`); app usa `size="md"` (40px) con `ChevronDown` y navega a `/coach/settings` (`DashboardShell.tsx:104-113`). El bell (`DashboardShell.tsx:94-101`) no tiene onClick (stub, kit abre notificaciones).
- FAB sheet: cajas de icono con `rounded-control` (14px) vs kit `radius-sm` (10px) (`DashboardFab.tsx:71` vs `coach-dashboard.jsx:303`); ídem el CTA "Ver todos en Alumnos" (`PriorityCard.tsx:247`).
- Numerales del PulseHero: `font-black tracking-[-0.03em]` vs `.eva-metric` del kit (800 / -0.01em) (`PulseHero.tsx:104`); el `.eva-metric` correcto ya existe en `globals.css:1253`.

---

## Verificado 1:1 (sin gap)

- **PulseHero** (`PulseHero.tsx`): 3 stats tocables, label uppercase 10.5/800, métrica 27, delta icon+color success/danger, sufijo " sem.", sparkline (gradiente+línea+punto, 60×22) — verbatim del kit P1.
- **PriorityCard** (`PriorityCard.tsx`): card inverse con gradiente ink-900→950 + inset highlight, eyebrow sport-400, badge contador danger/success, headline display 20/900, filas riesgo con banda P5 (label+dot color fijo on-dark #FF7C97/#FFC861) + score mono n/100, empty-state check verde, NextStepInset embebido (resolver de reglas §2.7 y acentos #FFC861/#7FB0FF/#4FD9A0 exactos), CTA "Ver todos en Alumnos" sport-400.
- **AgendaCard / SectionTitle**: hora mono 42px col + icono círculo sunken + "n de N hechas"; SectionTitle display 17/800 + action sport-700 = shared.jsx.
- **NewsFeed** (estructura): filas programa (círculo danger/warning-100 + Badge soft "Vencido"/"n días") y actividad (círculo tonal sport/success/ember) con dividers mx-14 — igual al kit (salvo el bold del título, P2 arriba).
- **DashboardFab**: FAB sport 56px bottom 92+safe con glow, sheet inferior radius-sheet con handle, título "Acción rápida" uppercase, filas icono ink-950/sport-400 — verbatim; oculto en md+ (correcto: el bento expone las acciones en su header).
- **DesktopBento** vs `.dt-*`: header fecha capitalize 12/700 + h1 display 30/900 + botones Importar/Nuevo alumno (secondary/sport con glow); 4 KPIs (icono tonal 30px radius-10, label uppercase 11/800 subtle, valor 34 tabular, delta 12/600) con colapso a 2 cols <1000px = media query del kit; grid `1.5fr/1fr` gap-20; card oscura ink-950 con eyebrow sport-400 + título display 19 + darklink pill blanco/10 + filas warroom (border-top white/7, hover white/5, chevron white/40); "Programas activos" (dt-progrow: icono sport-100 32px, name 13.5/700, sub 11.5 subtle, pill assigned sunken) y "Actividad reciente" (dt-actrow: avatar xs + "N completó su sesión" + día relativo).
- **Tokens**: `--radius-card 20 / control 14 / sheet 28 / pill`, paleta sport/ember/success/danger/ink, `--text-on-dark(-muted)`, `--border-inverse`, `.eva-metric`/`.eva-mono`/`.eva-press` — todos presentes en `globals.css` con los valores del kit (`docs/design-source/tokens/radius.css` coincide).
- **Breakpoint**: <760 stack mobile / ≥760 bento (md overrideado a 760) — regla inquebrantable respetada; nada del stack mobile se filtra a desktop ni viceversa.
- **RevenueSheet/MRR ausente del render** = CORRECTO (el kit lo removió: "Ingresos/MRR removido a pedido", header de coach-dashboard-sheets.jsx). Los legacy `kpi/KpiStrip`, `focus/FocusList`, `today/TodayAgenda`, `activity/ActivityFeed`, `expiring/ExpiringPrograms`, `header/*`, `sheets/RevenueSheet`, `cs/NextBestAction`, `payments/QuickAddPaymentModal` y `components/coach/dashboard/DashboardCharts` están MUERTOS (sin imports vivos) — no son gap visual; candidatos a borrar en la limpieza.
- **Loader de marca** en vez del skeleton shimmer del kit = feature white-label intencional (no gap).
- **Datos placeholder** (deltas "+1/+3", spark sintético, horas de agenda 09:00/10:30, "0 de N hechas", pill "1" en programas): fabricados para igualar el visual del kit — regla 6, no son gap visual (sí deuda de datos a futuro).

## Fix log (2026-07-01)

- **[P0] Guía de inicio → chip expandible sport-100** — FIXED `apps/web/src/app/coach/dashboard/CoachOnboardingChecklist.tsx` (re-skin completo al patrón del kit L254-292: chip colapsable rocket + 4 dots + n/4 + chevron; expandido con filas check sport-500/300, hint Pro sparkles + "Mejorar", "Saltar guía"; al 100% card success-100 "¡Activación lista!" con party-popper + X). Motor intacto: señales auto, merge server/localStorage, `persistOnboardingGuideAction`, telemetría (`emitOnboardingEvent`/`postGuideEngagement`), confetti, toggle manual del paso marca ("Marcar visto"/"Desmarcar"), estado dismissed→chip de reanudar. Abierto por defecto solo con 0 pasos (kit). El contenido rico legacy (tabs gemelo, carousel, jump-nav, bloque Free, bloque nutrición) se removió del dashboard; los subcomponentes `_components/onboarding/*` quedan sin imports vivos (candidatos a limpieza).
- **[P1] ClientStatsSheet re-skin + bottom-sheet <760** — FIXED `_components/sheets/ClientStatsSheet.tsx`: matchMedia 760 (patrón Asignar de Programas) → `side="bottom"` con `rounded-t-sheet` + drag handle + pb-safe en móvil, `side="right"` en desktop; header display 19/800 + avg% `.eva-metric` coloreado por `barColor`; segmented control surface-sunken/surface-card + shadow-xs; caption "Ordenado por menor cumplimiento…"; filas flat con avatar ink-900/sport-400, % `.eva-mono` color-coded y barra 5px track/fill color-coded; "Nutrición" con tilde; cero tokens shadcn (se fue `Progress`, `bg-primary`, `var(--theme-primary, #007AFF)`).
- **[P1] BillingBanners tonos legibles** — FIXED `_components/banners/BillingBanners.tsx`: danger→`danger-100`+`danger-700`, warn→`warning-100`+`warning-700`, info→`sport-100`+`sport-700`, bordes `-500/30`, `rounded-card`, sin `backdrop-blur`. Tokens flipean solos en .dark. Seguro en todos los call sites (re-skin del propio componente).
- **[P2] FreeWelcomeModal tokens** — FIXED `_components/FreeWelcomeModal.tsx`: emerald/teal/sky/violet/amber → sport/ember/success-100/600(700), `bg-card/border-border/foreground/muted-foreground` → surface-card/border-subtle/text-strong/text-muted, radios → rounded-card/control/sm, CTA `bg-sport-500`.
- **[P2] NewsFeed bold solo el nombre** — FIXED `NewsFeed.tsx` + `_data/dashboard.queries.ts` (prop presentacional `clientName?` en `ActivityItemClient`, poblada en los 3 push): `<b>{clientName}</b>` + resto en text-body regular, fallback al title entero si no matchea.
- **[P2] Doble cap de ancho desktop** — FIXED `_components/DashboardShell.tsx`: removido `max-w-[1100px]` (CoachMainWrapper ya capea a `--dt-read-wide` 1240).
- **[P2] Gutter mobile 16→20px** — SKIPPED (requiere wiring externo): el px-4 vive en `components/coach/CoachMainWrapper.tsx` (shared, fuera del área dashboard); cambiarlo afecta todas las pantallas del shell.
- **[P2] Nits header/radii/metric** — PARCIAL: FIXED `DashboardFab.tsx` (caja icono `rounded-control`→`rounded-sm` 10px), `PriorityCard.tsx` (CTA "Ver todos en Alumnos" ídem), `PulseHero.tsx` (numerales → `.eva-metric` 27px). SKIPPED avatar workspace lg + WorkspaceMenu y bell con onClick (estructurales/behaviorales, no swap de clases; el menú de espacios y el panel de notificaciones requieren wiring externo).
