---
status: active
owner: product-engineering
last_verified: "2026-09-01"
canonical: true
---

# Current status

Vista global mínima: solo prioridades vigentes y punteros. El código, las migraciones aplicadas y
el estado remoto (Vercel/Supabase) prevalecen sobre este resumen. La cronología y la evidencia
viven en `specs/`, `docs/audits/` y el historial de git, no aquí.

## Estado por frente

| Frente | Estado | Fuente de detalle |
|---|---|---|
| Web/PWA | **Pricing v3 (2026-08-21, [spec](../specs/pricing-v3/SPEC.md)): Free = 1 alumno + white-label completo + sello «Hecho con EVA»; Pro 25 sin sello; backfill por uso de `coaches.max_clients` (Free con ≤1 alumno → 1; los 5 con ≥2 conservan su fila)**. Productivo; confirmar deployment activo antes de incidentes o despliegues. **QA pre-campaña Meta EN PRODUCCIÓN (2026-08-18, master = `a5ecbf3d`)**: landing sin testimonios ficticios (3 tarjetas de hechos verificables) ni promesas falsas («sin tiendas» con la app iOS publicada, «sin tracking de terceros» con el pixel activo); cupo Free derivado del catálogo en 10 superficies web+RN (el «3» del correo de beta expirada es deliberado: grandfather); alta por Google gana conversión `CompleteRegistration` (CAPI + espejo del pixel con el mismo `eid`) y la bienvenida+drip que solo emitía el camino por email; `_fbc` (click-id del anuncio) se captura antes del consentimiento; la X del banner de cookies ya no persiste un rechazo permanente y el banner no tapa el CTA sticky móvil; botón de reenvío de confirmación por fin cableado (helper huérfano desde el incidente del 16-08), con la identidad resuelta por sesión o `uid` — nunca por el email tipeado. **El Meta Pixel estuvo MUDO días por nuestra propia CSP** (`connect.facebook.net` faltaba en `script-src`); resuelto en `4115d951` y verificado end-to-end desde navegador limpio. Correo de lanzamiento iOS enviado a los 25 coaches activos (25/25 delivered) | [Runbook](../operations/RUNBOOK.md) |
| App nativa (RN) | **29-08: guard de acceso RN contaba al alumno DEMO** (`lib/coach-access.ts` sin `is_demo = false`): 6 coaches free v3 con demo + 1 alumno real caían en `/coach/reactivate` y «Continuar gratis» rebotaba con 403 (status ya `active`) — deadlock solo en la app, la web nunca gateó mal. Fix = mismo predicado que `capacity.service.ts` + test; **pendiente OTA 1.1.2 android/ios**. Detalle en [MOBILE_PARITY](MOBILE_PARITY.md). · **28-08: «+ Nueva» pregunta qué crear (web + RN, spec [library-new-choice](../specs/library-new-choice/SPEC.md), mockup `9d979bfa` aprobado) + filas legibles de la hoja «Entrenamiento incompleto» del alumno RN + 25 tests de `register`/`onboarding complete` VERDES otra vez (mock de `cookies()` tras `1757195f`; +2 tests del fallback cookie `eva_utm`). Salida: deploy web + OTA android/ios a 1.1.2 (grupos en [MOBILE_RELEASES_OTA](../operations/MOBILE_RELEASES_OTA.md)); QA device/navegador del owner pendiente.** **21-08: `/join` standalone pasa a solicitud al coach** (spec [coach-leads](../specs/coach-leads/SPEC.md)). **2026-08-20: tres runtimes conviven en el canal `production`** — 1.1.0 (público: App Store 54 + Play closed 81), 1.1.1 (57/84, en revisión de Apple) y 1.1.2 (58/85, TestFlight + Play closed, lleva Share Entreno de la rama `rnmobiledenuevo`, sin merge). Los OTA del 19-08 desde `2fdecebd` salieron a runtime **1.1.1** (no a 1.1.0): hoy se publicaron a 1.1.0 (`ota/1.1.0-20260820` = `39e64ff8`: días anteriores + share iOS + cupos Free) y 1.1.1 (`ota/1.1.1-20260820` = `b4d958fa`: share iOS), android e ios, todos verdes — procedimiento en [MOBILE_RELEASES_OTA](../operations/MOBILE_RELEASES_OTA.md#partición-de-runtimes-vigente-2026-08-20). Web `master` recibió el hotfix del alta (`0be1130e`: bienvenida+drip con `await`, `register_failed` instrumentado) y la key de PostHog para builds/OTAs (`a2cca2a5`). **Histórico 18-08 — OTAs desde `4de66a5c` (android + ios, canal production, runtime 1.1.0)**: tanda del QA en device del dueño — equivalencia del reemplazo visible para el coach (solo lectura; la edición de cantidad se DESCARTÓ), teclado que tapaba «Notas para tu alumno», sheets `nativeModal` al borde en Android (`navigationBarTranslucent`, toca ~68 sheets ⇒ pide pasada visual), «Aplicar» plantilla desde la pestaña Plantillas (paridad web, 2 toques) y foco del tour que caía en la barra de estado. **SIN QA en device todavía.** Antes, `ebfbec11` cerró un BLOQUEO DURO: guardar una plantilla desde la app fallaba SIEMPRE («La plantilla tiene datos inválidos») porque Hermes no define `globalThis.crypto` y el fallback de ids emitía `synthetic-…`, que el contrato rechaza con `z.string().uuid()`; CI no lo cazaba porque en Node sí existe. Deuda viva del tour: la tarjeta queda bajo la tab bar y el arreglo real es subir el punto de montaje del overlay al layout raíz (tanda propia). 🔴 `eas update --platform all` FALLA (exporta web sin `react-native-web`): siempre android e ios por separado | [Mobile parity](MOBILE_PARITY.md) |
| App nativa (RN) — histórico | OTAs android `a93d01a3` + `901dc471` (2026-08-15, commit `dd43c45c`): T2.6 F6 + T2.7 re-skin + fixes 12-08 + copys App Review, **QA en device del owner COMPLETO (rondas 1 y 2 OK)**. iOS 1.1.0 (54) en App Review (reenviada 15-08 tras el 4º rechazo): no cancelar, no build nueva; al aprobar, replicar los OTA con `--platform ios`. **Las tres deudas RN que esta fila declaraba están cerradas** (corregido en el saneo documental del 19-08, verificado contra código): la paridad del tab Alimentos RN cerró el 11-08 (`6c4da722` los 5 modos, `949a1eab` crear, `a0b976e7` clasificar), T2.6 F4 porción pegajosa la cerró T3.3b W3 (`a95f1811`) y el nivel día de T2.6 F2 cruzó con la gramática compartida (RN despacha `COPY_VARIANT_TO_DAYS` con Reemplazar/Sumar). Lo único que queda sin puerta es el quick-select «próximos 1/2/4» del menú de la **franja**, hoy inalcanzable también en web: colgaba del wizard retirado en `0355d67d` | [Mobile parity](MOBILE_PARITY.md) |
| Archivado de alumnos | Migraciones y fixes del P0 de alta en producción (2026-08-03); falta QA físico y matriz Team | [Spec de corte](../../specs/archive-nutrition-v2-cutover/SPEC.md) |
| Nutrition V2 | Canónica para Standalone/Team. En prod web: T2.1+T2.2 overrides, T2.3 hub de alimentos, T2.4 sustituciones FULL y **T2.5 intercambio por grupo (swipe ⇄ + sheet 2 bloques)** — merge `654efd33` (2026-08-10) + OTA android `7a9b3877`; 2 migraciones más en LIVE (`20260810161604`, `20260810171529`); incluye la UI optimista del Hoy web (`5139b29e`) y cierra el reparo offline de T2.4 (chip "En cola"). H1 y D5 se cerraron el 2026-08-11 con T2.5 F8 (ver prioridad 3; esta línea decía «abiertas» hasta el saneo documental del 19-08). **T2.6 velocidad de autoría CERRADA en rama `rnmobiledenuevo` (2026-08-13, sin merge a master)**: F1 gramática destructiva unificada (undo de franja, web+RN), F2 copy semana (quick-select "próximos N" + modo Sumar en el día; franja con quick-select por decisión A), F3 tabla `coach_food_last_qty` + 2 RPC en LIVE, F4 porción pegajosa (3 caminos de precedencia verificados en DB), F5 notas visibles editables en el wizard (web+RN), F6 paridad RN parcial declarada en MOBILE_PARITY (F2/F4 RN no cruzan). T2.7 re-skin cerrada (2026-08-15); **T3.x editor único: T3.1→W4 COMPLETAS el 2026-08-15 en rama `rnmobiledenuevo`** ([programa](../specs/nutrition-unified-editor/SPEC.md)) — editor en `/coach/nutrition-v2/[clientId]/editor` edita el plan vigente y crea (`?from=template:|plan:` o blank) con metadatos, sustituciones editables, override, duplicar/copiar día, porción pegajosa, cápsula de día activo, totales fijos y paleta desktop; QA visual del owner en preview OK y **corte W4 hecho** (CTAs web al editor; quick-edit clásico y wizard como secundarios en el menú "..." durante la ventana de retiro de 2 semanas). **T3.2b modo plantilla CODIGO LISTO en la rama (2026-08-15, sin corte)**: ruta `/coach/nutrition-v2/plantillas/editor` (`?template=` editar / sin id crear) sobre el mismo lienzo — guardado sin CAS (paridad wizard), sin vigencia, descripción editable, banner permanente, degradación avisada; harness `?mode=template` headless verde con regresiones create/edit. El **corte 2** (CTAs de `PlanTemplatesLibrary` + redirect legacy → editor) **espera QA del owner en preview** (mismo protocolo que W4). **R1 extracción de reducers CERRADA (2026-08-16)**: la gramática del editor (ex `quick-edit-state.ts`, 2.400 LOC) vive en `packages/nutrition-v2/editor-state.ts` (+ `editor-food`/`editor-portions`, re-exportadas por el wizard desde sus rutas históricas); web consume el paquete (16 importadores), suite completa del reducer corre contra el paquete, `tsc` mobile y `expo export` verdes. **T3.3a CERRADA EN CODIGO (2026-08-16, SIN QA device)**: el quick-edit del coach RN consume la MISMA gramática — murieron el reducer RN y el paralelo de porciones; publish por proyección compartida; convergencias declaradas en MOBILE_PARITY (lápiz T2.2 solo con food hidratado, diff id-based, respaldos v1 descartados). **QA del owner CERRADO el 2026-08-16 (preview de plantillas + device Android del quick-edit convergido: ambos OK)** ⇒ **corte 2 HECHO**: las CTAs de `PlanTemplatesLibrary` y el redirect legacy `nutrition-plans/new` apuntan al editor de plantillas, y el wizard de plantillas queda solo por URL directa. **T3.3b CERRADA EN CODIGO (2026-08-16, SIN QA device)**: el editor unico existe en RN (`/coach/nutrition-v2/editor/[clientId]` + `plantillas/editor`) con metadatos, creacion `?from=`, reemplazos editables, duplicar/copiar dia, reorden, capsula de dia activo, totales fijos, **porcion pegajosa (cierra la deuda T2.6 F4 RN)** y plantillas por un camino de escritura movil nuevo (`saveTemplate`); el **corte RN** ya apunta ficha, hub, roster y picker de plantillas al editor, con el par viejo como camino secundario. **T3.v Cabina (pasada visual del editor único) CERRADA EN CÓDIGO (2026-08-16, SIN QA del owner)** ([spec](../specs/nutrition-editor-cabina/SPEC.md)): MacroSpark + popover de gramos (contrato D3) en todas las superficies del editor web y RN, fotos reales de producto con fallback, cinta ≥768 con anillo semántico y «Metas del día ▾» (hospeda `TargetsEditorCard`), rail de días ≥1024 con estados verde/ámbar y B4 como enlace, responsive 390–1536 verificado con Playwright headless (41 asserts × 5 anchos × 2 temas), `macroCalorieShares` único en `@eva/nutrition-v2`; builder y área alumno diff CERO; gates completos verdes (typecheck web+mobile, vitest, tokens, boundaries, `expo export android`, build, docs). **OTA android ACUMULADO PUBLICADO (2026-08-17, decisión owner de adelantarlo al QA): grupo `aca6fc76` / android `01a00d3d`, runtime 1.1.0, canal production, vía GH Actions run 31983615828 desde `73b227ba`**. **Paquete post-QA CERRADO EN CÓDIGO (2026-08-17)**: fixes del QA del owner (cinta 768 sin solapes + gate anti-solape, hub RN = tablist de la web con Porciones rescatada y su fetch reparado — z.guid() por UUIDs de seed no-RFC —, centrado 2xl 49px→0, cantidad/unidad estable web+RN, chevrons animados) + **«Familia N»** (AddActionButton con iconos clay del owner en TODAS las altas web+RN, contraste white-label dinámico verificado en DOM) + **Guía Viva SPEC/PLAN/TASKS** ([spec](../specs/nutrition-onboarding-tour/SPEC.md)); **OTA android #2 acumulado PUBLICADO** (grupo `7738d234` / android `01a00e11`). **Guía Viva IMPLEMENTADA (2026-08-17, workflow 5 workers + juicio del jefe)**: motor spotlight propio en web (`components/nutrition-v2/tour/` — posicionador puro clamp/flip/dock, flags `eva.tour.<id>.v1.<coach>` SSR-safe fail-closed) y RN (`Modal` translúcido + registro de targets por `measureInWindow`, AsyncStorage con la MISMA clave — test de contrato cruzado web↔RN), montado en el editor (guion 8 pasos ≥768 / 6 compacto) y en el hub (6 pasos, copys literales del artifact aprobado, incl. Curación); «?» de 30 px con D2 garantizado por layout (doble instancia CSS por el `backdrop-filter` de la PublishBar en web; flotante sobre la barra en RN); gates geométricos BLOQUEANTES en `cabina-visual-check.mjs` (273 asserts: D2 cero solapes, D3 tarjeta ⊆ viewport en cada paso × 5 anchos × ambos tours, D4 flag/reload/reiniciar — probados en rojo); fix del juicio: `findTarget` ya no devuelve targets CSS-ocultos (pinhole fantasma 768-1023). Suite 5776 verde (de paso: test stale del tablist RN actualizado al contrato «web es JEFE» y test de register reparado tras el commit Meta Pixel `7df9aa6c`). **Sello EVA v2 IMPLEMENTADO (2026-08-17, workflow 4 workers + juicio)** ([spec](../specs/eva-seal-background/SPEC.md)): fondo «Horizonte B» por defecto en toda la app logueada — `sealPair` en `@eva/brand-kit` (par curado del preset del catálogo o derivado H+38°, modo estricto W-brand B2, 22 tests golden), `AppSeal` web (blobs normativos 46s/58s motion-safe + grano; montado en shell coach vía `CoachMainWrapper` no-builder, shell alumno `/c`, y el overlay del editor con el fondo COMPLETO por la reversa de D2 — la variante `grain`, que quedó sin consumidores, se borró el 2026-08-19; login apagado ESTRUCTURAL vía `:has(.login-brand)`; print apagado), layouts publican `--seal-p/s-rgb` junto a `--theme-*`, `check:tokens` gobierna 5 tokens seal web↔RN, RN `AppBackground` v2 (grilla RETIRADA, SKY muerto → par del tema, deriva Reanimated UI-thread pura con reduce-motion + kill-switch `animated`, grano jamás anima), gate visual ahora **308 asserts** (hue exacto de blobs × 2 marcas × 2 temas, contraste WCAG sin cambio con sello ON, reduced-motion congela, pre-auth sin marcador — probado en rojo). 2ª ronda de fixes del QA del owner en el MISMO paquete: roster `align-middle`, hueco muerto del slot card <md, chips «Verificado» solo-check (web+RN), tarjeta dock del tour flipea arriba si taparía su target (paridad web/RN, contrato testeado), altas «Agregar franja + Agregar día» en una fila (web+RN). **Pricing v2 EJECUTADO (2026-08-17, workflow 9 workers + juicio, [spec](../specs/pricing-v2/SPEC.md))**: catálogo Free 2 / Pro 25 / Elite 60 con **grandfather por fecha de corte** (`tierMaxClientsFor(tier, created_at)`, corte 2026-08-18: todo coach existente conserva 3/10/30/100 — escritores activate-free/trial-expiry/confirms/webhook y lectores clients/mobile/archive migrados; gates web+mobile de over-capacity grandfather-aware), free full-features (capabilities + 4 módulos vía `hasPaidModuleAccess` sin `!= 'free'`; addon nutrition_exchanges = `included_in_plan`), starter FUERA de venta (SALE_TIERS/enums de pago/cupones lo rechazan; queda en union/CHECK patrón growth/scale), 35 `?? 'starter'` → `'free'` + los 4 `return 'starter'` de normalizadores legacy, `joinViaInviteAction` gana cerco (`checkJoinCapacity`: standalone por límite grandfathered, enterprise por `client_limit` de la org; team sin cuota de alumnos POR SCHEMA — documentado, DDL si el owner la quiere), superficies a 3 planes (/pricing sin agrupado + JSON-LD, landing ×2, register, i18n sin Starter), reactivate web comunica el límite REAL del coach (viejo ve 3) + card Free dispara activate-free, RN reactivate cupo dinámico sin precios (stores), PostHog `pricing_viewed/plan_clicked/checkout_started/confirmed` gateado por consentimiento. Suite 5889 verdes, lint 0 errores, tsc ×2, tokens 86+5, boundaries, visual 308, build web, `expo export` android — todo verde. Decisiones pendientes del dueño anotadas en la SPEC (tramo 17.990 post-Meta, WhatsApp 9 pros, cupones starter históricos, IVA = estudio artifact `63daf16b` con hallazgo boletas). OTAs #3 (`a4dafcea`) y #4 (fix tour RN + sello en editor) PUBLICADOS 17-08. **QA del owner CERRADO 17-08 (3 rondas: web preview + device android; todos los hallazgos arreglados en el día)**. **RETIRO DEL PAR VIEJO EJECUTADO (2026-08-17, adelantado por el owner tras su QA)**: cero puertas de usuario al quick-edit clásico y al wizard en web y RN (inventario exhaustivo + verificación adversarial con suite 5933 + gate visual + probe de los 3 modos del harness); las rutas del wizard redirigen al editor único preservando `?from=`; el CÓDIGO del par queda intacto (demolición = tanda aparte post 2026-08-30; huérfanos F2-F4 y el canary E2E skipeado documentados para esa tanda). **NOTAS DEL COACH IMPLEMENTADAS (2026-08-17, [spec](../specs/nutrition-coach-notes/SPEC.md))**: botón 📝 por franja (`instructions` ≤2000) y por grupo de porciones (`notes` ≤1000) en el editor web y RN (sheet con contador y limpiar, tinte de marca con-nota, gramática/undo/publish del reducer con topes espejados), y el ALUMNO las ve como banda 💬 en el Hoy web (`/c/*/nutrition-v2`) y RN — el camino de datos ya existía completo (snapshot + `get_nutrition_today_v2` + read-models Zod), CERO DDL. Con esto el programa de rediseño de nutrición queda CERRADO de punta a punta; frente vivo del paraguas: T3.4-T3.7 (backlog) + regen `database.types`. **V5.1 de la Cabina CERRADA CON EVIDENCIA (2026-08-19)**: el OTA acumulado final del día salió el 17-08 (grupo `768389fb` / android `01a010fd`) y `eas update:insights` corrió hoy sobre los 5 grupos android del 17-08 — `aca6fc76`/`01a00d3d` (2 launches, 2 usuarios únicos, 4,93 MB), `7738d234`/`01a00e11` (0), `a4dafcea`/`01a00f74` (1 launch, 1 usuario, 5,16 MB), `dff7edc8`/`01a010a4` (0) y `768389fb`/`01a010fd` (1 launch, 1 usuario, 4,92 MB): **0 failed launches y crash rate 0,00 % en los cinco**. Los dos grupos en cero los superó el OTA siguiente el mismo día, no son fallas; la corrida anterior (15-08, sobre los grupos vigentes entonces) también había dado 0 crashes / 0 failed installs con adopción 1-4 usuarios únicos/7 d. iOS dejó de estar rezagado: desde el 18-08 los OTA salen android **+ ios** sobre el mismo commit y un update trae el bundle completo (no un delta), así que el iPhone recibe todo el paquete del 17-08 sin republicar grupo por grupo. Preflight mantiene 7 enlaces V1→V2 por reconciliar | [Programa](../specs/nutrition-flows-redesign/TASKS.md) · [Runbook de corte](../operations/NUTRITION_V2_CUTOVER_RUNBOOK.md) |
| V1 nutrición | Congelada, **no se borra** (decisión owner 2026-08-03): solo migrar usuarios a V2 | [Delta del mapa](../audits/v1-deprecation-map-delta-2026-08-03.md) |
| Teams | Pool, membresías y workspaces implementados | [Flows](../architecture/FLOWS_AND_COMPONENTS.md#team) |
| Enterprise | **Congelado (cuarentena 2026-08-06)**: sin desarrollo activo ni gates locales; `apps/enterprise` permanece en el repo hasta un plan de retiro propio. El e2e de aislamiento RLS sigue en CI manual | [Flows](../architecture/FLOWS_AND_COMPONENTS.md#enterprise) |

## Prioridades vigentes

0. **Ola de orden W1→W4 «La casa ordenada» — EN PRODUCCIÓN (web + OTA 1.1.2) (2026-09-01, [SDD](../specs/ola-de-orden/TASKS.md); mockups `9801fec7` (W1: 1A 2A 3A 4A) y `bff90120` (W2→W4: 1A 2A 3A 4A 5A 6A 7C 8A 9A 10A 11B) aprobados por el owner):** 35 commits locales en `rnmobiledenuevo` (`11ebbbbc..be2d0cdb` + este cierre documental`), pusheados como preview de Vercel el 2026-09-01 (`https://gymappjp-git-rnmobiledenuevo-juancho2706s-projects.vercel.app`). **W1** apagar un dominio cierra la ruta de verdad (gates web con redirect + status `domain_off` in-page, banner del dashboard, `useDomainGuard` + `DomainOffNotice` en 7 pantallas RN, pestañas de la ficha por dominio, `FEATURE_PREFS_ENABLED` retirado). **W2** sidebar web en Principal / Tu trabajo / Gestión (Cardio, Movimiento y Soporte por fin visibles), barra RN «Inicio · Alumnos · 2 dominios por especialidad · Más» + hoja «Más» (cierra el `.slice(0,5)` que le tiraba «Equipo» al coach de team), FAB por dominio. **W3** una sola pantalla «Funciones» (web y RN) que absorbe Módulos + Mi panel + Herramientas, con «Abrir» por dominio prendido; las 4 rutas viejas redirigen; hub y rail con una entrada; «Mi panel» pasa a llamarse «Funciones» (una constante). **W4** todo-incluido: sin candados ni «Pro/Base» en Funciones, `ModuleOffNotice` = «temporalmente no disponible» (kill-switch de operador, sin CTA de pago), `hasActiveModuleAccess`, `canUseAdvancedReports` y 2 flags muertas fuera, tab «Check-ins» huérfana borrada, buscador global RN cableado en el header del dashboard, y **deltas REALES en los KPI del dashboard** (7C fase 1: altas 7 d sin demo, adherencia vs. semana previa, sesiones vs. ayer; `riskCount` ya no está topeado a 5). Ejecución: jefe (esta sesión) + sesión «Asistente Principal» (W4 no-visual, 7C), workers Opus/Sonnet, commit por tarea. **Decisiones del jefe, pendientes declarados y lista de QA por wave en TASKS** (§ «Juicio del jefe» W1/W2/W3/W4). **Suite completa al cierre (2026-09-01): lint 0 errores · typecheck web · test 8262/8262 (tercera corrida sin fallos; las dos anteriores tuvieron 1 y 3 timeouts por carga de CPU que pasan aislados) · tsc mobile · check:tokens · nutrition-v2-boundaries · docs:check · expo export android.** **QA en celular del owner VERDE (01-09)** con 3 pedidos + 2 decisiones ejecutados el mismo día (TASKS § «QA del owner ronda 2»): Herramientas fuera de Alumnos, «Cerrar sesión» en «Más», **orden de la barra editable** (fila `_nav` de `coach_feature_prefs`, web y RN), switch por dominio del pool para gestores de team, **7C fase 2** (snapshot diario `coach_kpi_snapshots` + cron; primer delta de «En riesgo» el 08-09) y copy de Suscripción. **Segundo tren EN PROD (01-09 23:50Z): `master` = `935cd4c8`, deploy `dpl_6rRPbnuJtxKYUJVFxgsNAtmkBqMV` READY, OTA 1.1.2 android `27e920aa` / ios `1677cf76`.** Con el QA final del owner sobre este tren la SDD pasa a `status: done`. Regla nueva del owner: ENTERPRISE eliminado (no tocar; demoler a futuro). Fuera de la ola (declarado): 7C fase 2 (delta de «En riesgo» con snapshot diario), cápsula móvil web con `.slice(0,5)`, W4.6c (Facturación RN, espera Cobros), CTAs de plan en nutrition-v2 RN.
0. **Errores al día (2026-09-01, [tareas](../specs/cierre-sentry-vivos/TASKS.md) § O6):** 13 issues vivos de Sentry + 8 grupos de Vercel + señales PostHog verificados contra `HEAD` con 15 agentes y refutadores. Ola O6 en código (sin UI nueva): `.catch` en 32 server actions fire-and-forget (`EVA-NEXTJS-19`/`-3`), fecha de PRs con tabla fija sin `Intl` (`EVA-NEXTJS-18` regresado en el dashboard del alumno), fingerprints en los 8 `captureMessage` (RN + Despegue web; el diagnóstico de `EVA-MOBILE-9` era erróneo), `PLAN_ALREADY_ACTIVE` → diálogo de recarga + log `warn`, `after()` del proxy sin rechazo no manejado, debounce del preview de volumen (P6), programa embebido en la query del ejecutor RN (O4.4), panel «Así lo ve tu alumno» plegable en desktop (reporte de un coach) y **Dependabot a cero** (overrides `decode-uri-component` 0.5.0 #99, `browserslist` 4.28.8 #101/#102, `postcss-selector-parser` 6.1.4 #100). **EN PRODUCCIÓN 01-09 17:1xZ**: `master` = `rnmobiledenuevo` = `231d2937`, deploy web READY, OTA 1.1.2 tren 1 (android `66e32589` / ios `b53e8e5d`) + tren 2 (android `d2f948a0` / ios `d40564a9`), detalle en [MOBILE_RELEASES_OTA](../operations/MOBILE_RELEASES_OTA.md). Sentry: 18/19/1C/8 «resolved in next release», Z/1A/3/MOBILE-C/D/A resueltos, MOBILE-E/B archivados hasta escalar; quedan abiertos solo MOBILE-9 (se reclasifica solo con el fingerprint) — **verificar a 72 h (~04-09) que nada regresó (O6.8)**. **QA device del owner VERDE (01-09)**. Skia (`EVA-MOBILE-E/B`) = upstream, exige build nativo 1.1.3 — decisión del owner. P5 `noUncheckedIndexedAccess` medido (431 errores) → tanda propia; P4 exige QA de fluidez. **Frente cerrado; lo siguiente es la Ola de orden W1** (SDD `docs/specs/ola-de-orden` commiteada como `draft` el 01-09; `docs/specs/cobros-coach-alumno` también versionada, sigue esperando P1–P8). PostHog no tiene error tracking habilitado (0 issues); señal UX: rage clicks concentrados en el ejecutor web (`/c/*/workout/:id`, 262 en 7 d de 11 alumnos de un coach) y en `/coach/builder/:id` (26, 10 coaches) — sin acción, medir con un replay antes de tocar. **Preflight «Ola de orden»** hecho: 46/48 refs `archivo:línea` exactas, 0 rotas, 11 supuestos de W1 confirmados; corregida la premisa de W4.5 (3 de las 4 capabilities «muertas» siguen gateando rutas); no hay E1–E4 pendientes en la SDD (se absorbieron en D1–D6). **Decisión del owner 01-09: arranca la Ola de orden por W1 «Interruptores de verdad»** (Cobros y Yoga esperan). Precondiciones de W1 antes de tocar código: valor vivo de `FEATURE_PREFS_ENABLED` en Edge Config y muestra de coaches reales con `_enabled=false` (SPEC §6.3 y blast radius).
0. **PLAN «Cobros coach → alumno» (2026-08-29, [spec](../specs/cobros-coach-alumno/SPEC.md), status draft, SIN implementar ni commit):** motor de cobro del coach a su alumno (tres modos: transferencia · link MP por ciclo · suscripción MP), aparte del billing EVA↔coach (segunda app MP «EVA Cobros», webhook `/api/cobros/mp/webhook`, 16 migraciones, estado derivado de `paid_through + gracia 0/3`), solo Pro, beta cerrada. Artifact «El motor de cobros» `046f3bb1`. **Bloqueado por 8 decisiones del owner (SPEC §18.1) y 3 verificaciones externas (§18.2: contador SII, abogado retracto, smoke MP con plata real).** Estimación 26-32 días-agente + 2-3 semanas de beta.
0. **Pricing v3 EN PRODUCCIÓN (2026-08-21, [spec](../specs/pricing-v3/SPEC.md), commit `2edea500` = `master` = `rnmobiledenuevo`).** Free = 1 alumno + white-label completo + sello «Hecho con EVA» en las superficies del alumno; Pro 25 sin sello; `tierMaxClientsFor` con tercer peldaño (pre-v2 3 · v2 2 · v3 1). Backfill por uso aplicado en LIVE (`20260821153527`: 31 Free con ≤1 alumno → 1, respaldo `_bak_pricing_v3_free_limits_20260821`; los 5 con ≥2 conservan su fila; `robin-coach` sigue 3/5 por decisión del owner). Verificado en prod (`/pricing`, `#precios`, login Free con marca + sello, login Pro sin sello). OTA a los tres runtimes (tags `ota/*-20260821`). Insights PostHog `I5QSlFSq` / `9cVq1F6v`. Correo «Tu plan Free ahora incluye tu marca» enviado desde el panel admin el 21-08 21:31Z (34/34). **Queda solo el QA visual del owner en device (sello en login/perfil del alumno, Mi Marca abierta en Free) y archivar la spec cuando se dé por cerrada.**
   - **Embudo Free→Pro W0–W5 EN PRODUCCIÓN (2026-08-22, [spec](../specs/embudo-free-pro/SPEC.md)): W0+W1 `1a82fcb7`/`f8ed12f5` · W3 `866bea6d` (`?next=` con allowlist: el correo de cupo aterriza en el checkout; Google verificado por el owner) · W2 `0e42d480` (drip Free=1 `day1_value/day2_pro/day7_nutrition/day14_last_call`, ledger `coach_email_ledger` en LIVE `20260822004243`+`20260822005701`, webhook Resend creado y con secreto en Vercel, cancelación del drip al pagar, alta por Google desde la app ya manda correos) · W4 `55c57222` (reenvío de confirmación móvil por `uid`, índice `20260822015421`) · W5 `4d0c6851` (sello → `/hecho-con-eva` sin precios, `/register?tier=free` sin grilla, RN sin `TIER_CONFIG` en bundle). **W6 `a1c2f2f9`** (muro de cupo con «Archivar un alumno», medidor, Mi plan con pull-to-refresh y celebración, allowlist de URLs en novedades). **OTA W4+W5+W6 PUBLICADO 2026-08-22 05:1xZ** a 1.1.1 (android `d4df67f0` / ios `b337b986`) y 1.1.2 (android `3974482f` / ios `caa038a2`); **1.1.0 ya no recibe OTA** (regla del owner: piso = versión aprobada por Apple, ver [MOBILE_RELEASES_OTA](../operations/MOBILE_RELEASES_OTA.md#regla-del-piso-owner-2026-08-22-ota-solo-desde-la-versión-aprobada-por-apple-hacia-arriba)). **Segundo OTA del día (14:45Z)**: `ota/1.1.2-20260822b` = `8cf7b886` (android `17fa6905` / ios `93834df2`: fixes W6.10 del QA del owner + onboarding v2 RN + header `x-eva-platform` en `apiFetch`) y `ota/1.1.1-20260822b` = `abbd7f87` (android `230bd40b` / ios `65033c6d`: port mínimo, solo W6.10 + header). W2.7 cerrada: 53 correos del drip viejo cancelados en Resend. QA visual `/hecho-con-eva` y `/register?tier=free` aprobado por el owner. **Tarde del 22-08**: W4.8 sanación del coach Free confirmado fuera de `/auth/confirm` (`9308e264`+`9a7800f3`: helper `activateConfirmedFreeCoach`, idempotente, llamado desde confirm/verify-email/reenvío web/reenvío móvil; 2 coaches reales destrabados en prod a las 15:19Z) · W4.9 link de acceso sin `//` · W6.11 el alta corta del home abre el muro de cupo · W6.12 el WhatsApp/compartir usa la plantilla por persona sin EVA (`443aa350`; OTA `ota/1.1.2-20260822d` `d8af5381`/`4bd9dfd2` y port `ota/1.1.1-20260822c` `b7c249d3`/`0404b8be`) · W4.9 `publicAppUrl()` única base del link del alumno (verificado en prod) · W4.10 preview de WhatsApp con la marca del coach (`api/og/[coach_slug]`) · W4.11 mensajes sin 👋 (`d2707323` web, `4a914dc0` RN; OTA `ota/1.1.2-20260822e` `e3267430`/`c2bfd97c` y port `ota/1.1.1-20260822d` `4c163825`/`4956af62`). · W4.12 QA del owner en device: confirmar desde el correo vuelve a la app en Android (`intent://`, `c6df14ff`), «Ya confirmé» entra solo al panel (credenciales del alta en memoria), registro sin pill y contraseña visible (`9f6ca283`; OTA `ota/1.1.2-20260822g` `f17092d4`/`d1abbacd` y port `ota/1.1.1-20260822f` `f66dab4b`/`c76712a9`) · OG del coach sobre negro, con su logo y sin sello (`420f76f8`). · W6.13 Recorrido B aprobado por el owner con 3 retoques de Mi plan (cupo en una línea, callout Android, celebración con figura EVA; `9b2f15f5`; OTA `ota/1.1.2-20260822i` `28ca5f8d`/`8cced802` y port `ota/1.1.1-20260822g` `3a22af0b`/`11b5d8af`). Pendiente owner: QA en device W4.6 + W6.8 (guía paso a paso en el artifact `00246733-723b-456f-82c0-f14c8588d137`) y App Store Connect (W7.4).** Histórico del arranque: Decisión cerrada: sin IAP; iOS solo estado, Android una línea sin link, venta por correo/WhatsApp. **W0**: los 4 call sites de `sendClientLimitReachedEmail` pasan de `void` a `await` (el helper nunca lanza; el patrón fire-and-forget ya perdió bienvenidas el 19-08). **W1**: cron nuevo `/api/cron/cap-nudge` (`0 13 * * *`, #11 en `vercel.json`) que barre coaches Free activos standalone con `alumnos_activos ≥ cupo` y reusa el correo de cupo con **variante `sweep`** (sin «intentaste agregar», pie de baja en texto plano por Ley 19.496 art. 28 B, UTM `cap_email`); **escalera 0/7/28 d, máx 3 toques por nivel de cupo** (Free = 1 deja a 11 coaches «en cupo» para siempre: un semanal eterno era spam), ledger **fail-closed**, fail-open por coach, resumen `cron.cap_nudge_ran` siempre, throttle 600 ms, `?dry=1`. Verificado contra LIVE por el revisor: **38 candidatos / 15 en cupo / 0 de prueba / 0 envíos históricos** ⇒ la primera corrida manda 15 correos. Gates: lint 0 errores, typecheck, suite completa 6150 verdes, docs, tokens, boundaries, build (detalle en [TASKS](../specs/embudo-free-pro/TASKS.md) W1.8). **Primera corrida: `?dry=1` desde entorno seguro antes de dejarlo solo.** Deuda declarada: el coach SIN sesión aterriza en el dashboard y no en el checkout hasta W3 (`?next=`); la atribución no se pierde (el proxy clona el query al mandar a `/login` y el `$pageview` lleva `utm_source=cap_email`). Mapa de W3 verificado (read-only) en TASKS.
   - **Onboarding del coach v2 — W1–W4.5 EN MASTER y W4.6 + W5 RN + W4.7 EN LA RAMA (2026-08-22, [spec](../specs/coach-onboarding-v2/SPEC.md), [tareas](../specs/coach-onboarding-v2/TASKS.md)).** Persona al primer ingreso (web + app), `clients.is_demo`, alumno de ejemplo por persona, guía en pantalla propia `/coach/guia` (decisión del owner: dashboard limpio) con píldora flotante (web, PWA y RN), primera entrada = guía para todos los planes, tareas guiadas (alta en 3 pasos, primera rutina con vista del alumno, pauta/screening/zonas con `?primera=1`), **«Vive tu app» — el QR quedó SOLO para escritorio: desde el 2026-08-26 el móvil navega directo al demo en el mismo gesto (web y RN), y el paso 2 deja de tildarse al pedir el link: se tilda cuando el coach ENTRA de verdad, por el evento server-side `vive_tu_app_entered` (spec [vive-tu-app-directo](../specs/vive-tu-app-directo/SPEC.md), commits `2d19e237..e7ed1de9`, `VIVE_TU_APP_ENTERED_CUTOVER = 2026-08-26T06:00:00Z`)** — el funnel reportaba 100 % de un paso que convertía 33 %; la vuelta al panel es un toque (banner de sesión demo con magic link en cookie o deep link `eva://coach/guia` en RN), el login del alumno reconoce al coach y le ofrece su login, y el panel admin dejó de contar al alumno de ejemplo en el cupo. **Cierre 26-08: QA device del owner APROBADO (matriz completa: entrar/volver un toque, chip «Entró hace X» web+RN, auto-alta bloqueada, admin activos=cupo) y G1 CERRADO — App Links VERIFIED en ambos dominios tras `e4187269` (spec vive-tu-app-directo → `status: closed`). Quedan de esa spec: insight de PostHog (V5.4) y el test manual GoTrue recovery-vs-vta (V2.13); observación cosmética del QA: demo de rehab con ejercicios sin video — fix en curso (resolver prefiere media + blueprint).** Sigue vigente: un solo onboarding por área (modal y checklist viejos borrados en web y RN; tours de módulo no auto-arrancan con la guía activa), gate visual de la guía (159 asserts), modal de código público solo para coaches anteriores al 23-05. Commits locales sin push al cierre del 22-08 (el owner pushea y hace QA). **Pendiente:** W6 correos por comportamiento (sobre el ledger del embudo), W7 medición/Playwright/Maestro, F5.3–F5.5 RN (nav por dominio, alta compacta, tarjetas del builder), revisión D4 del contenido de ejemplo.
1. **iOS al 2026-08-28: 1.1.2 APROBADA y en la tienda (`READY_FOR_SALE`, leído por la API de ASC en el run 33212983838; la build en revisión era la 59 del tren del 26-08) ⇒ el piso OTA sube a 1.1.2 y salió el OTA iOS 1.1.2 desde `master` `1e25229c` (keypad; grupo `d402f897`). 1.1.3 NO tiene motivo todavía: cero cambio nativo desde la build 59 (`package.json`/lockfile/plugins intactos; lo único de binario pendiente son los intentFilters Android estrechados de `34d1187a`) — se construye cuando haya cambio nativo real, no antes. Android sigue en 1.1.2 (build 86 del 21-08, closed testing Alpha; NO existe binario 1.1.3). Población 7 d (`eas channel:insights`): 1.1.1 = 13 embebido + 49 OTA únicos · 1.1.2 = 20 + 39 · 1.1.0 = 2 + 4 ⇒ 1.1.1 deja de recibir ports salvo excepción.** Histórico 21-08: 1.1.1 (57) APROBADA (`READY_FOR_SALE`); 1.1.2 (58) enviada a revisión (`WAITING_FOR_REVIEW`, 02:23 UTC) por la API de ASC ([procedimiento](../operations/MOBILE_RELEASES_OTA.md#enviar-a-revisión-de-apple-sin-la-sesión-web)); lleva Share Entreno + deep links `www` + `backgroundColor`; su bundle embebido recibe la key de PostHog y el link `/join` por OTA (`ota/1.1.2-20260820b`). Play: prueba cerrada Alpha = 1.1.2 (85), producción inactiva hasta 12 testers × 14 días (8 aceptados). **Histórico — 4º ciclo de review RESUELTO EL MISMO DÍA (2026-08-15) — 1.1.0 (54) REENVIADA, "Pendiente de revisión"**. El rechazo de la mañana traía UN solo motivo, 5.1.1(iv): el botón "Conectar con Apple Salud" de `/alumno/salud` debía decir "Continuar" (verbo neutro antes de una hoja de permisos); 2.5.4/2.5.1/1.4.1 del 13-08 NO volvieron (superados). Fix solo-copy `2ede7b22` (`salud.tsx` → "Continuar" + defensivos `HabitsCard` y cámara del scanner) — regla permanente documentada. Build 54 por GH Actions (run `31892015639`), smoke QA del owner en iPhone, y en ASC: swap 53→54 + respuesta en Resolution Center + reenvío (verificado "Pendiente de revisión"). NO OTA a producción mientras revisan; descartado además porque `fallbackToCacheTimeout: 0` mostraría el bundle viejo al revisor. Al aprobar → OTAs pendientes `--platform ios`. **ACTUALIZACIÓN 2026-08-19 (saneo documental): la ventana de review ya terminó** — desde el 18-08 los OTA del canal `production` salen `android + ios` sobre el mismo commit (verificado hoy con `eas update:list`), cosa que la regla de la casa solo permite sin build en revisión, y `app.json` va en **1.1.1**. Esta prioridad queda como registro del 4º ciclo; la fecha exacta de aprobación hay que confirmarla en App Store Connect antes de citarla | [Respuesta y checklist](../operations/app-review-1.1.0-respuesta-20260815.md) · [rechazo anterior](../operations/app-review-1.1.0-respuesta-20260813.md)
2. Hydration del dashboard del ALUMNO (EVA-NEXTJS-18): **CAUSA RAÍZ CAZADA Y FIX EN PROD (2026-08-15)** — el `useReducedMotion` de framer 12.38 hace `useState(prefersReducedMotion.current)`: SSR=null y el PRIMER render de cliente lee el media query ⇒ con "Reducir movimiento" activado (iPhone, el perfil exacto de los 41 replays) todo componente que bifurca su árbol con él (p. ej. `ClientGreeting`) hidrata contra HTML distinto en CADA carga. Reproducido determinista en local (harness `dev-harness/reduced-motion` + Playwright `reducedMotion:'reduce'` → "Hydration failed"; sin reduce → limpia). Fix: wrapper hydration-safe `@/lib/use-reduced-motion` (`useSyncExternalStore`, mismo nombre/contrato) + codemod de los 81 archivos + regla `no-restricted-imports` que prohíbe el de framer. Bonus misma familia: `timeAgo` de NewsFeed/ActivityFeed (coach) con `suppressHydrationWarning` (cruce de minuto SSR→hydration). **RESUELTO en Sentry (2026-08-15, pedido del owner): 0 eventos desde el 11-08 — 4+ días de silencio incluido el post-deploy; el issue auto-regresa a unresolved si reaparece. EVA-NEXTJS-V verificado resolved (nada pendiente).**
3. Programa nutrición: **T2.5 F8 EN PRODUCCIÓN** (2026-08-11) — web `31fa0631` + **OTA android `723c92d6`** (runtime 1.1.0). Lleva: H1 (migración `20260811020826` en LIVE; `foods.name_search` pasa a la misma normalización que el query, asimetría 0/4.649), D5 (micro-animación one-shot del swipe, web y RN) y los **3 arreglos del reporte del coach JP** (una plantilla dejó de parecer un plan), verificados en preview con evidencia. **T2.6 cerrada el 2026-08-13 en `rnmobiledenuevo`** (detalle en [TASKS de authoring-speed](../specs/nutrition-authoring-speed/TASKS.md)). **T2.7 re-skin del alumno: F1-F4 en rama y la saga "marcar → tabs muertos" CERRADA con H13 (`c74b176c`, 2026-08-14)** — causa raíz real: tormenta de re-renders de `usePortionMarks` web bajo delta de `useOptimistic` que dejaba las transiciones del App Router (la navegación) sin turno; fix de identidad en 2 líneas + scroll-lock de modales movido a `documentElement` (el de `body` era no-op por `html{overflow-x:clip}` y desanclaba el sidebar sticky). **Validado por el owner en preview `a74ab574`** ("ya por fin funciona"); repro/verificación local sin auth en el harness `apps/web/src/app/dev-harness/nutrition-tabs` + Playwright headless (regla nueva del owner: verificar LOCAL antes de preview). RN inmune verificado (sin `useOptimistic` en `apps/mobile`); paridad T2.7 declarada en MOBILE_PARITY. **T2.6 + T2.7 EN PRODUCCIÓN WEB (2026-08-15)**: master fast-forward `ad08e319..5175de5b` con OK explícito del owner ("Si a todo"); deploy production READY verificado en Vercel. **CERRADO 2026-08-15: QA device RN completo (owner, rondas 1 y 2) sobre los OTAs android `a93d01a3` + `901dc471`**. La ronda 2 (`dd43c45c`) salió del propio QA: media del detalle de Aprender (el catálogo importado guarda el GIF animado en `video_url` — gif_url null en 818/889 filas — y un `<video>` con .gif pinta negro; ahora clasifica con `execMediaKind`), y el gotcha css-interop de `Pressable` con style-función confirmado en prod (mataba el layout de "Fuentes y método" y el share de Records). Falsa alarma verificada en DB: el consumo del día persiste al cambiar de plan por diseño (entries = alumno+fecha). Regla vigente: OTA android-only NO toca la build iOS en revisión; prohibido el OTA sin `--platform` o con `ios`.
4. **Catálogo sólidos-como-líquidos: CORREGIDO EN LIVE (2026-08-19, con OK del owner)**. El hallazgo del 11-08 (250 filas) resultó mayor al medirlo de nuevo: 673 alimentos globales en `ml` con `is_liquid` derivado de la unidad (100%) y `category='bebida'` derivada — pastas, aceitunas, alfajores, pan y brócoli de `open_food_facts` como «bebida». **307 corregidos** (`serving_unit` ml→g, `is_liquid` false, categoría reclasificada por heurística de nombre; cafés/cacaos en polvo pasan a g pero conservan categoría bebida) + **29 items de plan vigentes** (draft/published) ml→g; los 58 de versiones superseded quedan como historia. Backup completo en `_bak_foods_ml_fix_20260819`. Cero macros movidas (g y ml comparten rama del factor en engine web/RN/SQL; items con snapshot propio). `nutrition_intake_entries` intocado (prohibición de la migración `20260728120000`). Quedan 366 en ml (bebidas reales) + ~10 ambiguos deliberados (leches condensadas, sopas/cremas en polvo) para la cola de Curación. Exposición medida antes de ejecutar: 2 coaches / 8 alumnos / 14 planes.
5. Regen completo de `database.types.ts` (deja 13 errores en 7 archivos V1; los workarounds tipados de T2.3 y el cast `V2ReadClient` se retiran ahí).
6. Matriz RLS con JWTs reales + preflight V1→V2 (7 enlaces) — sin cambios desde 08-06.
7. **TTFB del área alumno: la causa MEDIDA era la REGIÓN, no el fan-out SQL** (esta prioridad culpaba al fan-out hasta el saneo documental del 19-08). Medición base (2026-08-15, 7 d de pageloads, p75): `/c/:slug/dashboard` **5.204 ms** (n=160) 🔴, `/c/:slug/login` 3.063 ms 🔴, `/login` 2.652 ms, `/c/:slug/exercises` 1.900, `/c/:slug/workout/:planId` 1.828, `/c/:slug/nutrition` 1.766, `/` 1.696, `/c/:slug/nutrition-v2` 1.566 (borde), `/not-found` 198 ✓ — la plataforma estaba sana. La auditoría del 18-08 cazó la causa con `curl -D -`: el compute corría en **iad1** (Virginia), sin `regions` declarado en ningún lado, mientras Supabase vive en **us-west-2** (Oregón) — ~14 queries bloqueantes en ~8 saltos seriales × ~65 ms de cruce ≈ 0,9 s de pura red, con **ninguna query individual sobre 160 ms** y la base entera (70 MB) en `shared_buffers`. `deb8aee3` (2026-08-18) declaró `regions: ["pdx1"]` en `vercel.json`; baseline registrado el día del cambio: p50 **2.871** / p75 **4.748** ms. 🔴 **Falta re-medir el delta** (p50/p75 de `/c/:coach_slug/dashboard` en Sentry, 24 h antes vs después) — y es solo la MITAD del arreglo: el middleware corre en el POP del visitante, así que cada lectura del proxy de un alumno chileno sigue cruzando Sudamérica→Oregón (~180 ms). Quick wins restantes: **QW1/QW2** (gates del layout + dedupe de `clients`, en curso hoy) y **QW3** (doble render móvil+desktop, **pendiente de decisión del dueño**). Descartada con medición: las 543 políticas RLS permisivas duplicadas valen 30-60 ms por render, no segundos — y consolidarlas toca autorización, así que va al final y nunca con campaña viva. (La corrida de `eas update:insights` que vivía en esta línea se mudó a la fila de Nutrition V2, que es de donde salía el requisito.)

## Reglas de actualización

- `master` integrado no implica production sana: confirmar Vercel y Supabase.
- Un build verde no sustituye QA físico; una migración en el repo no significa que esté aplicada.
- Las acciones operativas/manuales viven en [MANUAL_TASKS.md](../operations/MANUAL_TASKS.md).
- Este archivo guarda solo prioridades y punteros; la evidencia extensa va a specs y auditorías fechadas.
