---
status: active
owner: product-engineering
last_verified: "2026-08-17"
canonical: true
---

# Current status

Vista global mínima: solo prioridades vigentes y punteros. El código, las migraciones aplicadas y
el estado remoto (Vercel/Supabase) prevalecen sobre este resumen. La cronología y la evidencia
viven en `specs/`, `docs/audits/` y el historial de git, no aquí.

## Estado por frente

| Frente | Estado | Fuente de detalle |
|---|---|---|
| Web/PWA | Productivo; confirmar deployment activo antes de incidentes o despliegues | [Runbook](../operations/RUNBOOK.md) |
| App nativa (RN) | OTAs android `a93d01a3` + `901dc471` (2026-08-15, commit `dd43c45c`): T2.6 F6 + T2.7 re-skin + fixes 12-08 + copys App Review, **QA en device del owner COMPLETO (rondas 1 y 2 OK)**. iOS 1.1.0 (54) en App Review (reenviada 15-08 tras el 4º rechazo): no cancelar, no build nueva; al aprobar, replicar los OTA con `--platform ios`. Deuda: paridad RN del tab Alimentos (T2.3 fue web-only), T2.6 F2/F4 RN declarados sin portar | [Mobile parity](MOBILE_PARITY.md) |
| Archivado de alumnos | Migraciones y fixes del P0 de alta en producción (2026-08-03); falta QA físico y matriz Team | [Spec de corte](../../specs/archive-nutrition-v2-cutover/SPEC.md) |
| Nutrition V2 | Canónica para Standalone/Team. En prod web: T2.1+T2.2 overrides, T2.3 hub de alimentos, T2.4 sustituciones FULL y **T2.5 intercambio por grupo (swipe ⇄ + sheet 2 bloques)** — merge `654efd33` (2026-08-10) + OTA android `7a9b3877`; 2 migraciones más en LIVE (`20260810161604`, `20260810171529`); incluye la UI optimista del Hoy web (`5139b29e`) y cierra el reparo offline de T2.4 (chip "En cola"). Decisiones abiertas del QA: H1 (buscador vs paréntesis, fix de catálogo) y D5 (pista del swipe). **T2.6 velocidad de autoría CERRADA en rama `rnmobiledenuevo` (2026-08-13, sin merge a master)**: F1 gramática destructiva unificada (undo de franja, web+RN), F2 copy semana (quick-select "próximos N" + modo Sumar en el día; franja con quick-select por decisión A), F3 tabla `coach_food_last_qty` + 2 RPC en LIVE, F4 porción pegajosa (3 caminos de precedencia verificados en DB), F5 notas visibles editables en el wizard (web+RN), F6 paridad RN parcial declarada en MOBILE_PARITY (F2/F4 RN no cruzan). T2.7 re-skin cerrada (2026-08-15); **T3.x editor único: T3.1→W4 COMPLETAS el 2026-08-15 en rama `rnmobiledenuevo`** ([programa](../specs/nutrition-unified-editor/SPEC.md)) — editor en `/coach/nutrition-v2/[clientId]/editor` edita el plan vigente y crea (`?from=template:|plan:` o blank) con metadatos, sustituciones editables, override, duplicar/copiar día, porción pegajosa, cápsula de día activo, totales fijos y paleta desktop; QA visual del owner en preview OK y **corte W4 hecho** (CTAs web al editor; quick-edit clásico y wizard como secundarios en el menú "..." durante la ventana de retiro de 2 semanas). **T3.2b modo plantilla CODIGO LISTO en la rama (2026-08-15, sin corte)**: ruta `/coach/nutrition-v2/plantillas/editor` (`?template=` editar / sin id crear) sobre el mismo lienzo — guardado sin CAS (paridad wizard), sin vigencia, descripción editable, banner permanente, degradación avisada; harness `?mode=template` headless verde con regresiones create/edit. El **corte 2** (CTAs de `PlanTemplatesLibrary` + redirect legacy → editor) **espera QA del owner en preview** (mismo protocolo que W4). **R1 extracción de reducers CERRADA (2026-08-16)**: la gramática del editor (ex `quick-edit-state.ts`, 2.400 LOC) vive en `packages/nutrition-v2/editor-state.ts` (+ `editor-food`/`editor-portions`, re-exportadas por el wizard desde sus rutas históricas); web consume el paquete (16 importadores), suite completa del reducer corre contra el paquete, `tsc` mobile y `expo export` verdes. **T3.3a CERRADA EN CODIGO (2026-08-16, SIN QA device)**: el quick-edit del coach RN consume la MISMA gramática — murieron el reducer RN y el paralelo de porciones; publish por proyección compartida; convergencias declaradas en MOBILE_PARITY (lápiz T2.2 solo con food hidratado, diff id-based, respaldos v1 descartados). **QA del owner CERRADO el 2026-08-16 (preview de plantillas + device Android del quick-edit convergido: ambos OK)** ⇒ **corte 2 HECHO**: las CTAs de `PlanTemplatesLibrary` y el redirect legacy `nutrition-plans/new` apuntan al editor de plantillas, y el wizard de plantillas queda solo por URL directa. **T3.3b CERRADA EN CODIGO (2026-08-16, SIN QA device)**: el editor unico existe en RN (`/coach/nutrition-v2/editor/[clientId]` + `plantillas/editor`) con metadatos, creacion `?from=`, reemplazos editables, duplicar/copiar dia, reorden, capsula de dia activo, totales fijos, **porcion pegajosa (cierra la deuda T2.6 F4 RN)** y plantillas por un camino de escritura movil nuevo (`saveTemplate`); el **corte RN** ya apunta ficha, hub, roster y picker de plantillas al editor, con el par viejo como camino secundario. **T3.v Cabina (pasada visual del editor único) CERRADA EN CÓDIGO (2026-08-16, SIN QA del owner)** ([spec](../specs/nutrition-editor-cabina/SPEC.md)): MacroSpark + popover de gramos (contrato D3) en todas las superficies del editor web y RN, fotos reales de producto con fallback, cinta ≥768 con anillo semántico y «Metas del día ▾» (hospeda `TargetsEditorCard`), rail de días ≥1024 con estados verde/ámbar y B4 como enlace, responsive 390–1536 verificado con Playwright headless (41 asserts × 5 anchos × 2 temas), `macroCalorieShares` único en `@eva/nutrition-v2`; builder y área alumno diff CERO; gates completos verdes (typecheck web+mobile, vitest, tokens, boundaries, `expo export android`, build, docs). **OTA android ACUMULADO PUBLICADO (2026-08-17, decisión owner de adelantarlo al QA): grupo `aca6fc76` / android `01a00d3d`, runtime 1.1.0, canal production, vía GH Actions run 31983615828 desde `73b227ba`**. **Paquete post-QA CERRADO EN CÓDIGO (2026-08-17)**: fixes del QA del owner (cinta 768 sin solapes + gate anti-solape, hub RN = tablist de la web con Porciones rescatada y su fetch reparado — z.guid() por UUIDs de seed no-RFC —, centrado 2xl 49px→0, cantidad/unidad estable web+RN, chevrons animados) + **«Familia N»** (AddActionButton con iconos clay del owner en TODAS las altas web+RN, contraste white-label dinámico verificado en DOM) + **Guía Viva SPEC/PLAN/TASKS** ([spec](../specs/nutrition-onboarding-tour/SPEC.md)); **OTA android #2 acumulado PUBLICADO** (grupo `7738d234` / android `01a00e11`). **Guía Viva IMPLEMENTADA (2026-08-17, workflow 5 workers + juicio del jefe)**: motor spotlight propio en web (`components/nutrition-v2/tour/` — posicionador puro clamp/flip/dock, flags `eva.tour.<id>.v1.<coach>` SSR-safe fail-closed) y RN (`Modal` translúcido + registro de targets por `measureInWindow`, AsyncStorage con la MISMA clave — test de contrato cruzado web↔RN), montado en el editor (guion 8 pasos ≥768 / 6 compacto) y en el hub (6 pasos, copys literales del artifact aprobado, incl. Curación); «?» de 30 px con D2 garantizado por layout (doble instancia CSS por el `backdrop-filter` de la PublishBar en web; flotante sobre la barra en RN); gates geométricos BLOQUEANTES en `cabina-visual-check.mjs` (273 asserts: D2 cero solapes, D3 tarjeta ⊆ viewport en cada paso × 5 anchos × ambos tours, D4 flag/reload/reiniciar — probados en rojo); fix del juicio: `findTarget` ya no devuelve targets CSS-ocultos (pinhole fantasma 768-1023). Suite 5776 verde (de paso: test stale del tablist RN actualizado al contrato «web es JEFE» y test de register reparado tras el commit Meta Pixel `7df9aa6c`). Quedan: **Sello v2 (S1-S4)** → gates completos → **OTA android #3 acumulado** → QA owner en UN pase (funde T3.3b + Cabina + Familia N + Guía Viva + Sello) → `eas update:insights` → retiro del par viejo agendado 2026-08-30. iOS intacto mientras dure el App Review; preflight mantiene 7 enlaces V1→V2 por reconciliar | [Programa](../specs/nutrition-flows-redesign/TASKS.md) · [Runbook de corte](../operations/NUTRITION_V2_CUTOVER_RUNBOOK.md) |
| V1 nutrición | Congelada, **no se borra** (decisión owner 2026-08-03): solo migrar usuarios a V2 | [Delta del mapa](../audits/v1-deprecation-map-delta-2026-08-03.md) |
| Teams | Pool, membresías y workspaces implementados | [Flows](../architecture/FLOWS_AND_COMPONENTS.md#team) |
| Enterprise | **Congelado (cuarentena 2026-08-06)**: sin desarrollo activo ni gates locales; `apps/enterprise` permanece en el repo hasta un plan de retiro propio. El e2e de aislamiento RLS sigue en CI manual | [Flows](../architecture/FLOWS_AND_COMPONENTS.md#enterprise) |

## Prioridades vigentes

0. **iOS: 4º ciclo de review RESUELTO EL MISMO DÍA (2026-08-15) — 1.1.0 (54) REENVIADA, "Pendiente de revisión"**. El rechazo de la mañana traía UN solo motivo, 5.1.1(iv): el botón "Conectar con Apple Salud" de `/alumno/salud` debía decir "Continuar" (verbo neutro antes de una hoja de permisos); 2.5.4/2.5.1/1.4.1 del 13-08 NO volvieron (superados). Fix solo-copy `2ede7b22` (`salud.tsx` → "Continuar" + defensivos `HabitsCard` y cámara del scanner) — regla permanente documentada. Build 54 por GH Actions (run `31892015639`), smoke QA del owner en iPhone, y en ASC: swap 53→54 + respuesta en Resolution Center + reenvío (verificado "Pendiente de revisión"). NO OTA a producción mientras revisan; descartado además porque `fallbackToCacheTimeout: 0` mostraría el bundle viejo al revisor. Al aprobar → OTAs pendientes `--platform ios` | [Respuesta y checklist](../operations/app-review-1.1.0-respuesta-20260815.md) · [rechazo anterior](../operations/app-review-1.1.0-respuesta-20260813.md)
1. Hydration del dashboard del ALUMNO (EVA-NEXTJS-18): **CAUSA RAÍZ CAZADA Y FIX EN PROD (2026-08-15)** — el `useReducedMotion` de framer 12.38 hace `useState(prefersReducedMotion.current)`: SSR=null y el PRIMER render de cliente lee el media query ⇒ con "Reducir movimiento" activado (iPhone, el perfil exacto de los 41 replays) todo componente que bifurca su árbol con él (p. ej. `ClientGreeting`) hidrata contra HTML distinto en CADA carga. Reproducido determinista en local (harness `dev-harness/reduced-motion` + Playwright `reducedMotion:'reduce'` → "Hydration failed"; sin reduce → limpia). Fix: wrapper hydration-safe `@/lib/use-reduced-motion` (`useSyncExternalStore`, mismo nombre/contrato) + codemod de los 81 archivos + regla `no-restricted-imports` que prohíbe el de framer. Bonus misma familia: `timeAgo` de NewsFeed/ActivityFeed (coach) con `suppressHydrationWarning` (cruce de minuto SSR→hydration). **RESUELTO en Sentry (2026-08-15, pedido del owner): 0 eventos desde el 11-08 — 4+ días de silencio incluido el post-deploy; el issue auto-regresa a unresolved si reaparece. EVA-NEXTJS-V verificado resolved (nada pendiente).**
2. Programa nutrición: **T2.5 F8 EN PRODUCCIÓN** (2026-08-11) — web `31fa0631` + **OTA android `723c92d6`** (runtime 1.1.0). Lleva: H1 (migración `20260811020826` en LIVE; `foods.name_search` pasa a la misma normalización que el query, asimetría 0/4.649), D5 (micro-animación one-shot del swipe, web y RN) y los **3 arreglos del reporte del coach JP** (una plantilla dejó de parecer un plan), verificados en preview con evidencia. **T2.6 cerrada el 2026-08-13 en `rnmobiledenuevo`** (detalle en [TASKS de authoring-speed](../specs/nutrition-authoring-speed/TASKS.md)). **T2.7 re-skin del alumno: F1-F4 en rama y la saga "marcar → tabs muertos" CERRADA con H13 (`c74b176c`, 2026-08-14)** — causa raíz real: tormenta de re-renders de `usePortionMarks` web bajo delta de `useOptimistic` que dejaba las transiciones del App Router (la navegación) sin turno; fix de identidad en 2 líneas + scroll-lock de modales movido a `documentElement` (el de `body` era no-op por `html{overflow-x:clip}` y desanclaba el sidebar sticky). **Validado por el owner en preview `a74ab574`** ("ya por fin funciona"); repro/verificación local sin auth en el harness `apps/web/src/app/dev-harness/nutrition-tabs` + Playwright headless (regla nueva del owner: verificar LOCAL antes de preview). RN inmune verificado (sin `useOptimistic` en `apps/mobile`); paridad T2.7 declarada en MOBILE_PARITY. **T2.6 + T2.7 EN PRODUCCIÓN WEB (2026-08-15)**: master fast-forward `ad08e319..5175de5b` con OK explícito del owner ("Si a todo"); deploy production READY verificado en Vercel. **CERRADO 2026-08-15: QA device RN completo (owner, rondas 1 y 2) sobre los OTAs android `a93d01a3` + `901dc471`**. La ronda 2 (`dd43c45c`) salió del propio QA: media del detalle de Aprender (el catálogo importado guarda el GIF animado en `video_url` — gif_url null en 818/889 filas — y un `<video>` con .gif pinta negro; ahora clasifica con `execMediaKind`), y el gotcha css-interop de `Pressable` con style-función confirmado en prod (mataba el layout de "Fuentes y método" y el share de Records). Falsa alarma verificada en DB: el consumo del día persiste al cambiar de plan por diseño (entries = alumno+fecha). Regla vigente: OTA android-only NO toca la build iOS en revisión; prohibido el OTA sin `--platform` o con `ios`.
3. **Catálogo: 250 alimentos sólidos marcados como líquidos** (hallazgo 2026-08-11, reporte de un coach). `is_liquid = true` + `category = 'bebida'` en cereales, granolas y harinas ⇒ el registro libre del alumno ofrece **ml/unidad y nunca gramos**. Medido en LIVE: 677 filas con `is_liquid`, 302 con ≥200 kcal/100, de las cuales **250 no son líquidos calóricos legítimos** (212 con ≥300 kcal); 7 ya están en planes publicados y 8 tienen registros. Arreglado a mano solo el caso reportado (`Avena · Quajer`, `8a8102e6…` → sólido/g/carbohidrato). **La corrección masiva es decisión del owner**: es un cambio de datos en LIVE que altera las unidades ofrecidas sobre alimentos ya prescritos.
4. Regen completo de `database.types.ts` (deja 13 errores en 7 archivos V1; los workarounds tipados de T2.3 y el cast `V2ReadClient` se retiran ahí).
5. Matriz RLS con JWTs reales + preflight V1→V2 (7 enlaces) — sin cambios desde 08-06.
6. **TTFB p75 MEDIDO (2026-08-15, 7d de pageloads): el objetivo <1,5 s FALLA en casi todas las rutas** — `/c/:slug/dashboard` **5.204 ms** (n=160) 🔴, `/c/:slug/login` 3.063 ms 🔴, `/login` 2.652 ms, `/c/:slug/exercises` 1.900, `/c/:slug/workout/:planId` 1.828, `/c/:slug/nutrition` 1.766, `/` 1.696, `/c/:slug/nutrition-v2` 1.566 (borde); `/not-found` 198 ✓. El dashboard del alumno es EL cuello (fan-out de queries del server render); optimizarlo es un proyecto propio — priorizar contra T3.x. ✅ `eas update:insights` corrido (2026-08-15): los 5 grupos recientes de production con **0 crashes / 0 failed installs** (adopción 1-4 usuarios únicos/7d).

## Reglas de actualización

- `master` integrado no implica production sana: confirmar Vercel y Supabase.
- Un build verde no sustituye QA físico; una migración en el repo no significa que esté aplicada.
- Las acciones operativas/manuales viven en [MANUAL_TASKS.md](../operations/MANUAL_TASKS.md).
- Este archivo guarda solo prioridades y punteros; la evidencia extensa va a specs y auditorías fechadas.
