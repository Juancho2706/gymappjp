---
status: reference
owner: product-engineering
last_verified: "2026-09-02"
canonical: false
---

# Historial de `CURRENT.md` — corte del 2026-09-02

El 2026-09-02 `docs/status/CURRENT.md` pesaba 50 KB y se había vuelto una crónica. Se redujo a la
vista mínima que declara ser (≤ 4 líneas por prioridad) y la prosa se movió: la de cada frente con
spec, a la sección «Cierre» de su `TASKS.md`; la del resto, a este archivo.

**No es fuente de estado ni de instrucciones.** Se conserva para no perder hashes, deploys, OTAs y
decisiones citadas en su momento. El estado vigente vive en [CURRENT.md](../status/CURRENT.md) y en
las specs.

## Frentes sin spec propia y prioridades retiradas

### Frente Web/PWA — QA pre-campaña Meta y pricing v3 (2026-08-18/21)

**Pricing v3 (2026-08-21, [spec](../specs/pricing-v3/SPEC.md)): Free = 1 alumno + white-label completo + sello «Hecho con EVA»; Pro 25 sin sello; backfill por uso de `coaches.max_clients` (Free con ≤1 alumno → 1; los 5 con ≥2 conservan su fila)**. Productivo; confirmar deployment activo antes de incidentes o despliegues. **QA pre-campaña Meta EN PRODUCCIÓN (2026-08-18, master = `a5ecbf3d`)**: landing sin testimonios ficticios (3 tarjetas de hechos verificables) ni promesas falsas («sin tiendas» con la app iOS publicada, «sin tracking de terceros» con el pixel activo); cupo Free derivado del catálogo en 10 superficies web+RN (el «3» del correo de beta expirada es deliberado: grandfather); alta por Google gana conversión `CompleteRegistration` (CAPI + espejo del pixel con el mismo `eid`) y la bienvenida+drip que solo emitía el camino por email; `_fbc` (click-id del anuncio) se captura antes del consentimiento; la X del banner de cookies ya no persiste un rechazo permanente y el banner no tapa el CTA sticky móvil; botón de reenvío de confirmación por fin cableado (helper huérfano desde el incidente del 16-08), con la identidad resuelta por sesión o `uid` — nunca por el email tipeado. **El Meta Pixel estuvo MUDO días por nuestra propia CSP** (`connect.facebook.net` faltaba en `script-src`); resuelto en `4115d951` y verificado end-to-end desde navegador limpio. Correo de lanzamiento iOS enviado a los 25 coaches activos (25/25 delivered)

### Frente App nativa (RN) — crónica 2026-08-18 → 2026-08-29

**29-08: guard de acceso RN contaba al alumno DEMO** (`lib/coach-access.ts` sin `is_demo = false`): 6 coaches free v3 con demo + 1 alumno real caían en `/coach/reactivate` y «Continuar gratis» rebotaba con 403 (status ya `active`) — deadlock solo en la app, la web nunca gateó mal. Fix = mismo predicado que `capacity.service.ts` + test; **pendiente OTA 1.1.2 android/ios**. Detalle en [MOBILE_PARITY](../status/MOBILE_PARITY.md). · **28-08: «+ Nueva» pregunta qué crear (web + RN, spec [library-new-choice](../specs/library-new-choice/SPEC.md), mockup `9d979bfa` aprobado) + filas legibles de la hoja «Entrenamiento incompleto» del alumno RN + 25 tests de `register`/`onboarding complete` VERDES otra vez (mock de `cookies()` tras `1757195f`; +2 tests del fallback cookie `eva_utm`). Salida: deploy web + OTA android/ios a 1.1.2 (grupos en [MOBILE_RELEASES_OTA](../operations/MOBILE_RELEASES_OTA.md)); QA device/navegador del owner pendiente.** **21-08: `/join` standalone pasa a solicitud al coach** (spec [coach-leads](../specs/coach-leads/SPEC.md)). **2026-08-20: tres runtimes conviven en el canal `production`** — 1.1.0 (público: App Store 54 + Play closed 81), 1.1.1 (57/84, en revisión de Apple) y 1.1.2 (58/85, TestFlight + Play closed, lleva Share Entreno de la rama `rnmobiledenuevo`, sin merge). Los OTA del 19-08 desde `2fdecebd` salieron a runtime **1.1.1** (no a 1.1.0): hoy se publicaron a 1.1.0 (`ota/1.1.0-20260820` = `39e64ff8`: días anteriores + share iOS + cupos Free) y 1.1.1 (`ota/1.1.1-20260820` = `b4d958fa`: share iOS), android e ios, todos verdes — procedimiento en [MOBILE_RELEASES_OTA](../operations/MOBILE_RELEASES_OTA.md#partición-de-runtimes-vigente-2026-08-20). Web `master` recibió el hotfix del alta (`0be1130e`: bienvenida+drip con `await`, `register_failed` instrumentado) y la key de PostHog para builds/OTAs (`a2cca2a5`). **Histórico 18-08 — OTAs desde `4de66a5c` (android + ios, canal production, runtime 1.1.0)**: tanda del QA en device del dueño — equivalencia del reemplazo visible para el coach (solo lectura; la edición de cantidad se DESCARTÓ), teclado que tapaba «Notas para tu alumno», sheets `nativeModal` al borde en Android (`navigationBarTranslucent`, toca ~68 sheets ⇒ pide pasada visual), «Aplicar» plantilla desde la pestaña Plantillas (paridad web, 2 toques) y foco del tour que caía en la barra de estado. **SIN QA en device todavía.** Antes, `ebfbec11` cerró un BLOQUEO DURO: guardar una plantilla desde la app fallaba SIEMPRE («La plantilla tiene datos inválidos») porque Hermes no define `globalThis.crypto` y el fallback de ids emitía `synthetic-…`, que el contrato rechaza con `z.string().uuid()`; CI no lo cazaba porque en Node sí existe. Deuda viva del tour: la tarjeta queda bajo la tab bar y el arreglo real es subir el punto de montaje del overlay al layout raíz (tanda propia). 🔴 `eas update --platform all` FALLA (exporta web sin `react-native-web`): siempre android e ios por separado

### Frente App nativa (RN) — histórico 2026-08-15

OTAs android `a93d01a3` + `901dc471` (2026-08-15, commit `dd43c45c`): T2.6 F6 + T2.7 re-skin + fixes 12-08 + copys App Review, **QA en device del owner COMPLETO (rondas 1 y 2 OK)**. iOS 1.1.0 (54) en App Review (reenviada 15-08 tras el 4º rechazo): no cancelar, no build nueva; al aprobar, replicar los OTA con `--platform ios`. **Las tres deudas RN que esta fila declaraba están cerradas** (corregido en el saneo documental del 19-08, verificado contra código): la paridad del tab Alimentos RN cerró el 11-08 (`6c4da722` los 5 modos, `949a1eab` crear, `a0b976e7` clasificar), T2.6 F4 porción pegajosa la cerró T3.3b W3 (`a95f1811`) y el nivel día de T2.6 F2 cruzó con la gramática compartida (RN despacha `COPY_VARIANT_TO_DAYS` con Reemplazar/Sumar). Lo único que queda sin puerta es el quick-select «próximos 1/2/4» del menú de la **franja**, hoy inalcanzable también en web: colgaba del wizard retirado en `0355d67d`

### Frente Archivado de alumnos

Migraciones y fixes del P0 de alta en producción (2026-08-03); falta QA físico y matriz Team

### Frente V1 nutrición

Congelada, **no se borra** (decisión owner 2026-08-03): solo migrar usuarios a V2

### Frente Teams

Pool, membresías y workspaces implementados

### Frente Enterprise

**ELIMINADO de EVA (decisión del owner 2026-09-01)**: no tocar, no extender, no diseñar ni testear ese caso; quedan solo Teams y Standalone. `apps/enterprise` sigue en el repo (cuarentena desde 2026-08-06) hasta la ola de demolición (`apps/enterprise`, rutas `/org/*`, `enterprise_coach` en coach-nav/workspace, RLS/policies de org, cron/e2e) — backlog B15 de la [Ola de orden](../specs/ola-de-orden/TASKS.md)

### Tren «billing + seguridad» — en producción 2026-09-02

**Tren «billing + seguridad» — EN PRODUCCIÓN 02-09 01:5xZ** (`master` = `rnmobiledenuevo` = `16c06fba`, deploy `dpl_8AJgWw36sPjyhoNT3pJBuebBgDbx` READY, OTA android `42f021f4` / ios `4052b874`): (1) `8da174e4` el barrido «abandoned signup coupons» de `mp-reconcile` ya no revierte cupones de coaches Flow con tarjeta enrolada ni de quien ya pagó (`sweepAbandonedSignupCoupons`, 7 tests; incidente BILL-01); (2) SEC-01 fases 0-2: `anon` pierde los grants de tabla sobre `client_payments` y TRIGGER/TRUNCATE/REFERENCES sobre `coaches` (LIVE `20260902012559`), y el branding público (proxy `/c/**`, login del alumno, manifest/og/splash/pwa-screenshot, pantalla de código RN) sale del RPC `get_coach_public_branding` (LIVE `20260902014246`) — la revocación de `invite_code` a `anon` (fase 3) espera la OTA adoptada ([MANUAL_TASKS § SEC-01](../operations/MANUAL_TASKS.md)); (3) el dispatch manual de CI corre solo `prod-suave` (faltan 2 secrets, QA-01); (4) `coach_kpi_snapshots` se poda a 90 d (calendario Santiago). Diagnósticos del mismo tren, sin código: TTFB del alumno NO es regresión de servidor (p75 7 d 7,5 s vs 30 d 8,4 s; 47 % del pageload es post-LCP en celular; la mejora real es QW3) y `DEP0169` es `web-push@3.6.7` sin release (se deja). Suite completa 8401 verdes pre-push. **QA del owner pendiente en device**: código de coach → marca → login.

### Tren «cierre de backlog 02-09» — en producción 2026-09-02

**Tren «cierre de backlog 02-09» — EN PRODUCCIÓN 02-09 19:55Z** (`master` = `rnmobiledenuevo` = `794aee52`, deploy `dpl_E6Rt7ETYsZ5tpbW84DLLdCP4RMHk` READY, OTA 1.1.2 `production` android `01a063b0-6a6a-7d09-ad3c-bbdd04591b0d` (grupo `ec7da7fb`, run 33675885999) / ios `01a063b0-86f0-7c30-8079-9e61d9b6b356` (grupo `6db6747f`, run 33675889223); artifact del tablero `b506f555`): todo el backlog acumulado verificado contra código/DB por 6 agentes y ejecutado en 13 commits por 11 workers — check-in del alumno sobrevive al corte de red (B2), aviso de correo en la pantalla de persona (B9), hora/día en Santiago en notas y mediciones (O7.6), purga real a 30 días de cuentas dadas de baja + retención de solicitudes 90 d (`purge-data`), ejercicios propios E1/E2/E3/E5/E6 web y E1 RN, swipe-back del builder bloqueado en nativo (D6), velo de Share fuera (D2), 6 CTAs RN sin «no incluido en tu plan» (OB3), reemplazos estructurados en el plan del alumno + stepper de confirmación (SUB-T10/SUBI-F5), **solicitudes de alumnos en la app** (API móvil + pantalla + push; decisión del owner: RN ≥ PWA), link de invitación a `/join`, gates de dominio en movimiento/builder/ficha (OB9), instrumentación de la respuesta del servidor para cazar el E394, lint/tokens/logout RN (OB6/7/8/10, Q-B1), Dependabot #103, `nutrition-smoke` fuera del dispatch, 12 specs cerradas o actualizadas y `docs/testing/QA_DEVICE_PENDIENTE.md` (18 ítems). **E2E `prod-suave` verde por primera vez** (run 33673130561) con el coach QA propio `qa-e2e-coach@evatest.cl` (secrets `E2E_QA_COACH_*`; la cuenta de App Review no se usa para tests). Gates reales pre-push: vitest 8888/8888, tsc web y mobile, eslint 0 errores, tokens, boundaries, docs. Queda: **QA del owner en device** (lista en `QA_DEVICE_PENDIENTE.md` + lo nuevo de este tren), ola 2 chica (Duplicar sin sufijo «(copia)» choca por nombre; borrar cuenta desde la web falla con foods/planes/comidas por FK; 4 sitios de «Nutrición Pro» en la estrategia híbrida RN + string del API; hexes success-600 en `CoachDashboardSections`), y la tanda de higiene (tests con jsdom solo donde hace falta, `test:changed`, shards en CI, memoria ≤ 45 líneas, CURRENT con tope).

### PLAN «Cobros coach → alumno» (2026-08-29, draft)

**PLAN «Cobros coach → alumno» (2026-08-29, [spec](../specs/cobros-coach-alumno/SPEC.md), status draft, SDD versionada el 01-09 en `edf6a07c`, SIN implementar):** motor de cobro del coach a su alumno (tres modos: transferencia · link MP por ciclo · suscripción MP), aparte del billing EVA↔coach (segunda app MP «EVA Cobros», webhook `/api/cobros/mp/webhook`, 16 migraciones, estado derivado de `paid_through + gracia 0/3`), solo Pro, beta cerrada. Artifact «El motor de cobros» `046f3bb1`. **Bloqueado por 8 decisiones del owner (SPEC §18.1) y 3 verificaciones externas (§18.2: contador SII, abogado retracto, smoke MP con plata real).** Estimación 26-32 días-agente + 2-3 semanas de beta.

### iOS/Android — releases y ciclos de App Review (2026-08-15 → 2026-08-28)

**iOS al 2026-08-28: 1.1.2 APROBADA y en la tienda (`READY_FOR_SALE`, leído por la API de ASC en el run 33212983838; la build en revisión era la 59 del tren del 26-08) ⇒ el piso OTA sube a 1.1.2 y salió el OTA iOS 1.1.2 desde `master` `1e25229c` (keypad; grupo `d402f897`). 1.1.3 NO tiene motivo todavía: cero cambio nativo desde la build 59 (`package.json`/lockfile/plugins intactos; lo único de binario pendiente son los intentFilters Android estrechados de `34d1187a`) — se construye cuando haya cambio nativo real, no antes. Android sigue en 1.1.2 (build 86 del 21-08, closed testing Alpha; NO existe binario 1.1.3). Población 7 d (`eas channel:insights`): 1.1.1 = 13 embebido + 49 OTA únicos · 1.1.2 = 20 + 39 · 1.1.0 = 2 + 4 ⇒ 1.1.1 deja de recibir ports salvo excepción.** Histórico 21-08: 1.1.1 (57) APROBADA (`READY_FOR_SALE`); 1.1.2 (58) enviada a revisión (`WAITING_FOR_REVIEW`, 02:23 UTC) por la API de ASC ([procedimiento](../operations/MOBILE_RELEASES_OTA.md#enviar-a-revisión-de-apple-sin-la-sesión-web)); lleva Share Entreno + deep links `www` + `backgroundColor`; su bundle embebido recibe la key de PostHog y el link `/join` por OTA (`ota/1.1.2-20260820b`). Play: prueba cerrada Alpha = 1.1.2 (85), producción inactiva hasta 12 testers × 14 días (8 aceptados). **Histórico — 4º ciclo de review RESUELTO EL MISMO DÍA (2026-08-15) — 1.1.0 (54) REENVIADA, "Pendiente de revisión"**. El rechazo de la mañana traía UN solo motivo, 5.1.1(iv): el botón "Conectar con Apple Salud" de `/alumno/salud` debía decir "Continuar" (verbo neutro antes de una hoja de permisos); 2.5.4/2.5.1/1.4.1 del 13-08 NO volvieron (superados). Fix solo-copy `2ede7b22` (`salud.tsx` → "Continuar" + defensivos `HabitsCard` y cámara del scanner) — regla permanente documentada. Build 54 por GH Actions (run `31892015639`), smoke QA del owner en iPhone, y en ASC: swap 53→54 + respuesta en Resolution Center + reenvío (verificado "Pendiente de revisión"). NO OTA a producción mientras revisan; descartado además porque `fallbackToCacheTimeout: 0` mostraría el bundle viejo al revisor. Al aprobar → OTAs pendientes `--platform ios`. **ACTUALIZACIÓN 2026-08-19 (saneo documental): la ventana de review ya terminó** — desde el 18-08 los OTA del canal `production` salen `android + ios` sobre el mismo commit (verificado hoy con `eas update:list`), cosa que la regla de la casa solo permite sin build en revisión, y `app.json` va en **1.1.1**. Esta prioridad queda como registro del 4º ciclo; la fecha exacta de aprobación hay que confirmarla en App Store Connect antes de citarla | [Respuesta y checklist](../operations/app-review-1.1.0-respuesta-20260815.md) · [rechazo anterior](../operations/app-review-1.1.0-respuesta-20260813.md)

### Hydration del dashboard del alumno (EVA-NEXTJS-18) — resuelto 2026-08-15

Hydration del dashboard del ALUMNO (EVA-NEXTJS-18): **CAUSA RAÍZ CAZADA Y FIX EN PROD (2026-08-15)** — el `useReducedMotion` de framer 12.38 hace `useState(prefersReducedMotion.current)`: SSR=null y el PRIMER render de cliente lee el media query ⇒ con "Reducir movimiento" activado (iPhone, el perfil exacto de los 41 replays) todo componente que bifurca su árbol con él (p. ej. `ClientGreeting`) hidrata contra HTML distinto en CADA carga. Reproducido determinista en local (harness `dev-harness/reduced-motion` + Playwright `reducedMotion:'reduce'` → "Hydration failed"; sin reduce → limpia). Fix: wrapper hydration-safe `@/lib/use-reduced-motion` (`useSyncExternalStore`, mismo nombre/contrato) + codemod de los 81 archivos + regla `no-restricted-imports` que prohíbe el de framer. Bonus misma familia: `timeAgo` de NewsFeed/ActivityFeed (coach) con `suppressHydrationWarning` (cruce de minuto SSR→hydration). **RESUELTO en Sentry (2026-08-15, pedido del owner): 0 eventos desde el 11-08 — 4+ días de silencio incluido el post-deploy; el issue auto-regresa a unresolved si reaparece. EVA-NEXTJS-V verificado resolved (nada pendiente).**

### Catálogo sólidos-como-líquidos — corregido en LIVE 2026-08-19

**Catálogo sólidos-como-líquidos: CORREGIDO EN LIVE (2026-08-19, con OK del owner)**. El hallazgo del 11-08 (250 filas) resultó mayor al medirlo de nuevo: 673 alimentos globales en `ml` con `is_liquid` derivado de la unidad (100%) y `category='bebida'` derivada — pastas, aceitunas, alfajores, pan y brócoli de `open_food_facts` como «bebida». **307 corregidos** (`serving_unit` ml→g, `is_liquid` false, categoría reclasificada por heurística de nombre; cafés/cacaos en polvo pasan a g pero conservan categoría bebida) + **29 items de plan vigentes** (draft/published) ml→g; los 58 de versiones superseded quedan como historia. Backup completo en `_bak_foods_ml_fix_20260819`. Cero macros movidas (g y ml comparten rama del factor en engine web/RN/SQL; items con snapshot propio). `nutrition_intake_entries` intocado (prohibición de la migración `20260728120000`). Quedan 366 en ml (bebidas reales) + ~10 ambiguos deliberados (leches condensadas, sopas/cremas en polvo) para la cola de Curación. Exposición medida antes de ejecutar: 2 coaches / 8 alumnos / 14 planes.

### TTFB del área alumno — la causa medida era la región

**TTFB del área alumno: la causa MEDIDA era la REGIÓN, no el fan-out SQL** (esta prioridad culpaba al fan-out hasta el saneo documental del 19-08). Medición base (2026-08-15, 7 d de pageloads, p75): `/c/:slug/dashboard` **5.204 ms** (n=160) 🔴, `/c/:slug/login` 3.063 ms 🔴, `/login` 2.652 ms, `/c/:slug/exercises` 1.900, `/c/:slug/workout/:planId` 1.828, `/c/:slug/nutrition` 1.766, `/` 1.696, `/c/:slug/nutrition-v2` 1.566 (borde), `/not-found` 198 ✓ — la plataforma estaba sana. La auditoría del 18-08 cazó la causa con `curl -D -`: el compute corría en **iad1** (Virginia), sin `regions` declarado en ningún lado, mientras Supabase vive en **us-west-2** (Oregón) — ~14 queries bloqueantes en ~8 saltos seriales × ~65 ms de cruce ≈ 0,9 s de pura red, con **ninguna query individual sobre 160 ms** y la base entera (70 MB) en `shared_buffers`. `deb8aee3` (2026-08-18) declaró `regions: ["pdx1"]` en `vercel.json`; baseline registrado el día del cambio: p50 **2.871** / p75 **4.748** ms. 🔴 **Falta re-medir el delta** (p50/p75 de `/c/:coach_slug/dashboard` en Sentry, 24 h antes vs después) — y es solo la MITAD del arreglo: el middleware corre en el POP del visitante, así que cada lectura del proxy de un alumno chileno sigue cruzando Sudamérica→Oregón (~180 ms). Quick wins restantes: **QW1/QW2** (gates del layout + dedupe de `clients`, en curso hoy) y **QW3** (doble render móvil+desktop, **pendiente de decisión del dueño**). Descartada con medición: las 543 políticas RLS permisivas duplicadas valen 30-60 ms por render, no segundos — y consolidarlas toca autorización, así que va al final y nunca con campaña viva. (La corrida de `eas update:insights` que vivía en esta línea se mudó a la fila de Nutrition V2, que es de donde salía el requisito.)
