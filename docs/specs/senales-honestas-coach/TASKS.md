---
status: active
owner: product-engineering
last_verified: "2026-09-10"
canonical: false
---

# TASKS — Señales honestas para el coach

Ver [SPEC](SPEC.md) · [PLAN](PLAN.md) · [TESTING-QA](TESTING-QA.md). **Ningún checkbox se marca sin gate real o QA del owner.** Push, deploy y OTA solo a pedido del owner.

Todas las rutas:línea de abajo están verificadas contra `D:/Proyectos/Antigravity/gymappjp`, rama `rnmobiledenuevo`, HEAD `f93378c3`. Los nombres canónicos (copys, `nutritionEnabled`, `agendaTotal`, `ENTERED_CHIP_WINDOW_DAYS`, `buildAgendaLabel`) son los del PLAN; ningún fixer los renombra.

> **Las líneas apuntan a HEAD `f93378c3` y pueden correrse ±3; el implementador ancla por identificador (nombre de función, constante o prop), nunca por número de línea.**

## W0 · Decisiones del owner (2026-09-10)

No son tareas: son el marco que ya decidió el owner. **Por eso van sin checkbox** — la regla de arriba reserva `- [ ]` para lo que se ejecuta y se cierra con gate real o QA.

| # | Decisión (literal del owner) | Fuente |
|---|---|---|
| W0.1 | **D1** (Q1 = A) Agenda honesta: sección `Pendientes de hoy`, sin hora, sin «hechas», **contador = pendientes (filas)**, subtítulo con la fecha real. Web y RN. El carril C arranca con **filas** (R6); «alumnos» queda como punto vetable en el mensaje final (`DECISIONS.md § Abierto`). | `DECISIONS.md:5` |
| W0.2 | **D2** (Q2 = A) Chip «Entró hace X d» solo ≤ 7 días desde `first_login_at`; después `Activo`. Sin tocar columna, sellado ni drip. | `DECISIONS.md:6` |
| W0.3 | **D3** (Q3 = A) Nutrición apagada ⇒ el anillo desaparece, sin banner, sin pill/chip/tab en home y directorio (RN). Encendida sin plan ⇒ anillo gris `Sin plan vigente`, banner no dispara. `null` en vez de `0`. | `DECISIONS.md:7` |
| W0.4 | **D4** FAB de Alumnos: `bottom: insets.bottom + 92`, patrón `MobileQuickActionsFab` (`apps/mobile/components/coach/CoachDashboardSections.tsx:764`). | `DECISIONS.md:8` |
| W0.5 | **D5** Un solo tren: deploy web + OTA 1.1.2. Sin migraciones, sin nativo. Orden: nutrición → FAB → agenda → chip. | `DECISIONS.md:9` |
| W0.6 | **D6** Fase de plan: cero código, cero commit. Docs solo en `docs/specs/senales-honestas-coach/`. | `DECISIONS.md:10` |

## A · Nutrición (R4)

- [x] A0 **Inventario antes de tocar tipos.** Correr `grep -rn "nutritionWeeklyAvgPct\|nutritionPrevWeeklyAvgPct\|nutritionTodayCompliancePct\|nutritionPct" apps/mobile` (ignorar `apps/mobile/dist/**`) y anotar los 9 archivos vivos. Sumar `grep -rn "weeklyAvgPct\|prevWeeklyAvgPct" apps/mobile` porque `apps/mobile/lib/nutrition-coach-alerts.ts:24-25` pide `number` y hoy lo alimenta `NutricionTab.tsx:125` con `?? 0`. Los consumidores reales de estos campos son `NutricionTab.tsx:125-126, :174-175`, `OverviewTab.tsx:154, :210-211, :223, :268` y `[clientId].tsx:498` (no los consume `client-dossier-pdf.ts`).
- [x] A1 `apps/mobile/lib/coach-client-detail-logic.ts:80-101` — firma a `number | null`; `:86` `if (!activePlanId) return null`; antes del `Math.max(1, …)` de `:99`, `if (applicableMeals.length === 0) return null`. Un 0 real (plan activo con comidas y ninguna completada) sigue devolviendo `0`.
- [x] A2 `apps/mobile/lib/coach-client-detail-logic.ts` (al final del archivo) — nueva función pura `resolveNutritionSignal({ nutritionEnabled, weeklyAvgPct, prevWeeklyAvgPct, todayPct })` que devuelve `{ showRing, ringValue, ringHint, alertInput, atRisk }`. Es la única que decide qué se ve; los `.tsx` solo pintan.
- [x] A3 `apps/mobile/lib/coach-client-detail.ts:325-326` — `nutritionAveragePct(rows): number | null`; `if (!rows.length) return null`.
- [x] A4 `apps/mobile/lib/coach-client-detail.ts:202-203` y `:696` — `nutritionWeeklyAvgPct`, `nutritionPrevWeeklyAvgPct` y `nutritionTodayCompliancePct` pasan a `number | null` (paridad con `nutritionMonthlyAvgPct`, `:695`, que ya lo es).
- [x] A5 `apps/mobile/lib/coach-client-detail.ts:738` y `:750` — en el objeto `EMPTY`, los dos pct de `compliance` y `nutritionTodayCompliancePct` pasan de `0` a `null`.
- [x] A6 `apps/mobile/components/coach/clientDetail/OverviewTab.tsx:354-379` — `Ring` acepta `value: number | null` y `hint?: string`; con `value == null` pasa `empty` a `ComplianceRing` (`apps/mobile/components/ComplianceRing.tsx:18,31,34,67`, que ya pinta gris + «—») y el `accessibilityLabel` dice `Sin plan vigente`. El `hint` se pinta entre label y delta.
- [x] A7 `apps/mobile/components/coach/clientDetail/OverviewTab.tsx:122-144` — prop nueva `nutritionEnabled: boolean` (booleano, no el objeto `resourceDomains`).
- [x] A8 `apps/mobile/components/coach/clientDetail/OverviewTab.tsx:210-211` y `:251` — reemplazar el cálculo inline por `resolveNutritionSignal`; el `Ring` de Nutrición solo se renderiza si `showRing`, con `hint = 'Sin plan vigente'` cuando el valor es `null` y sin delta fabricado.
- [x] A9 `apps/mobile/components/coach/clientDetail/OverviewTab.tsx:220-229` — `nutritionCompliancePercent` recibe `undefined` cuando la señal no es honesta; `packages/profile-analytics/top-alert.ts:32,43` ya omite la regla con `null` y no se toca.
- [x] A10 `apps/mobile/components/coach/clientDetail/OverviewTab.tsx:268`, `:400` y `:461-480` — `ProgramSummary.nutritionAtRisk: boolean | null`; la píldora «Nutrición en riesgo / en track» solo se renderiza con `nutritionAtRisk != null`.
- [x] A11 `apps/mobile/app/coach/cliente/[clientId].tsx:773-788` — pasar `nutritionEnabled={resourceDomains.nutrition !== false}` a `OverviewTab`; `onViewNutrition` (`:782`) queda como está.
- [x] A12 `apps/mobile/app/coach/cliente/[clientId].tsx:593-599` — `todayNutritionPct: number | null` (null si el dominio está apagado o no hay `data.activeNutrition`); `:599` suma los 20 puntos solo con `todayNutritionPct != null && todayNutritionPct < 60`.
- [x] A13 `apps/mobile/app/coach/cliente/[clientId].tsx:606` y `:627` — `nutritionAdherencePct: todayNutritionPct` (ya acepta `null` en `packages/profile-analytics/client-status.ts`) y `heroChips.nutritionPct: number | null`.
- [x] A14 `apps/mobile/components/coach/clientDetail/ClientHero.tsx:55` y `:189-193` — `HeroChips.nutritionPct: number | null` y `ClientHeroProps` suma `nutritionEnabled: boolean` (cableada desde `[clientId].tsx:773-788`, misma fuente que A11). El gate del chip completo es `nutritionEnabled`: con `false` el chip «Comidas hoy» **no se renderiza** (cuadrante OFF, `SPEC.md:329-331`); con el dominio encendido y `nutritionPct === null` el chip **sigue presente en «—» sin el sub** (cuadrante «ON · sin plan», `SPEC.md:329-331`), y el sub `{N}% plan` solo se pinta con `nutritionPct != null`. Deja de decir «0 % plan» con el dominio apagado.
- [x] A15 `apps/mobile/app/coach/cliente/[clientId].tsx:498` — la razón «Adherencia nutricional baja esta semana.» exige además dominio encendido y `nutritionWeeklyAvgPct != null`. Justificación: **falta el gate de dominio** (el `?? 0` no dispara solo: `:498` ya exige `data.activeNutrition`).
- [x] A16 `apps/mobile/app/coach/(tabs)/home.tsx` — `const { enabled: nutritionEnabled } = useDomainGuard('nutrition')` (`apps/mobile/lib/domain-guard.ts:53-56`) y pasarlo a `<MobileClientStatsSheet>` (`:210-214`). Respetar el contrato de consumo del guard: sin early-return antes de los hooks.
- [x] A17 `apps/mobile/components/coach/CoachDashboardSections.tsx:2908-2922` y `:2960-2963` — `MobileClientStatsSheet` recibe `nutritionEnabled`; con el dominio apagado el tab se fuerza a `'adherence'` y el array de tabs queda con un solo elemento (sin switcher).
- [x] A18 `apps/mobile/components/coach/CoachDashboardSections.tsx:2062` — el label del foco filtra `NUTRICION_RIESGO` de `item.flags` cuando `nutritionEnabled === false`, así el coach no lee «Nutricion en riesgo» (`:1963`) con el dominio apagado.
- [x] A19 `apps/mobile/app/coach/(tabs)/clientes.tsx` (**screen**, `:331+`) — `const { enabled: nutritionEnabled } = useDomainGuard('nutrition')` en el screen y cablearlo hacia abajo (`DenseDirectoryTable`, `DirectorySummary`, `DirRowCard`, `DirectoryFilterSheet`). Sin early-return antes de los hooks.
- [x] A19b `apps/mobile/app/coach/(tabs)/clientes.tsx:177-315` — `DenseDirectoryTable` es un componente aparte dentro del mismo archivo (R16): gana la prop `nutritionEnabled: boolean`, cableada desde el screen (A19). Con ella se apaga `nutritionRisk` (`:240`) y el ícono `Apple` (`:270`).
- [x] A20 `apps/mobile/app/coach/(tabs)/clientes.tsx:626-629` — mover el conteo a un helper puro `nutritionLowCountFor(pulseRows, nutritionEnabled)` en `apps/mobile/lib/clients-directory.ts` (junto a los contadores existentes) y consumirlo desde el `useMemo`. **No** tocar `filterClients` (`clients-directory.ts:285-290`) para no mover `tests/mobile-directory-pulse-parity.test.ts`.
- [x] A21 `apps/mobile/components/coach/directory/DirectorySummary.tsx:156` y `:193-198` — prop `nutritionEnabled: boolean`; el tile `Nutri.` (`:197`) solo entra en `metricTiles` con el dominio encendido (la grilla pasa a 3). Cablear desde `clientes.tsx:861-870`.
- [x] A22 `apps/mobile/components/coach/directory/DirRowCard.tsx:93` y `:135-143` — prop `nutritionEnabled?: boolean` con default `true` (fail-OPEN); `hasNutritionData = nutritionEnabled !== false && nutritionPct > 0`.
- [x] A23 `apps/mobile/components/coach/directory/DirectoryFilterSheet.tsx:24` — ocultar la fila `nutrition_low` con el dominio apagado (prop nueva) y, en `clientes.tsx`, un `useEffect` que resetee `riskFilter` a `'all'` cuando `!nutritionEnabled` y el filtro activo sea `nutrition_low`.
- [x] A24 **Servidor** `apps/web/src/services/dashboard.service.ts:715-724` — destructurar también `perDay` de `computeNutritionAdherence` (el tipo ya lo expone: `packages/nutrition-engine/adherence.ts:92`, con `applicableMeals` en `:77`). El engine NO se toca.
- [x] A24b `apps/web/src/services/dashboard.service.ts:741-748` (`nutritionCompliance` en `:745`) — pasa a `null` cuando ningún día del rango tuvo comidas aplicables (`perDay.some((d) => d.applicableMeals > 0)`); `calculateAttentionScore` (`:73`) ya omite el término con `null` (`:101-105`), así que el flag `NUTRICION_RIESGO` y sus 20 puntos dejan de nacer para alumnos sin plan. Extraer la expresión a un helper exportado del mismo servicio (junto a `applyNutritionAttentionScore`, `:66`) con el nombre canónico **`nutritionComplianceFromAdherence(perDay, summary)`** (R18), para poder testearlo en N8.
- [x] A24c `apps/web/src/services/dashboard.service.ts:253` — `DirectoryPulseRow.nutritionPercentage` **sigue** `number` para no mover las 4 superficies web que lo consumen: rellenar con `?? 0` solo en ese campo de presentación y dejarlo documentado en el comentario, aclarando que el `null` entra ANTES del score.
- [x] A25 **Web ficha** `apps/web/src/app/coach/clients/[clientId]/ClientProfileDashboard.tsx:117` y `:271-277` — pasar una prop nueva `nutritionEnabled={domainsEnabled.nutrition !== false}` a `ProfileOverviewB3`, para distinguir «dominio apagado» (ocultar) de «sin plan» (gris).
- [x] A26 **Web ficha** `apps/web/src/app/coach/clients/[clientId]/ProfileOverviewB3.tsx:306-319` — el `ComplianceRing` de Nutrición no se renderiza con `nutritionEnabled === false`; el copy del `hint` (`:314`) pasa de `sin plan vigente` a `Sin plan vigente` (R1: una sola variante en web y RN).
- [x] A27 **Tests del carril**: N3 (`tests/mobile-coach-client-detail-logic.test.ts`, ampliar), N7 (`tests/mobile-directory-nutrition-gate.test.ts`, nuevo) y N8 (`apps/web/src/services/dashboard-attention-nutrition.test.ts`, nuevo). Detalle y casos en [TESTING-QA](TESTING-QA.md).
- [x] A28 **Gates del carril**: `pnpm --filter @eva/mobile exec tsc --noEmit` (el que más duele por A1–A5) · `pnpm lint:mobile` · `pnpm typecheck` · `pnpm exec vitest run tests/mobile-coach-client-detail-logic.test.ts tests/mobile-directory-pulse-parity.test.ts tests/mobile-directory-nutrition-gate.test.ts packages/profile-analytics/top-alert.test.ts packages/profile-analytics/client-status.test.ts apps/web/src/services/dashboard-attention-nutrition.test.ts`.

## B · FAB (R3)

- [x] B1 `apps/mobile/app/coach/(tabs)/clientes.tsx:1413` — eliminar `bottom: 84,` de `styles.fab` (el resto de `:1410-1420` queda igual), para que no compita con el valor inline.
- [x] B2 `apps/mobile/app/coach/(tabs)/clientes.tsx:1076` — `style={[styles.fab, { backgroundColor: theme.primary, bottom: insets.bottom + 92 }, GLOWS.sport]}`. `insets` ya existe en el scope (`:333`). Sin tests: se valida en QA de device (Q11–Q13).

## C · Agenda (R1)

- [x] C1 `packages/profile-analytics/agenda-label.ts` (**nuevo**) — módulo puro compartido web/RN con `AgendaKind`, `AgendaSeverity`, `agendaSeverity(days)`, `programSeverity(daysLeft)` (`<= 0 ⇒ danger`, `1..3 ⇒ warning`, `>= 4 ⇒ none`), `daysSince(fromYmd: string | null, todayYmd: string): number | null` (resta de YMD de Santiago, **nunca** la medianoche del runtime: el llamador arma los YMD con `getSantiagoIsoYmdForUtcInstant` y `getTodayInSantiago().iso`) y `buildAgendaLabel(input)`. El módulo exporta además **`shortDayMonthEs(ymd: string): string`** (R22), con la tabla `SHORT_MONTHS_ES` copiada de `apps/web/src/lib/date-utils.ts:163` (12 entradas, septiembre = `sept`): es la **única** fuente de fecha corta para servidor y RN, sin `Intl` en ningún lado. `buildAgendaLabel` recibe la fecha ya formateada; los copys son los de la tabla del PLAN. Precedente de package consumido por ambos: `packages/profile-analytics/top-alert.ts`.
- [x] C1b `packages/profile-analytics/index.ts:28-33` — agregar `export * from './agenda-label'` al barrel, junto a los seis `export *` existentes. **Es obligatorio**: `package.json:8-10` publica solo la raíz (`"exports": { ".": "./index.ts" }`) y `vitest.config.ts:35` aliasea únicamente `@eva/profile-analytics` → `index.ts`, así que sin esta línea el subpath queda bloqueado en Node, en Metro y en el runner, y ni web ni RN ni N4/N5/N6 ven `buildAgendaLabel`.
- [x] C2 `apps/web/src/app/coach/dashboard/_data/dashboard.queries.ts:341` — exportar `buildAgendaFromPulse` (hoy es privada; `dashboard.queries.test.ts:126` ya importa por nombre desde el módulo) y devolver `{ items: items.slice(0, 8), total: items.length }` en vez del `slice` pelado de `:391`.
- [x] C2b `apps/web/src/app/coach/dashboard/_data/dashboard.queries.ts:355-389` — cada fila calcula `days` con `daysSince(getSantiagoIsoYmdForUtcInstant(dueAt), getTodayInSantiago().iso)` (nunca `daysSince(dueAt, now)`: en el servidor, UTC, entre las 21:00 y medianoche de Chile eso cuenta un día de más), `severity` y `label` con `buildAgendaLabel`; la fecha se formatea **en servidor** con `shortDayMonthEs(getSantiagoIsoYmdForUtcInstant(dueAt))` — la función del package (C1, R22), con el YMD armado por `getSantiagoIsoYmdForUtcInstant` (`apps/web/src/lib/date-utils.ts:340`) —, nunca `Intl`. El `else if` check-in/ejercicio se mantiene: una fila por alumno.
- [x] C2c `apps/web/src/app/coach/dashboard/_data/dashboard.queries.ts:341-392` — ordenar por urgencia antes del `slice`: `danger` → `warning` → `none`, y dentro del grupo por `days` desc (los `programa_vence`, por `daysLeft` asc).
- [x] C3 `apps/web/src/app/coach/dashboard/_data/types.ts:64-72` — `AgendaItem` suma `days: number | null` y `severity: AgendaSeverity`; agregar `agendaTotal: number` al tipo del dashboard y exponerlo en los dos caminos que llaman al builder (`dashboard.queries.ts:261` y `:308`). El endpoint móvil serializa el objeto entero (`apps/web/src/app/api/mobile/coach/dashboard/route.ts:188`), no hay mapeo campo a campo que tocar.
- [x] C4 `apps/web/src/app/coach/dashboard/_components/AgendaCard.tsx:17-23` — borrar `slotTime` y la constante `done` (`:32`); el `SectionTitle` (`:35`) pasa a `Pendientes de hoy` con acción `{agendaTotal} pendientes` (`1 pendiente` en singular).
- [x] C4b `apps/web/src/app/coach/dashboard/_components/AgendaCard.tsx:46-60` — la fila pinta punto de severidad + nombre + `label`; `:57` deja de imprimir la hora. El `href` de cada fila (`a.href`) no cambia.
- [x] C4c `apps/web/src/app/coach/dashboard/_components/AgendaCard.tsx:40-44` — el vacío pasa de `Todo cerrado` / `Sin pendientes en el día.` a `Todo al día` / `Sin pendientes hoy.`; si `agendaTotal > 8`, agregar la última fila `y {N} más en Alumnos` con link a `/coach/clients`.
- [x] C5 `apps/web/src/app/coach/dashboard/_components/PriorityCard.tsx:66-73` y `apps/web/src/app/coach/dashboard/_components/DashboardShell.tsx:257, :266` — `agendaPending` pasa a alimentarse de `agendaTotal` (no `data.agenda.length`, hoy `DashboardShell.tsx:257`) y el CTA (`PriorityCard.tsx:70`) pasa de `Ver agenda` a `Ver pendientes`.
- [x] C5b `apps/web/src/app/coach/dashboard/_components/PriorityCard.tsx:22, :40, :71, :136, :145` y `DashboardShell.tsx:254-261` — prop nueva opcional `agendaTopHref?: string` que `DashboardShell` alimenta con `data.agenda[0]?.href` (tras C2c, el más urgente) y que la rama `agendaPending > 0` usa como `href: agendaTopHref ?? '/coach/clients'`, en vez del `/coach/clients` fijo de `:71`. Sin esto Q6 («el NBA manda al **mismo** alumno que encabeza la lista») es imposible de aprobar en web. Las otras ramas de `resolveNextStep` (`:63`, `:78`) siguen apuntando al directorio.
- [x] C6 Borrar `apps/web/src/app/coach/dashboard/_components/today/TodayAgenda.tsx` (huérfano: `grep -rn "TodayAgenda" apps/web/src` solo devuelve su propia definición, `:16`). La carpeta `today/` queda vacía y se elimina.
- [x] C7 `apps/mobile/lib/coach-dashboard.ts:91-97` — `MobileAgendaItem` suma `dueAt: string | null`, `days: number | null`, `severity: AgendaSeverity`; el payload ya trae los campos, solo los descartaba el tipo. Sumar `agendaTotal` a `MobileDashboardData` (`:199`) y a `MobileDashboardApiResponse` (`:706`); `mapApiDashboard` (`:789`, `:823`) no recorta campos.
- [x] C8 `apps/mobile/lib/coach-dashboard.ts:1115-1130` — el fallback local arma el mismo shape con `buildAgendaLabel` (mismos strings que el servidor), tomando `dueAt` de `latestWorkout.get(id).logged_at` y `latestCheckIn.get(id).created_at` (mapas ya existentes en `:989-990`). La fecha se formatea con **`shortDayMonthEs`** del package (C1, R22) sobre el YMD de Santiago que da `apps/mobile/lib/date-utils.ts` (`getSantiagoIsoYmdForUtcInstant`, `:75`; hoy con `getTodayInSantiago`, `:58`): **nunca** `toLocaleDateString`. Deja de reusar `client.label` de `riskItems`. **`agendaTotal` (R17)** se cuenta sobre `riskItems` (`:1061`) más los programas, **ANTES** de los `slice` (`:1087`, `:1113`, `:1130`), nunca sobre `topRiskClients` (N4: fallback con 9 riesgos ⇒ `agendaTotal: 9` y 8 filas).
- [x] C9 `apps/mobile/components/coach/CoachDashboardSections.tsx:2318, :2320` (`MobileTodayAgenda`) — título `Pendientes de hoy` y acción `{agendaTotal} pendientes` en lugar de `0 de {items.length} hechas`. La firma del componente (`:2309`) pasa a recibir también `agendaTotal`.
- [x] C9b `apps/mobile/components/coach/CoachDashboardSections.tsx:2329-2330, :2344-2351` — quitar `startMinutes`/`slot` y reemplazar el bloque hora+ícono del `leading` por el punto de severidad (colores `WARNING`/`DANGER` de `apps/mobile/components/coach/directory/directory-shared.ts:13-15` y `theme.mutedForeground`); `subtitle` sigue siendo `item.label`.
- [x] C9c `apps/mobile/components/coach/CoachDashboardSections.tsx:2324` — el vacío pasa a `Todo al día` / `Sin pendientes hoy.`; con `agendaTotal > 8`, última fila `y {N} más en Alumnos` que navega a `/coach/clientes`.
- [x] C10 `apps/mobile/components/coach/CoachDashboardSections.tsx:2215-2222` — el NBA `agenda-hoy` usa `agendaTotal` en el título (`:2218`, con singular `1 pendiente hoy`) y sigue navegando a `agenda[0]` (`:2006-2009`), que tras C2c es el más urgente. **Cableado (R26)**: `agendaTotal` baja como prop a `MobileFocusList` / NBA desde `apps/mobile/app/coach/(tabs)/home.tsx:186-192` (junto a `agenda`, `kpi` y `expiringPrograms`), además del `<MobileTodayAgenda>` de `:195`. Cambio de destino esperado: va al checklist (Q6).
- [x] C10b `apps/mobile/components/coach/CoachDashboardSections.tsx:2219` y `:2220` — el copy del NBA deja de describir la lista vieja: `description` pasa de `Cierra los check-ins y recordatorios pendientes.` a `Alumnos sin entrenos, sin check-in o con programa por vencer.` (copy canónico, R21) y `ctaLabel` pasa de `Ver agenda` a `Ver pendientes` (SPEC R1.15, espejo de C5 en web). El resto del resolver (`:2205-2213`, `:2225-2231`) no se toca.
- [x] C11 **Tests del carril**: N4 (`tests/mobile/coach-dashboard-agenda.test.ts`, nuevo, cabecera copiada de `tests/mobile/coach-dashboard-deltas.test.ts:13-30`), N5 (`packages/profile-analytics/agenda-label.test.ts`, nuevo) y N6 (`apps/web/src/app/coach/dashboard/_data/dashboard.queries.test.ts`, ampliar).
- [x] C12 **Gates del carril**: `pnpm typecheck` · `pnpm --filter @eva/mobile exec tsc --noEmit` · `pnpm exec vitest run packages/profile-analytics/agenda-label.test.ts apps/web/src/app/coach/dashboard/_data/dashboard.queries.test.ts` · `pnpm exec vitest run --project mobile-node tests/mobile/coach-dashboard-agenda.test.ts tests/mobile/coach-dashboard-deltas.test.ts`.

## D · Chip (R2)

- [x] D1 `apps/web/src/app/coach/clients/_lib/client-status.ts:50-51` — agregar `const ENTERED_CHIP_WINDOW_DAYS = 7` junto a `HOUR_MS`/`DAY_MS`, y extraer el cálculo de días de `enteredLabel` (`:67-77`) a un helper local reusable.
- [x] D2 `apps/web/src/app/coach/clients/_lib/client-status.ts:109-116` — la rama `firstLoginMs !== null` devuelve `{ key: 'active', label: 'Activo', cls: … }` cuando `days > ENTERED_CHIP_WINDOW_DAYS`; 7 exactos siguen en `entered`. `enteredLabel`, `FIRST_LOGIN_SIGNAL_CUTOVER` (`:28`) y `clientStatusInputFromRow` no se tocan.
- [x] D3 `apps/mobile/components/coach/directory/directory-shared.ts:49-50` y `:110-111` — mismo cambio espejado en `statusMeta`, devolviendo `{ key: 'active', label: 'Activo', tone: 'success' }`. **Ningún `DirRowCard` se toca**: verificado en el repo, web solo calcula `st` en `apps/web/src/app/coach/clients/DirRowCard.tsx:89` y el gate real está en `:191` (`{st.key !== 'active' ? …}`, con `{st.label}` en `:198`), y RN gatea igual en `apps/mobile/components/coach/directory/DirRowCard.tsx:144`. Es decir, a los 8 d la píldora **desaparece sola** en ambas plataformas, como piden SPEC R2.6 (`SPEC.md:238-239`) y Q15.
- [x] D4 **Tests del carril**: N1 (`apps/web/src/app/coach/clients/_lib/client-status.test.ts:91-98`, partir el `it` de «varios días») y N2 (`tests/mobile/directory-status.test.ts:111-114`, espejo). Detalle en [TESTING-QA](TESTING-QA.md).
- [x] D5 **Gates del carril**: `pnpm exec vitest run apps/web/src/app/coach/clients/_lib/client-status.test.ts` · `pnpm exec vitest run --project mobile-node tests/mobile/directory-status.test.ts` · `pnpm typecheck` · `pnpm --filter @eva/mobile exec tsc --noEmit`.

## E · Cierre

- [x] E1 Revisión de los cuatro diffs contra este documento y contra `DECISIONS.md` (jefe), carril por carril.
- [x] E2 Gates completos, en este orden: `pnpm docs:check` · `pnpm lint` · `pnpm lint:mobile` · `pnpm typecheck` · `pnpm --filter @eva/mobile exec tsc --noEmit` · `pnpm exec vitest run apps/web/src/app/coach/clients/_lib/client-status.test.ts apps/web/src/app/coach/dashboard/_data/dashboard.queries.test.ts packages/profile-analytics/agenda-label.test.ts packages/profile-analytics/top-alert.test.ts packages/profile-analytics/client-status.test.ts apps/web/src/services/dashboard-attention-nutrition.test.ts tests/mobile-coach-client-detail-logic.test.ts tests/mobile-directory-pulse-parity.test.ts tests/mobile-directory-nutrition-gate.test.ts` · `pnpm exec vitest run --project mobile-node tests/mobile/directory-status.test.ts tests/mobile/coach-dashboard-deltas.test.ts tests/mobile/coach-dashboard-agenda.test.ts tests/mobile/domain-guard.test.ts tests/mobile/client-tabs.test.ts` · `pnpm check:tokens`. **Los archivos del package van uno por uno, como en A28**: pasar la carpeta `packages/profile-analytics` como filtro posicional barre también `overview.test.ts`, que se pone rojo solo entre las 21:00 y medianoche de Chile (`packages/profile-analytics/overview.test.ts:10, :12`: `new Date()` + `toISOString()` en UTC contra ventanas en hora local) y teñiría de rojo el gate de cierre justo cuando su salida es la evidencia. `overview.test.ts`, `strength.test.ts` y `body-composition.test.ts` no los toca este tren; si se quieren correr igual, se corren aparte y de día.
- [x] E3 `docs/status/CURRENT.md` — medir con `wc -c docs/status/CURRENT.md` **antes y después** (hoy 16 106 B, tope 16 384 B: entra una fila nueva solo si se recorta una anterior). Mover una entrada «Anterior …» de la fila Web/PWA a su propia spec y volver a correr `pnpm docs:check`.
- [x] E4 `docs/status/MOBILE_PARITY.md` — blockquote de evidencia de QA + la deuda web de nutrición declarada (`apps/web/src/app/coach/clients/CoachWarRoom.tsx:222` y `:350-358`, `apps/web/src/app/coach/dashboard/_components/kpi/KpiStrip.tsx:45`). `docs/testing/TEST_STATUS.md` solo si cambia un gate local obligatorio (no mantiene inventario manual de tests).
- [ ] E5 Commit en `rnmobiledenuevo` con los cuatro carriles y los docs.
- [ ] E6 **Solo a pedido del owner**: push a `rnmobiledenuevo` y `master`, deploy web (`dpl_…` READY) y OTA 1.1.2 canal `production` desde el piso Apple (`docs/operations/MOBILE_RELEASES_OTA.md`); verificar con `eas update:list --branch production --limit 8 --json` un grupo por plataforma con `runtimeVersion` 1.1.2.
- [ ] E7 E2E `prod-suave` tras el deploy: `pnpm qa:prod:suave` (un solo navegador), registrar resultado y run.
- [ ] E8 **Owner**: QA según [TESTING-QA](TESTING-QA.md) **completo, Q1–Q24**. Q1–Q22 en device (RN) y **Q23–Q24 en web**, que no son opcionales: Q23 firma la ficha web con el dominio apagado y el copy `Sin plan vigente` (A25/A26, R1) y Q24 la hidratación de `/coach/dashboard` en Safari iOS (gotcha `EVA-NEXTJS-18`, riesgo declarado del carril C). Sin Q23 y Q24 el SDD no pasa a `done` aunque el device esté verde.
- [ ] E9 **Owner**: aviso a Movens con el texto de [TESTING-QA](TESTING-QA.md) § Aviso a coaches. Lo manda el owner, nunca la sesión.

## Backlog

Deuda declarada, fuera de alcance de este tren (se anota también en `docs/status/MOBILE_PARITY.md`):

- `apps/web/src/app/coach/clients/CoachWarRoom.tsx:222` y `:350-358` — el tile «Nutri.» y su filtro del directorio web no gatean por dominio. Con A24 el contador deja de mentir para coaches sin planes, que es el caso real, pero el gate por dominio sigue faltando. (Las RESOLUCIONES citan `:352-356`; la línea real del bloque `DirMetricChip` es `:350-358`.)
- `apps/web/src/app/coach/dashboard/_components/kpi/KpiStrip.tsx:45` — el hint `Nutricion: {avgNutrition}%` se pinta sin gate de dominio.
- Fila «y N más en Alumnos» de la agenda: navega al directorio sin filtro preaplicado; queda pendiente llevar el filtro de pendientes.
- Agenda con horario real: no existe tabla ni UI de agendamiento; «Pendientes de hoy» es trabajo derivado, no una agenda.
- Marcar un pendiente como hecho: no hay estado persistido; por eso se retira «0 de N hechas» en vez de cablearlo.
- El gate de nutrición vive en presentación: el payload de dashboard/pulse sigue viajando con datos de nutrición aunque el coach la tenga apagada (son datos del propio coach; aceptado).
- `apps/mobile/components/coach/clientDetail/NutricionTab.tsx:125-126, :174-175` — la pestaña Nutrición sigue rellenando con `?? 0` hacia `apps/mobile/lib/nutrition-coach-alerts.ts:24-25` (pide `number`), así que el `null` honesto de `ComplianceSummary` vuelve a ser 0 % ahí. Ítem nuevo si se quiere «sin dato ⇒ sin alerta» (cambiar el input a `number | null`).
- Fallback offline RN (`buildLocalAgenda`): la consulta de check-ins del camino degradado mira 30 d y `SIN_CHECKIN_1M` significa justamente «sin check-in en 30 d» ⇒ esas filas salen sin fecha («Todavía no registra check-ins», `none`) mientras el servidor imprime «Sin check-in desde el … · N d». Ensanchar la ventana de la consulta o traer `lastCheckinDate` del pulse.
- **Pulse de nutrición sobre V2 (R15).** El pulse de nutrición (`apps/web/src/services/dashboard.service.ts:548-560, :679-748`) lee SOLO tablas V1 (`daily_nutrition_logs` / `nutrition_meal_logs`). V1 está congelada y V2 es la canónica, así que hoy **todo** alumno que registra en V2 (o que no tiene plan) recibe un `NUTRICION_RIESGO` falso y sus +20 puntos de atención. Con el `null` de A24b ese falso positivo desaparece. **Efecto secundario aceptado**: un alumno con plan V1 activo y cero registros en 35 días deja de llevar el flag de nutrición (sigue con los de entreno y check-in). Queda como deuda llevar el pulse a V2: hoy el service no conoce `nutrition_plans_v2` (`apps/web/src/app/coach/dashboard/_data/dashboard.queries.ts:986` solo lo usa para el demo).

## Cierre

**Ejecución 2026-09-10 (sesión «Asistente Principal», jefe Fable + workers Opus/Sonnet vía Workflow).** W1 (4 workers Opus/Sonnet por lote de archivos), W2 (2 líneas, jefe), W3 (package → web ∥ RN, Opus), W4 (Sonnet), W5 (docs Sonnet). Base `f93378c3`; commit del tren en `rnmobiledenuevo`, **sin push** (E6/E7 a pedido del owner).

### Gates E2 (salida real, en este orden)

| Gate | Resultado |
|---|---|
| `pnpm docs:check` | OK — 20 canónicos, 261 Markdown activos, `CURRENT.md` bajo el tope (medido antes y después de E3) |
| `pnpm lint` | exit 0 — 0 errores, 549 warnings preexistentes del repo; en los archivos del tren solo los 3 warnings viejos de `dashboard.queries.ts` (`applyOrgScope`, `scoped`, `_`) |
| `pnpm lint:mobile` | exit 0 |
| `pnpm typecheck` | exit 0 |
| `pnpm --filter @eva/mobile exec tsc --noEmit` | exit 0 (corrido tras W1, W3, el fix del comparador y W4) |
| `vitest run` web-node (9 archivos: N1, N6, N5, top-alert, client-status, N8, N3, pulse-parity, N7) | 9 files / **101 tests** verdes |
| `vitest run --project mobile-node` (N2, deltas, N4, domain-guard, client-tabs) | 5 files / **58 tests** verdes |
| `pnpm check:tokens` | OK — 86 governed + 5 seal |

### Tests nuevos o ampliados

N1 `client-status.test.ts` (+3, 24 total) · N2 `tests/mobile/directory-status.test.ts` (21) · N3 `tests/mobile-coach-client-detail-logic.test.ts` (+2, matriz 4 cuadrantes) · N4 `tests/mobile/coach-dashboard-agenda.test.ts` (**nuevo**, 9) · N5 `packages/profile-analytics/agenda-label.test.ts` (**nuevo**, 22) · N6 `dashboard.queries.test.ts` (+7, 15) · N7 `tests/mobile-directory-nutrition-gate.test.ts` (**nuevo**, 7) · N8 `apps/web/src/services/dashboard-attention-nutrition.test.ts` (**nuevo**, 4).

### Decisiones del jefe durante la ejecución

- **Comparador de la agenda (web y RN)**: el snippet del PLAN (`(b.days ?? -1) - (a.days ?? -1)`) mandaba los `programa_vence` al final de su grupo; la prosa del PLAN y el ejemplo obligatorio de N6 los ponen ANTES. Gana la prosa: `compareAgendaStaleness` (null primero, luego `days` desc) en `dashboard.queries.ts` y espejo en `buildLocalAgenda` (RN); N4 y N6 lo fijan.
- **`atRisk` de `resolveNutritionSignal`** se decide con `todayPct < 60` (la ventana que ya tenía la píldora), no con el semanal: cero cambio de comportamiento en «ON · con plan». El delta del anillo se arma en `OverviewTab` con los dos promedios en `number` (contrato #6 intacto, 5 campos).
- **Fallback RN extraído a `buildLocalAgenda`** (pura, exportada) y `mapApiDashboard` exportada solo para N4 (precedente `mapKpiDeltas`). `agendaTotal` del payload móvil es opcional con default `?? rows.length` (deploy viejo).
- **`SHORT_MONTHS_ES` del package se exporta** para que N5 la compare entrada por entrada con la copia literal de la web (la de `date-utils.ts:163` es privada y no se tocó).
- `DesktopBento.test.tsx` gana `agendaTotal: 0` en su fixture (obligatorio para `typecheck` con el campo requerido en `DashboardV2Data`).
- `summaryMetricKeys(nutritionEnabled)` en `clients-directory.ts` es la única fuente del orden de tiles del resumen (probado en N7).

### Pendientes declarados (fuera del alcance del tren, van al Backlog y a MOBILE_PARITY)

- `NutricionTab.tsx:125-126, :174-175` sigue con `?? 0` hacia `nutrition-coach-alerts.ts` (pide `number`): el `null` honesto se vuelve 0 en esa pestaña. Ítem nuevo si se quiere «sin dato ⇒ sin alerta».
- Fallback offline RN: la consulta de check-ins mira 30 d y `SIN_CHECKIN_1M` significa «sin check-in en 30 d» ⇒ esas filas salen sin fecha («Todavía no registra check-ins», severidad `none`) mientras el servidor imprime «Sin check-in desde el … · N d». Afecta el texto esperado de Q10.
- Web: `agendaTopHref` solo se monta en el ancho móvil del dashboard (`DesktopBento` no monta `PriorityCard`): Q6 web se firma en ese ancho.
- Borde cosmético: payload con `agendaTotal > 0` y todas las filas caídas por el guard de uuid mostraría «N pendientes» sobre «Todo al día» (payload corrupto, ya deja migaja en Sentry).
