---
status: active
owner: product-engineering
last_verified: "2026-08-21"
canonical: false
---

# TASKS — Embudo Free → Pro

Archivos:línea verificados contra `rnmobiledenuevo` @ `8bf45cf4` el 2026-08-21. Modelo sugerido entre paréntesis.
**Orden obligado: W0 → W1 → (aprobación del owner) → W2 → W3 → W4 → W5 → W6 → W7 → W8.**

## W0 — Desatascar hoy (jefe)
- [ ] W0.1 Mandarle el link de `/coach/subscription` por WhatsApp al coach que está esperando (operación del owner, 0 h).
- [x] W0.2 `void` → `await` en `coach/clients/_actions/clients.actions.ts:128`, `coach/clients/import/_actions/import.actions.ts:201`, `api/mobile/coach/clients/route.ts:212`, `api/mobile/coach/clients/import/route.ts:270`. Verificado antes: `sendSalesEmailOnce` envuelve todo en `try/catch` y el test «excepción inesperada se traga» lo pinnea (`sales-emails.service.test.ts`). Comentario «Fire-and-forget» corregido.

## W1 — Encender el canal que ya existe: cron `cap-nudge` (2 Opus + juicio)
- [x] W1.1 (Opus A) `lib/email/sales-templates.ts:58-94`: `ClientLimitReachedContext.trigger?: 'attempt' | 'sweep'` (default `attempt`, copy actual intacto). `sweep`: reemplaza «Por eso no pudimos sumar al alumno que intentaste agregar.» por una frase de estado («El próximo alumno que quieras sumar no va a entrar hasta que liberes un cupo o amplíes tu plan.») y ajusta el `previewText`. Mismo único CTA, cero precios.
- [x] W1.2 (Opus A) `services/billing/sales-emails.service.ts:232-260`: `ClientLimitEmailInput.trigger?` pasa a la plantilla; `payload` del ledger lleva `trigger`; `buildSubscriptionUrl({ utmSource })` opcional (sin argumento = URL idéntica a hoy) y el correo de cupo usa `utm_source=cap_email&utm_medium=email&utm_campaign=<trigger>`.
- [x] W1.3 (Opus A) Tests: `sales-templates.test.ts` (caso `sweep`: sin «intentaste», `assertNoPrices`, 1 link; `attempt` sin cambios) y `sales-emails.service.test.ts` (`sendClientLimitReachedEmail` con `trigger: 'sweep'` → payload y URL con UTM; sin `trigger` → comportamiento actual).
- [x] W1.4 (Opus B) Nuevo `api/cron/cap-nudge/cap-nudge.ts` (puro, sin red): `CAP_NUDGE_SCHEDULE_DAYS = [0, 7, 28]`, `resolveCapNudgeDecision({ priorSends, currentLimit, now })` → `send | skip` con `touch` y `reason` (`first_touch | ladder_due | ladder_not_due | max_touches`); solo cuentan envíos del MISMO `current_limit` (subir de plan resetea la escalera); `isAtCap({ activeCount, maxClients })`.
- [x] W1.5 (Opus B) Nuevo `api/cron/cap-nudge/route.ts` (forma de `checkin-reminder`, auth `timingSafeEqual` de `paid-expiry`): candidatos `coaches` con `subscription_tier = 'free'` (constante `CAP_NUDGE_TIERS`), `subscription_status = 'active'`, `active_org_id IS NULL`; conteo de `clients` no archivados, `org_id IS NULL`, `team_id IS NULL` (mismo predicado que el 402 móvil), paginado; cupo = `max_clients ?? tierMaxClientsFor(tier, created_at)`; ledger leído UNA vez (`admin_audit_logs`, acción `coach.sales_email_client_limit_reached`, 400 d); email por `resolveCoachEmail`; `isTestCoachEmail` excluye cuentas de prueba; `?dry=1` lista sin enviar; `await sendClientLimitReachedEmail(admin, { …, source: 'cron_cap_nudge', trigger: 'sweep' })`; `try/catch` por coach (fail-open); resumen `cron.cap_nudge_ran`; JSON con conteos por outcome. NO reusar `payment-reminder`.
- [x] W1.6 (Opus B) Tests: `cap-nudge.test.ts` (escalera 0/7/28, reset por nivel, tope 3), `route.test.ts` (401 ×2; en cupo → envía `sweep`; bajo cupo → no; `dry=1` → 0 envíos; cuenta de prueba → skip; Resend 500 → `failed` y la corrida sigue 200; query de coaches rota → 500; `max_clients` null → escalera por fecha), `vercel-cron.test.ts` (entrada registrada; horario distinto a `paid-expiry`/`trial-expiry`/`mp-reconcile`/`flow-reconcile`).
- [x] W1.7 (Opus B) `vercel.json`: `{ "path": "/api/cron/cap-nudge", "schedule": "0 13 * * *" }` (09:00/10:00 Chile). Vercel permite 100 crons/proyecto en todo plan desde 2026-01 (hoy hay 10). `docs/operations/RUNBOOK.md` §Crons activos: fila nueva + nota del kill-switch `EVA_SALES_EMAILS_DISABLED=client_limit_reached` y del `?dry=1`.
- [x] W1.8 (jefe) Juicio de diffs + gates (21-08, todos ejecutados): vitest dirigido 106/106 (cron 40 + templates + service + paid-expiry), suite completa **6150 verdes / 468 archivos**, `pnpm lint` 0 errores (2 warnings nuevas del mismo patrón que los crons existentes: `console.info` del resumen y `_a` del mock), `pnpm typecheck` verde, `pnpm build` verde (Next 92 s + tsc), `pnpm docs:check`, `pnpm check:tokens`, `pnpm check:nutrition-v2-boundaries`. **Pendiente operativo: primera corrida `?dry=1` desde entorno seguro → lista esperada = 15 slugs.**
- [x] W1.9 Ronda de corrección tras el revisor adversarial (re-verificada: 9/9 cerrados, 0 regresiones; residual: `maxDuration` 120→60 por precedente y tolerancia 12 h también en el cooldown del service — aplicados por el jefe) (21-08; verificó contra LIVE: 38 candidatos / 15 en cupo / 0 de prueba / 0 envíos históricos):
  - B1 `sweep` es comunicación no solicitada ⇒ pie de baja en texto plano (Ley 19.496 art. 28 B), solo en `sweep`, sin segundo link.
  - I1 throttle 600 ms entre envíos (Resend 2 req/s) + `maxDuration = 120`.
  - I2 resumen `cron.cap_nudge_ran` SIEMPRE (también en 500/excepción; `outcome: 'aborted'`).
  - I3 ledger ilegible ⇒ **fail-closed** (aborta el barrido; con mapa vacío todos vuelven a `first_touch` y el cooldown de 7 d deja spam semanal eterno).
  - I4/M2/M3/M4 `.order('id')` antes de `.range`, chunk 100, `coaches`/`clients`/ledger paginados.
  - M1 tolerancia 12 h en la escalera (sin ella la cadencia real era 0/8/30).
  - M6 decisión de escalera antes de pedir el email a GoTrue.
  - I5 tests de fail-open por coach, ledger ilegible, sin email, `ladderNotDue` en ruta, resumen en 500, insert del resumen que lanza, `order` antes de `range`.
  - M9 RUNBOOK: fila de `checkin-reminder` que faltaba (11 crons = 11 filas).
  - Declarados, sin cambio: M5 (el cron filtra `active_org_id`; no existe columna de team — 0 free con team en LIVE), **M7 corregido tras el mapa de W3**: el proxy clona el query al mandar a `/login` (`proxy.ts:426-431`) ⇒ el `$pageview` lleva `utm_source=cap_email` y posthog-js persiste los `utm_*`; lo que se pierde hasta W3 es el DESTINO (dashboard en vez de `/coach/subscription`), M8 `&` sin escapar en el `href` (lo parsean todos los clientes; tocar `base-layout` está fuera de W1).

## W2 — El canal correo completo (2 Opus + operación)
- [x] W2.1 `lib/email/send-drip-sequence.ts`: `templateByKey(templates, key: DripTemplateKey)` con el union derivado de `DRIP_TEMPLATE_KEYS as const` (exportado por `drip-templates.ts`) y `throw new Error('drip template missing: <key>')` si falta. **Desviación deliberada**: las 4 plantillas se resuelven ANTES de agendar ninguna, así el throw deja la serie entera sin agendar en vez de media serie viva en la cola de Resend. Test `send-drip-sequence.test.ts` (escrito en rojo primero: la primera pasada agendaba el D+1 antes del throw y lo delató).
- [x] W2.2 `lib/email/drip-templates.ts`: serie rediseñada para Free = 1. **Keys nuevas** (reemplazan `day1_welcome`/`day3_clients`/`day7_nutrition`/`day14_upgrade`): `day1_value` (D+1) · `day2_pro` (D+2) · `day7_nutrition` (D+7) · `day14_last_call` (D+14), exportadas como `DRIP_TEMPLATE_KEYS`. `DripTemplateContext` gana `inviteCode?: string | null`; el D+1 imprime `${baseUrl}/join/<code>` como ÚNICO link y sin código cae a «lo encuentras en Alumnos» + CTA a `/coach/clients`. El precio de Pro sale de `TIER_CONFIG.pro.monthlyPriceClp` formateado es-CL y los cupos de `studentCountLabel(...)`. Pie de baja en texto plano (`DRIP_UNSUBSCRIBE_FOOTER`) en los cuatro. Se borraron las dos afirmaciones sin respaldo del drip viejo («5× más probabilidades», «retienen el doble»). `send-drip-sequence.ts` agenda las 4 (D+1 = +24 h, D+2 = +48 h, D+7, D+14) vía `scheduleCoachEmail(admin, { …, trigger: 'drip', payload: { day } })`. **Call sites actualizados** (`scheduleFreeCoachDripSequence` y `sendFreeCoachOnboardingEmails` ganan `admin`, `coachId` e `inviteCode`): `auth/confirm/route.ts:52` (suma `invite_code` al `select`), `coach/onboarding/complete/_actions/complete.actions.ts:174` y el nuevo de W2.5. Audiencia de Resend intacta.
- [x] W2.3 Nuevo `lib/email/__tests__/no-prices.ts` (no matchea `*.test.ts` ⇒ vitest no lo colecta): `assertNoPrices`, `countLinks` (extraídos de `sales-templates.test.ts`, que ahora los importa) y `assertOnlyCatalogPrice(html, priceClp)` para los correos que SÍ llevan precio. Aplicados en `drip-templates.test.ts` (D+1 y D+7 sin precios; D+2 y D+14 pinneados contra `TIER_CONFIG.pro.monthlyPriceClp` y sin ningún otro número con forma de precio) y en el nuevo `transactional-templates.test.ts`. **Hallazgo reportado, sin cambiar**: `buildTrialExpiryWarningEmail` y `buildTrialExpiredEmail` SÍ imprimen precio, pero catálogo-driven (`api/cron/trial-expiry/route.ts` les pasa `recConfig.monthlyPriceClp`) ⇒ excepción declarada y pinneada con `assertOnlyCatalogPrice`, no prohibida.
- [x] W2.4 `lib/email/transactional-templates.ts` (`buildFreeCoachWelcomeEmail`): el `subscriptionUrl` que se pasaba desde siempre y el cuerpo nunca renderizaba ahora vive en el bloque «Cómo funciona EVA» («Tu cuenta, tu plan y tu facturación se administran desde eva-app.cl con tu mismo correo y contraseña. Desde la app del teléfono ves tu estado; los cambios de plan se hacen en la web.»), sin precio. Tests en `transactional-templates.test.ts`.
- [x] W2.5 `api/mobile/auth/complete-coach-onboarding/route.ts`: `await sendFreeCoachOnboardingEmails({ admin: adminDb, coachId: user.id, email, coachName, brandName, inviteCode, appUrl })` justo antes del 200, en `try/catch` que loguea sin PII y NO rompe la respuesta. Cierra la causa (4) del diagnóstico W2.8. Nuevo `route.test.ts` (6 casos: 401 sin bearer, alta OK con los argumentos exactos, fallo de correo ⇒ sigue 200, re-entrada `alreadyOnboarded` sin reenvío, insert fallido ⇒ 500 sin correo, payload inválido ⇒ 400 sin correo).

**Gates W2-A (21-08, ejecutados)**: `pnpm exec vitest run apps/web/src/lib/email apps/web/src/lib/auth apps/web/src/app/api/mobile/auth "apps/web/src/app/(auth)"` = **19 archivos / 227 tests verdes** · `pnpm --filter @eva/web exec tsc --noEmit -p tsconfig.json` verde · `eslint` de los 14 archivos tocados = 0 errores · `pnpm docs:check` verde. Efecto colateral declarado: `pnpm exec` disparó el chequeo de dependencias de pnpm y materializó en `pnpm-lock.yaml` el `packages/onboarding` que la sesión de onboarding v2 agregó a los `package.json` (cambio ajeno, NO revertido).

- [x] W2.6 Ledger local del drip — **ejecutado con la decisión del owner de §Coordinación: tabla nueva, NO `admin_audit_logs`** (esa tabla es append-only compartida: no admite el UPDATE de estado del webhook ni un UNIQUE sobre el id del proveedor). El `providerMessageId` de cada correo agendado queda en `coach_email_ledger.provider_message_id` vía `scheduleCoachEmail`. Migración `supabase/migrations/20260822004243_coach_email_ledger.sql` (**sin aplicar**: la aplica el jefe con el protocolo de AGENTS).
- [ ] W2.7 Operación: purgar en el dashboard de Resend la cola viva del drip viejo (coaches de las últimas 2 semanas). Lista de ids en el PR.
- [x] W2.8 **Diagnóstico (read-only, 21-08) — la premisa estaba invertida: el drip NO murió, murió el LEDGER.** `23f3f015` (2026-04-26, «remove unused components») borró `api/internal/email-drip/run/route.ts`, el ÚNICO escritor de `coach_email_drip_events` (drip v1, cron GH `email-drip.yml`, borrado a su vez en `1da8d3bb` 25-07 tras 3 meses de 404). El drip v2 (`98469778`, 10-05: `send-drip-sequence.ts` con `scheduled_at` de Resend) nunca escribió la tabla ⇒ las 12 filas miden abril, nada más. Resend (listado solo hasta 24-07) prueba entregas: diegishii y javi 3/3 `delivered` (jul-ago), ~20 coaches del 19-21/08 con bienvenida `delivered` + 3 `scheduled`, 0 bounces. **Causas reales de coaches sin nurture**: (1) Google web sin correo hasta `56159d64` 17-08 → 4 altas free (01-05→16-06) con cero correos; (2) fire-and-forget hasta `0be1130e` 20-08 → pérdidas sueltas (n.pueblacoach, vildanalfaro, eltiokevin1); (3) ventana 13→17-05 sin correo de confirmación (`e8cf0c37`); (4) **ABIERTO HOY**: Google móvil (`api/mobile/auth/complete-coach-onboarding/route.ts:145`) = W2.5. `coach_email_drip_events` queda como fósil (CHECK `scheduled_day IN (1,3,7,14)` y `status IN (sent,failed,skipped)` incompatibles con el drip nuevo).
- [x] W2.9 Ledger del drip + webhook de Resend — EJECUTADO 21-08. Decisión resuelta: **tabla nueva** (ni `coach_email_drip_events`, fósil con CHECKs incompatibles, ni `coach_onboarding_events`, que es de producto y no de correo). Archivos: migración `supabase/migrations/20260822004243_coach_email_ledger.sql` · `infrastructure/db/coach-email-ledger.repository.ts` · `services/email/coach-email-ledger.service.ts` (`scheduleCoachEmail` / `cancelCoachEmails` / `DRIP_SALES_KEYS` / `resendCancelUrl`) · `lib/email/resend-webhook-signature.ts` (firma Svix HMAC-SHA256 sin dependencia nueva, tolerancia ±5 min) · `app/api/webhooks/resend/route.ts` (503 sin secreto, 401 firma inválida, 500 solo si la DB falla, 200 en el resto). Eventos suscritos: `email.sent/delivered/bounced/complained/delivery_delayed` (`email.cancelled` **no existe** en Resend — el estado `cancelled` lo escribe `cancelCoachEmails` con la respuesta del propio endpoint de cancelación). **Desviación deliberada del patrón `cap-nudge`: el ledger acá es FAIL-OPEN**, no fail-closed — el barrido diario justifica cerrar la puerta, un disparo puntual no (peor caso: un correo repetido; el otro peor caso era perder la bienvenida de un coach). Pendiente operativo: `OPS-RESEND-01` en `MANUAL_TASKS.md` (crear el webhook y pegar `RESEND_WEBHOOK_SECRET` en Vercel) y **agregar `RESEND_WEBHOOK_SECRET` a `.env.example`** (el archivo estaba tomado por otra sesión).
- [x] W2.10 **Cancelar agendados** — `cancelCoachEmails(admin, coachId, ['day2_pro', 'day14_last_call'])` en `lib/payments/webhook-pipeline.ts`, en la rama canónica, tras el UPDATE del coach y solo si el status final es `active`/`trialing` con tier ≠ free, dentro de `try/catch` (un correo jamás puede tumbar un webhook de pago: MP no reintentaría). Endpoint verificado en la doc: **`POST https://api.resend.com/emails/:id/cancel`**, no el `DELETE /emails/:id` que decía esta tarea. Tests en `api/payments/webhook/route.test.ts` (cancela con las 2 keys · pago pendiente no cancela · cancelación que explota deja el 200). **HECHO salvo el hook «primer alumno»**: la rama del webhook de pagos está cerrada y con tests. Queda ÚNICO pendiente el hook «primer alumno cargado» (`coach/clients/_actions/clients.actions.ts` y los 3 call sites móviles/import), que sigue TOMADO por la sesión de onboarding v2 — y el de «baja de cuenta» (`cancelCoachEmails(admin, coachId, '*')`), que ya tiene el tope de lote de M-3 listo para recibirlo. El dedupe atómico que faltaba ya está EN LIVE: índice único parcial `coach_email_ledger_dedupe_uidx` sobre `(coach_id, template_key) where status <> 'failed'`, migración `supabase/migrations/20260822005701_coach_email_ledger_dedupe_uidx.sql` (la aplicó el jefe).
- [x] W2.11 `.env.example` (raíz — no existe uno en `apps/web`) §Email: `RESEND_FREE_COACH_AUDIENCE_ID=` y `RESEND_WEBHOOK_SECRET=`, cada uno con su comentario de una línea (comentados, como el resto de la sección). **Pendiente operativo**: verificar en Vercel prod que `RESEND_FREE_COACH_AUDIENCE_ID` esté seteada; si falta, el alta a la audiencia se salta en silencio (`send-email.ts:59`).
- [x] W2.12 **Ronda de corrección tras revisión adversarial (21-08)** — sin bloqueantes; 7 importantes + 11 menores, todos aplicados o declarados:
  - **I-1 / I-2** `ACTIVE_LEDGER_STATUSES` pasa a ser TODO menos `failed` (suma `bounced`, `complained`, `cancelled`). `bounced`/`complained` = a esa dirección no se le vuelve a escribir con esa key nunca (dominio de envío + Ley 19.496 art. 28 B); `cancelled` = «decidimos que ya no aplica» (pagó, se dio de baja), no «no salió». Es el ESPEJO del índice único parcial en LIVE — si se cambia la lista, se cambia el índice. Escotilla nueva `force?: boolean` en `ScheduleCoachEmailInput`: saltea la LECTURA del dedupe, no el índice; documentada y sin uso hoy.
  - **I-3** `cancelCoachEmails`: un `404`/`422` de Resend es TERMINAL, no un fallo — el correo ya salió. La fila se cierra como `sent` con `payload.cancel_not_possible = <status>` (merge del payload, vía `markCancelNotPossible`) y cuenta en el contador nuevo `alreadySent`; el resultado pasa a `{ cancelled, alreadySent, failed }`. `listScheduledByCoach` filtra además `scheduled_at > now()` y ordena por fecha: lo vencido no es cancelable y gastaba un POST por correo ya enviado.
  - **I-4** `scheduleFreeCoachDripSequence` deja de devolver `void`: `{ scheduled, deduped, failed, failures: [{ key, error }] }`, sin PII (la key y el `reason`, nunca el email). `sendFreeCoachOnboardingEmails` loguea una línea `warn` SIEMPRE y un `console.error('[onboarding-emails] drip', { coachId, …resumen })` cuando `failed > 0`. Sin esto las 4 podían caerse dentro del `allSettled` (el ledger no lanza: devuelve `{ ok: false }`) y el alta se veía impecable.
  - **I-6** `buildFreeCoachWelcomeEmail`: «hasta 25 alumnos» cableado → `studentCountLabel(TIER_CONFIG.pro.maxClients)`, con test que lo pinnea contra el catálogo. Se borró «tarda menos de 2 minutos» (promesa de tiempo que el checkout no cumple).
  - **I-7** Carrera de dedupe: si el INSERT choca con `23505` (otra ejecución ganó — doble clic en el link de confirmación ⇒ dos GET a `/auth/confirm`), el service devuelve `{ ok: true, deduped: true, ledgerId: null }`, loguea `warn` y, si el correo estaba AGENDADO, lo retira de Resend con el mismo endpoint de cancelación. Para poder distinguirlo, el repository ahora lanza `CoachEmailLedgerDbError` con el `code` de Postgres preservado (antes `new Error(message)` lo tiraba).
  - **M-1** `email.delivery_delayed` pasa `onlyFromStatuses: [row.status]`: ese evento reescribe `status` con su valor viejo, y sin guard un `bounced` que entra entre el SELECT y el UPDATE quedaba revertido a `sent` por una anotación de demora.
  - **M-3** `cancelCoachEmails` procesa como máximo `CANCEL_BATCH_LIMIT = 50` filas por llamada y loguea cuántas quedaron. Importa con `'*'` (baja de cuenta): cada fila es un POST a Resend, en medio de un webhook de pago.
  - **M-4 / M-5** `buildSubscriptionUrl` se mudó a `lib/email/subscription-url.ts` junto con `siteBaseUrl()`; `services/billing/sales-emails.service.ts` la re-exporta (cero cambios para `api/cron/paid-expiry` ni para su suite). El fallback de baseUrl del drip pasa de `http://localhost:3000` a `https://www.eva-app.cl`: un D+14 agendado con links a localhost es un correo perdido.
  - **M-6** UTM en el CTA del D+7 (`utm_campaign=day7_nutrition`). El link de invitación del D+1 queda SIN UTM a propósito y comentado en el código: el coach lo reenvía a sus alumnos, y etiquetarlo atribuiría al drip altas que no son del coach.
  - **M-7** Sin `inviteCode`, el D+1 cambia también asunto («{coach}, tu app ya está lista») y preview («Invita a tu primer alumno desde Alumnos.»): el cuerpo manda a Alumnos, así que prometer «tu link ya está listo» era una promesa que el correo no cumple.
  - **M-8** Tests nuevos de los call sites del helper: `app/auth/confirm/route.test.ts` (9 casos: argumentos exactos con `admin`/`coachId`/`inviteCode`, `await` observado con promesa diferida, helper que rechaza ⇒ igual redirige, y las 5 puertas que NO mandan nada) y `app/coach/onboarding/complete/_actions/complete.actions.test.ts` (6 casos, incluido el ORDEN `['emails', 'redirect']`). Los dos call sites ganaron el `try/catch` que ya tenía el móvil: la cuenta ya está escrita, un correo caído no puede devolver 500 ni «Error al crear tu perfil».
  - **M-8bis** Preview de la bienvenida: «Empieza gratis con tus primeros alumnos» → «con tu primer alumno» (Free = 1 desde Pricing v3).
  - **M-9** El webhook maneja `email.failed` (→ `failed` + `payload.error`) y `email.suppressed` (→ `failed` + `payload.suppressed = true`). Los dos sacan la fila del dedupe ⇒ reintento posible, y por eso llevan guard `onlyFromStatuses: ['scheduled','sent','failed']`: no pueden pisar un `bounced`/`complained`/`delivered` y reabrir el dedupe de una dirección quemada. RUNBOOK y `OPS-RESEND-01` actualizados a **7 eventos**.
  - **M-10** Test del pipeline de pagos para la otra mitad del guard (`finalTier !== 'free'`). **Hallazgo al escribirlo**: `finalTier === 'free' && finalStatus === 'active'` es INALCANZABLE — `TIER_ALLOWED_BILLING_CYCLES.free` está vacío (`packages/tiers/index.ts:445`), así que un `approved` que resuelve a free cae a `pending_payment`. Se cubren los dos casos reales: aprobado→free (queda `pending_payment`) y `trialing`+free (paid-like con tier free), ninguno cancela.
  - **M-11** Test de `sendFreeCoachOnboardingEmails` con las 4 del drip devolviendo `{ ok: false, reason: 'send_failed' }` (el modo de fallo real: no rechaza) ⇒ `failed: 4` y `console.error` con el resumen.
  - **M-2 declarado sin cambio**: el webhook de Resend no lleva rate limit propio. Su puerta es la firma Svix con ventana de ±5 min (fail-closed sin secreto), que ya acota el abuso; un limitador por IP delante de un proveedor que reintenta en ráfaga haría perder eventos legítimos.
  - **Copy**: «¿Se te atasca algo?» → «¿Te trabas en algo?»; «Hasta acá llega la serie, {coach}» → «La serie llega hasta acá, {coach}»; fallback de `brandName` null pasa de «tu marca» a «tu app» (el coach sin marca ya tiene app, no marca). Sigue habiendo 1 solo link por correo en los cuatro.

## W3 — El destino que se pierde: `?next=` (1 Opus) — EJECUTADA 21-08 (código en árbol, sin commit)
Gates corridos (ronda de corrección incluida, 21-08): `pnpm exec vitest run apps/web/src/lib/auth "apps/web/src/app/(auth)" apps/web/src/app/admin apps/web/src/app/auth apps/web/src/components/auth` = **13 archivos / 119 tests verdes** (el conteo «8/84» de la primera pasada estaba mal: ese comando barre todo `app/admin`, no solo `admin/login`); `pnpm --filter @eva/web exec tsc --noEmit -p tsconfig.json` verde; `eslint` de los archivos tocados = **0 errores** (2 warnings preexistentes en `proxy.ts:866/1174`, ajenas al diff); `pnpm docs:check` verde. Falta W3.6 (verificación end-to-end en preview).

Estado previo (mapa read-only): el proxy mandaba a `/login` **conservando el query original** (clona `nextUrl` y solo cambia `pathname`, `proxy.ts:426-431`) ⇒ el `$pageview` de `/login?utm_source=cap_email…` SÍ llega a PostHog (`PageviewTracker` global, `lib/posthog/provider.tsx:58-70`). Lo que se pierde es el **destino**: `loginAction` (`(auth)/login/_actions/login.actions.ts:20-86`) no lee `next` y siempre hace `redirect(resolvePostLoginRedirect())` → dashboard. `/coach/subscription/page.tsx` no tiene lógica de `utm_` ni la necesita.
- [x] W3.1 Helper compartido `lib/auth/safe-next.ts`: `safeNext(raw: unknown, allowedPrefix: '/coach' | '/admin' | '/')` + test `safe-next.test.ts` (26 casos, escritos primero). **Eran 5 copias inline, no 3**: a las del mapa (`admin/login/_actions/login.actions.ts:17-22`, `lib/auth/post-google-auth.ts:33`, `app/auth/callback/route.ts:35`) se sumaban `admin/login/page.tsx:21` y `admin/login/AdminLoginForm.tsx:18` con el mismo `startsWith('/admin')`; las 5 migradas. Endurecido respecto de lo que había (sin cambiar la semántica del camino feliz): `\`, `://`, control chars y espacios, `..` y separadores codificados (`%2f`/`%5c`/`%2e`/`%00`) en el path, `javascript:`/`data:`, tope `MAX_NEXT_LENGTH` (ver W3.7), y prefijo exacto (antes `/adminx` pasaba). El query/hash viaja intacto: no se re-encodea nada.
- [x] W3.2 `proxy.ts` bloque `/coach` sin sesión: `redirectUrl.search = ''` + `next` armado por `buildCoachLoginNext(pathname, request.nextUrl.search)` (mismo criterio que `/admin`: nada para `/coach` ni `/coach/dashboard`). La función vive en el helper y **pasa por `safeNext`**, así que lo que escribe el proxy es exactamente lo que el login puede consumir. `proxy.ts` sigue sin test propio (15+ imports: `@supabase/ssr`, service-role, Edge Config, rate-limit ⇒ mock monstruoso); se cubrió con el test de la función pura extraída, mismo patrón que `lib/http/prefetch-request` y `lib/brand-header-codec`. **Extra fuera del mapa**: el bloque 4 (`isAuthPage && user`) ahora también honra `?next=` cuando un coach CON sesión cae en `/login?next=/coach/...`; antes lo mandaba al dashboard con el `?next=` pegado como query.
- [x] W3.3 `login/page.tsx` (`searchParams: { error?, next? }` → `safeNext(params.next, '/coach')` → prop `next`), `CoachLoginForm.tsx` (`fd.set('next', next)` + `<GoogleSignInButton intent="login" next={next} />`), `login.actions.ts` (`safeNext(formData.get('next'), '/coach')`, `redirectPath = next ?? await resolvePostLoginRedirect(...)`, `revalidatePath` con la ruta sin query). Tests nuevos en `actions.test.ts`: next válido con `utm_*` → redirige ahí y no consulta el default; 5 next maliciosos → default.
- [x] W3.4 Google: `GoogleSignInButton` gana prop `next` (se pasa a `resolvePostGoogleAuthUrl` y a `FallbackButton`) y `startCoachGoogleLogin(next?)` arma `redirectTo` con `encodeURIComponent(next ?? '/coach/dashboard')`. En `register` no se threadea (queda `null`). ⚠ El `redirectTo` pasa de `?next=/coach/dashboard` a `?next=%2Fcoach%2F…`: verificar en Supabase Auth que la allowlist de Redirect URLs sea por patrón (`https://www.eva-app.cl/**`) y no una URL literal.
- [x] W3.5 Declarado, sin cambios: los ~25 `redirect('/login')` pelados en páginas `/coach/**` son defensa en profundidad detrás del proxy (que corta antes y ya arma el `next`); no se les agregó `?next=`.
- [x] W3.7 **Ronda de corrección tras revisión adversarial (21-08)** — 2 IMPORTANTES + 6 menores, ningún bloqueante:
  - **I2** `safe-next.ts`: el filtro dejaba pasar todo lo ≥ 0x80, así que `/coach/x<U+2028>y` o un `аdmin` con cirílica llegaban al `redirect()` del Server Action → `x-action-redirect` solo admite ByteString → TypeError → **500 justo al terminar el login**. Ahora se rechaza todo `charCodeAt > 126` (`hasControlOrSpace` → `hasUnsafeChar`); lo no-ASCII legítimo viaja percent-encoded y sigue pasando (`/coach/%E2%80%A8y`).
  - **I1** `proxy.ts` bloque `/coach` sin sesión: `redirectUrl.search = ''` borraba los `utm_*` y el `$pageview` de `/login` quedaba sin atribución (el coach que abre el correo, ve el login y se va era invisible). Los `utm_*` se recopian a nivel top con `pickUtmParams(search)` (función pura nueva en el helper, con test): el `next` lleva el query completo para el aterrizaje, los duplicados de arriba son para el `$pageview` del login.
  - **M1** `COACH_LOGIN_DEFAULT_PATHS` suma `/coach/onboarding/complete`: el proxy saltea los guards ahí, y un `next` a esa ruta dejaba a un coach existente encerrado en el formulario de alta OAuth.
  - **M2** `buildCoachLoginNext` saca el `_rsc` que pegan las navegaciones RSC antes de armar el `next` (se filtra sobre el texto crudo para no re-encodear los `utm_*`).
  - **M5** Los chequeos de `://`, `javascript:` y `data:` pasan a mirar el **path**, no el raw entero: `/coach/x?title=Big%20Data:%20intro` caía a `null` en silencio. `//` y `\` se siguen mirando sobre el raw completo.
  - **M6** `MAX_NEXT_LENGTH` 2048 → **1024**, el tope real de `revalidatePath` (el paso previo al `redirect()` del login).
  - **M3 + M4** `app/auth/callback/route.ts` validaba solo la rama SIN `code`; ahora el `next` pasa por `safeNext` una vez, antes de las dos ramas. Y el prefijo deja de ser `'/'` (cualquier ruta interna, `/api/**` incluido): `safeNext` acepta lista de prefijos y se usa `AUTH_CALLBACK_NEXT_PREFIXES = ['/reset-password', '/coach']`, la allowlist exacta de los emisores que existen — `/reset-password[?team_slug&coach_slug]` (`(auth)/forgot-password/_actions/forgot-password.actions.ts:47`) y `/coach/**` con default `/coach/dashboard` (`lib/auth/client-oauth.ts:15` y `GoogleSignInButton.tsx:103`). Consumidores: `app/auth/exchange/AuthExchangeClient.tsx:36` y `GoogleSignInButton.tsx:103` vía `resolvePostGoogleAuthUrl`. No hay emisor con prefijo de alumno (`/c/**`) ni ningún otro.
  - **M7** `post-google-auth.ts`: el fallback `no_google_account` era **inalcanzable** con un `next` (el early return devolvía el destino antes del lookup, y los dos caminos de Google mandan `next` SIEMPRE), así que aplicarlo tal cual habría sido código muerto. Se acotó: solo un destino **explícito** bajo `/coach/**` (≠ `/coach`, `/coach/dashboard`, `/coach/onboarding/complete` — `isCoachDefaultLanding`, helper nuevo con test) espera al chequeo de la fila `coaches`; sin fila, el fallback conserva el destino (`/login?error=no_google_account&next=<encoded>`) y el coach reintenta con contraseña sin perderlo. **Deliberadamente NO se tocó** el caso `next = /coach/dashboard` (el default implícito de los dos callers): ahí sigue ganando el early return, así que el registro con Google desde `/login` de alguien sin fila `coaches` sigue derivando al alta OAuth como hoy, en vez de comerse un «No encontramos una cuenta con ese correo de Google».
  - **M8** `admin/login/AdminLoginForm.tsx` recupera su defensa en profundidad: `safeNext(next, '/admin')` en el componente, no solo en la page.
  - Tests nuevos en `safe-next.test.ts`: U+2028/U+2029/cirílico/U+3000 rechazados, `%E2%80%A8` aceptado, `%0d%0aSet-Cookie` y `%252e%252e/` aceptados (siguen encoded), `/coach//evil.com` + `?x=//evil` + `#//evil` aceptados con `new URL(r, 'https://www.eva-app.cl').origin` verificado, `_rsc`, `/coach/onboarding/complete`, lista de prefijos y `pickUtmParams`.
- [ ] W3.6 **Parcial 22-08 00:3xZ en prod (`866bea6d` dentro de `ee619738`)**: `curl` sin sesión a `/coach/subscription?utm_source=cap_email&utm_medium=email&utm_campaign=sweep` → `307 /login?next=%2Fcoach%2Fsubscription%3Futm…&utm_source=cap_email&utm_medium=email&utm_campaign=sweep` ✓; `/coach/dashboard` → `/login` sin `next` ✓; `/login?next=//evil.com` → 200 sin redirect ✓. **Falta (owner): login con Google por el camino fallback en prod.** El correo de cupo sigue apuntando a `/coach/subscription?utm…` directo: con sesión entra; sin sesión el proxy arma el `next` solo. Verificar end-to-end en preview: correo → sin sesión → login (password **y** Google) → aterriza en `/coach/subscription` con los `utm_*` intactos → `$pageview` y `checkout_started` los llevan. Incluir en la corrida la allowlist de Redirect URLs de Supabase (ver W3.4).

## W4 — El alta móvil (1 Opus)
- [ ] W4.1 `api/mobile/auth/register-coach-free/route.ts:185`: devolver `uid` en la respuesta.
- [ ] W4.2 Nuevo `api/mobile/auth/resend-confirmation/route.ts` con los 7 guards de `(auth)/verify-email/resend.actions.ts` (leer entero): identidad por `uid`, `pending_email`, rate-limit, nunca `generateLink` con email suelto.
- [ ] W4.3 `apps/mobile/lib/api.ts:138` + `app/(auth)/verify-email.tsx:66`: botón «Reenviar correo» con cooldown visible.
- [ ] W4.4 Tests del endpoint (uid inexistente, ya confirmado, rate-limit, email ≠ pending).

## W5 — Compliance y verdad en `packages/tiers` — un solo OTA (1 Opus web + 1 Opus RN)
- [ ] W5.1 `packages/tiers/index.ts:430` (`getEvaBadgeUrl`): el sello «Hecho con EVA» aterriza en una landing SIN precios (`/hecho-con-eva` o `/?seccion=...` sin `PreciosSection`). Tests en rojo primero: `pricing-v3.test.ts:78-92`, `email-brand.test.ts:59,68,74`.
- [ ] W5.2 `apps/mobile/components/coach/directory/CreateClientModal.tsx:292`: `studentCountLabel(upgradeLimit)` (hoy «1 alumnos»).
- [ ] W5.3 `apps/mobile/app/coach/(tabs)/perfil.tsx:30-34`: borrar el `TIER_LABELS` local (sin `free`) y usar el de `lib/coach-subscription.ts:40` (`@eva/tiers`).
- [ ] W5.4 `apps/web/src/app/coach/subscription/_components/SubscriptionContent.tsx:791`: con Anual seleccionado, «$287.904 /mes» → «/año» (o precio mensual equivalente con etiqueta correcta).
- [ ] W5.5 Test `apps/mobile` no consume `monthlyPriceClp` (patrón `readFileSync` sobre `apps/mobile/**`) y `lib/coach-tiers.ts:27` deja de re-exportar `TIER_CONFIG` entero (exportar solo `maxClients`/labels; `CoachDashboardSections.tsx:970` usa `TIER_CONFIG.free.maxClients`).

## W6 — La app dice la verdad y lo hace visual (2 Opus RN + 1 Opus web; mismo OTA que W5)
- [ ] W6.1 `apps/mobile/app/coach/(tabs)/subscription.tsx:153`: `hasPaidPlan` deja de excluir `free` (el server da los 4 módulos a todo plan activo desde Pricing v2: espejo de `hasPaidModuleAccess`); `addonBadge` (`:65-72`) y pie (`:270-274`) coherentes. `apps/mobile/app/coach/modules.tsx:209`: el texto «En el plan Free los módulos no están disponibles» se elimina (es falso y lo ve un Pro).
- [ ] W6.2 Muro de cupo rediseñado en `CreateClientModal.tsx:284-303`: ícono en círculo (ya existe), título «Alcanzaste el cupo de tu plan», cuerpo «Tu plan actual permite {studentCountLabel(n)} activo(s). Para dejar espacio puedes archivar un alumno: su historial se mantiene intacto.», **[Archivar un alumno]** (abre selector con `archiveClient` de `lib/client-actions.ts:53` / patrón de `ReactivateArchivePanel.tsx`) · **[Actualizar estado]** (`RefreshPlanButton`) · «Entendido». Android-only (`Platform.OS === 'android'`): caption «Los cambios de plan se hacen en eva-app.cl» (texto plano, sin `Linking`). Test de contrato: el string NO aparece en iOS.
- [ ] W6.3 ⚠ Re-verificar líneas antes de tocar: la sesión DOMINGO editó `CoachDashboardSections.tsx` el 21-08 (`:563/:573` modal Free sin «Marca personalizada ✗», `:1320` checklist «Personaliza tu marca» → `brand`), sin tocar el banner. Medidor de cupo: `MobileFreeTierBanner` (`CoachDashboardSections.tsx:252-293`) pasa a usar `components/ProgressBar.tsx` (hoy barra inline) con color semántico (marca <80 %, ámbar ≥80 %, `WARNING_500` al 100 %) y el mismo medidor entra en `subscription.tsx:161-165` junto a «Alumnos activos · x de N» (`ProgressRing` como anillo en la card inverse). Sin tiers ajenos.
- [ ] W6.4 Mi plan = verdad: `subscription.tsx` gana `RefreshControl` (pull-to-refresh reusa `RefreshPlanButton.handleRefresh`) y «Actualizado hace X»; **celebración**: si tras refrescar `maxClients` sube o `tier` cambia de `free` a pago, tarjeta «Tu cupo subió a {studentCountLabel(n)}» con micro-animación motion-safe (Moti/Reanimated, `useReducedMotion`) y toast. Es estado, no venta.
- [ ] W6.5 `subscription.tsx:277-279`: «¿Dudas con tu cuenta? Escríbenos a contacto@eva-app.cl» tocable (`Linking.openURL('mailto:…')`).
- [ ] W6.6 Tono: `ProgresoTab.tsx:832` «Mejorar mi plan» → «Ver mi plan» (sigue a `/coach/modules`); `BuilderDayStrip.tsx:486,490` ídem; `verify-email.tsx:16` «Upgrade cuando quieras» → «Cambia de plan cuando quieras desde eva-app.cl» SOLO Android / «Cambia de plan cuando quieras» en iOS.
- [ ] W6.7 Web: página de éxito del checkout (`/coach/subscription/processing` o su destino) gana «Abrir EVA en el teléfono» (universal link `https://www.eva-app.cl/coach/subscription`, ya soportado por `applinks` desde 1.1.2) + texto «toca Actualizar estado si no ves el cambio».
- [ ] W6.8 QA device del owner: iOS + Android × light/dark × marca EVA/custom: muro, medidor, Mi plan, celebración (simular con cuenta QA subiendo `max_clients` en LIVE).

## W7 — Blindar la posición y poder medir (1 Sonnet + jefe)
- [ ] W7.1 `api/mobile/auth/register-coach-free/route.ts` y `complete-coach-onboarding/route.ts`: `coach_registered` lleva `platform: 'ios' | 'android' | 'web'` (header `x-eva-platform` o `User-Agent` de Expo) + `pricing_version`. **Hallazgo DOMINGO 21-08**: el 29 % de las altas nuevas no emite `register_submitted`/`coach_registered` — el camino Google OAuth se lo come; cerrar ese hueco en la misma tarea (coordinar: DOMINGO edita `analytics.ts`/`register.actions` hoy).
- [ ] W7.2 `apps/mobile/AGENTS.md`: sección «Pagos y tiendas — permitido/prohibido» (iOS solo estado; Android una línea; correo/WhatsApp libre; `Platform.OS`, nunca storefront). Reemplaza como regla vigente al research del 31-07.
- [ ] W7.3 `docs/operations/APP_REVIEW_NOTES.md`: Notes for Review en inglés: modelo declarado, 3.1.3(f) por nombre, 3 cuentas demo, cierre «if any screen appears non-compliant, tell us which and we will change it in this submission». NO citar 3.1.3(b) ni (c).
- [ ] W7.4 App Store Connect (owner): «Sitio web del desarrollador» → landing que convierte; copy de la ficha como herramienta de trabajo invitada.

## W8 — Activación (decisión del owner; fuera del alcance pedido)
- [ ] W8.0 Decidir alcance: onboarding que no termina sin el primer alumno · D+1 con link de invitación listo · `/join` adelante · estados vacíos del funnel (artifact `04324b08`).

## Coordinación con «Onboarding coach v2» (`docs/specs/coach-onboarding-v2/`, sesión DOMINGO, 21-08)
- `CreateClientModal.tsx` RN: el embudo va PRIMERO (W5.2/W6.2 muro de cupo); el stepper de alta de onboarding v2 se monta después y conserva el estado (C) «límite» tal cual.
- `clients.is_demo`: onboarding v2 lo excluye de TODOS los conteos de cupo, incluido `api/cron/cap-nudge/route.ts` (`countActiveClients` + test) y los 4 call sites de clients/import (OK dado; después del commit de W3).
- `CoachDashboardSections.tsx`: quien toque primero avisa; el medidor W6.3 se conserva dentro del rediseño del home día 1.
- Ledger de correos (W2.9): compartido. Los «correos por comportamiento» de onboarding v2 entran por `scheduleCoachEmail`/`cancelCoachEmails` con `trigger: 'behavior'` y dedupe por `(coach_id, template_key)`. **Decisión del owner (21-08): TABLA NUEVA `coach_email_ledger`** (DDL aditiva, protocolo Supabase de AGENTS: tx-rollback de prueba + advisors antes de `apply_migration`; RLS solo lectura del coach dueño; escritura solo service_role; `provider_message_id` UNIQUE; índice `(coach_id, template_key, created_at desc)`; grants explícitos). Shape y helpers en W2.9.

## Gates (por wave; suite completa una vez antes del push)
`pnpm lint` · `pnpm typecheck` · `pnpm test` · `pnpm build` · `pnpm docs:check` · `pnpm check:tokens` · RN: `pnpm --filter @eva/mobile exec tsc --noEmit` + `expo export --platform android`.

## Entrega
- Sin commit/push/deploy/OTA sin pedido explícito del owner.
- OTA de W5+W6: tres runtimes (1.1.0/1.1.1/1.1.2), android e ios por separado, desde rama con master mergeado. ⚠ 21-08: la sesión DOMINGO prepara un binario **1.1.3** (fix nativo del crash Android: `expo-system-ui@57` vs SDK 54) ⇒ al llegar W5/W6 verificar la partición vigente en `MOBILE_RELEASES_OTA.md`; probablemente 4 runtimes.
