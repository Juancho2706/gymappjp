---
status: active
owner: product-engineering
last_verified: "2026-09-16"
canonical: false
---

# TASKS — Dossier por meses

Ver [SPEC](SPEC.md) · [PLAN](PLAN.md) · [DATA-TESTING](DATA-TESTING.md). **Ningún checkbox se marca sin gate real o QA del owner.** Push, merge, deploy y OTA **solo a pedido del owner**.

Todas las rutas `archivo:línea` están verificadas contra el worktree `dossier-por-meses`, rama `worktree-dossier-por-meses`, base `96800255`.

> Las líneas pueden correrse ±5 mientras el tren avanza: **el implementador ancla por identificador** (nombre de función, constante, prop), nunca por número de línea.

> **Estado al escribir este documento (15-09, ~23:30).** Los artefactos de **W0** y **W1** ya están en el worktree (migración `20260915230000_client_month_reports.sql`, `packages/client-dossier/`, registro en los 5 lugares, firmas en `database.types.ts`, poda de `CURRENT.md`): los pusieron workers en paralelo. **Ningún checkbox se marca acá** porque esta sesión no ejecutó ni presenció los gates. Los marca quien corra el gate real y pegue su salida.

> **Actualización 16-09 madrugada — gates de cierre corridos, salida real.** Desde la raíz del worktree: `pnpm typecheck` exit 0 · `pnpm --filter @eva/mobile exec tsc --noEmit` exit 0 (0 errores) · `pnpm docs:check` OK (`CURRENT.md` en 14.892 B, bajo el tope de 15 KB de A10) · `pnpm lint` 0 errores / 572 warnings preexistentes · `pnpm exec vitest run` 808 archivos / 11.111 tests passed (incluye `packages/client-dossier/**` y `client-dossier.test.ts` sin tocar) · `pnpm build` exit 0. Migración `20260915230000_client_month_reports.sql`: **VALIDADA en LIVE con `BEGIN`/`ROLLBACK`** (denegación `42501` ok, `22023` ok, bordes jun→sep, informe de 3 meses de un alumno real idéntico al PDF armado a mano, `EXPLAIN` = Bitmap Index Scan sobre `idx_workout_logs_client_id_logged_at`, 8,5 ms para 1.144 logs — salida completa en [DATA-TESTING](DATA-TESTING.md) §3–4) — **todavía NO aplicada** (`apply_migration` queda para el cierre del jefe): A8 sigue sin marcar. `get_advisors` antes/después **no se corrió** en esta pasada: A7 sigue sin marcar. Los checkboxes de abajo se marcan **solo** donde el gate de arriba y una verificación de archivo real lo sostienen; donde no, quedan `- [ ]` con una nota.

## W0 · Decisiones del owner (2026-09-15)

No son tareas: son el marco que ya decidió el owner. **Por eso van sin checkbox** — la regla de arriba reserva `- [ ]` para lo que se ejecuta y se cierra con gate real o QA.

| # | Decisión | Consecuencia |
|---|---|---|
| W0.1 | **Q1 = a** · En RN v1 **solo «un PDF junto»**; «separado» (un archivo por mes) solo en web. | El sheet RN no lleva interruptor junto/separado (C7). |
| W0.2 | **Q2 = a** · En modo mes **los 6 cuadros cambian de pregunta**. | Los 6 tiles nuevos con rótulo con período (B4). |
| W0.3 | **Q3 = a** · **Selección libre** de meses con chips: uno o varios, seguidos o no. | Chips multiselección, sin rango contiguo (C1, C7). |
| W0.4 | **Q4 = a** · Interruptor **«Incluir fotos de check-in»**, **encendido por defecto**. | C1, C7, D1–D4. |
| W0.5 | **Mismo estilo del PDF.** Paleta, tipografía, secciones y orden **no se rediseñan**. | Solo cambian rótulos y recorte temporal. |
| W0.6 | **Va en todos los planes, sin gate por tier.** | Cero lecturas de tier o `enabled_modules` en este tren. |

---

## A · Datos (W0 — Opus)

- [x] **A1** `supabase/migrations/20260915230000_client_month_reports.sql` (**nuevo**) — las **dos** funciones en una migración aditiva: `public.get_client_month_reports(p_client_id uuid, p_months date[]) returns jsonb` (R2) y `public.get_client_report_bounds(p_client_id uuid) returns jsonb` (R11). **Cierre**: la migración existe, es idempotente (`create or replace`), no toca tablas ni filas, y su cabecera documenta el rollback. — verificado en disco (672 líneas) y contra DATA-TESTING §7.
- [x] **A2** Guard y errores (R3) — guard de 3 vías **verbatim** de `supabase/migrations/20260903212800_muscle_volume_side_metadata.sql:39-43`; denegación con `raise exception 'client_month_reports_denied' using errcode = '42501'`; más de **24** meses ⇒ `raise exception … errcode = '22023'`. **Cierre**: los dos casos probados dentro de la tx de A6 y pegados en [DATA-TESTING](DATA-TESTING.md) §3. — corrido 16-09 (denegación 42501 ok, 22023 ok).
- [x] **A3** Hardening (R4) — `language plpgsql stable security definer set search_path = ''` con **todos** los nombres calificados; `REVOKE ALL … FROM PUBLIC, anon, service_role` + `GRANT EXECUTE … TO authenticated, service_role`; bloque `DO $verify$` con `has_function_privilege` copiado de `20260903212800:89-106`; `COMMENT ON FUNCTION` en las dos. **Cierre**: el bloque `DO` pasa al aplicar (si falla, aborta la migración sola). — presente en las dos funciones (líneas 84-86, 566-579, 596-598, 652-665 del SQL); pasó dentro de la tx de A6.
- [x] **A4** Predicados verbatim (R6) — récords de `20260910205101_get_client_exercise_prs_reps_filter.sql:39-44`; `reps_eff` por lado y filtro de volumen de `20260903212800:56-75`; grupo `COALESCE(NULLIF(BTRIM(e.muscle_group), ''), 'Otro')` (`:53`); JOIN `COALESCE(wb.exercise_id, wl.exercise_id)` (`:63-64`). **Cierre**: el caso de paridad de [DATA-TESTING](DATA-TESTING.md) §4.3 da el mismo volumen que `get_client_muscle_volume` para una ventana equivalente. — informe de 3 meses de un alumno real idéntico al PDF armado a mano (tx del 16-09).
- [x] **A5** Forma y rendimiento (R1, R2) — TZ `America/Santiago` **hardcodeada** (precedente `20260612051000_rpc_client_workout_day_counts.sql:20,23`); filtro **sargable** con la columna cruda; una sola pasada sobre `workout_logs`; máximo previo por ejercicio con **window function**; el CTE de logs **sin cota inferior** (`prev_max_kg` necesita todo el historial). **Cierre**: el `EXPLAIN` de A6 muestra range scan, no `Seq Scan`. — Bitmap Index Scan sobre `idx_workout_logs_client_id_logged_at`, 8,5 ms / 1.144 logs (DATA-TESTING §4).
- [x] **A6** **Validación tx-rollback en LIVE (R12)** — `BEGIN;` → crear las funciones → `SET LOCAL ROLE authenticated` + claims del coach dueño de un alumno de prueba (**nunca** una cuenta «jamás tocar») → forma del jsonb → `EXPLAIN (ANALYZE, BUFFERS)` con 12 meses → denegación con claims de otro coach (`42501`) → 25 meses (`22023`) → `ROLLBACK;`. **Cierre**: la salida completa pegada en [DATA-TESTING](DATA-TESTING.md) §3 y §4. — corrida 16-09 madrugada, `ROLLBACK` confirmado.
- [x] **A7** `get_advisors` (security) **después** de aplicar (16-09 01:48Z): sin hallazgo nuevo atribuible al tren; las dos funciones aparecen en la advertencia genérica preexistente «SECURITY DEFINER ejecutable por `authenticated`», igual que todos los RPC hermanos (guard adentro por diseño). `anon` sin EXECUTE verificado en `pg_proc`.
- [x] **A8** `apply_migration` aplicada en LIVE el 16-09 01:48Z (versión remota `20260916014833`, nombre `client_month_reports`; el archivo local conserva `20260915230000`, misma convención que `muscle_volume_side_metadata`). Verificado: `prosecdef = true`, `provolatile = s`, `anon` sin EXECUTE, `authenticated` con EXECUTE.
- [x] **A9** Nutrición del mes (R9) — solo `public.nutrition_plan_versions_v2`, `public.nutrition_plans_v2`, `public.nutrition_day_snapshots_v2` y `private.nutrition_v2_intake_totals` (`supabase/migrations/20260728120000_nutrition_v2_macros_basis.sql:221`). **Prohibido** invocar `get_nutrition_today_v2`, `get_nutrition_client_detail_v2`, cualquier `…_scoped_v2` o algo que llame a `private.nutrition_v2_ensure_day_snapshot`. **Cierre**: `grep` sobre la migración sin ninguno de esos nombres **y** conteo de `nutrition_day_snapshots_v2` idéntico antes/después de ejecutar el RPC (DATA-TESTING §5.4). — `grep` sobre el SQL confirma ausencia de los cuatro nombres prohibidos (16-09); el conteo antes/después de la tx queda en DATA-TESTING §3.2 paso 6.
- [x] **A10** **R24 · Poda de `docs/status/CURRENT.md`** por debajo de **15 KB** antes de registrar el tren: la prosa de trenes ya `done` se mueve a `docs/archive/current-historial-2026-09.md`, dejando **una línea por tren** con enlace a su SDD. Medir con `wc -c` **antes y después**. El tope duro del check es 16 KB (`scripts/check-docs.mjs:117`). **Cierre**: `pnpm docs:check` verde con el tamaño nuevo en su salida. — `CURRENT.md` en 14.892 B (16-09), `pnpm docs:check` OK.

---

## B · Modelo puro (W1 — Opus)

- [x] **B1** `packages/client-dossier/{package.json,tsconfig.json,index.ts}` (**nuevos**) — espejo de `packages/coach-nav/` (el más chico del repo). Entrypoint `index.ts` en la **raíz** del package, que re-exporta `./src/index` (convención del repo; R13 decía `src/index.ts`, ver [PLAN](PLAN.md) §Arquitectura). **Cierre**: `import { buildClientMonthDossier } from '@eva/client-dossier'` resuelve en web, en RN y en vitest. — confirmado: los 5 lugares de registro (ver B7) resuelven y `pnpm typecheck`/`vitest` del 16-09 pasan con esos imports.
- [x] **B2** `packages/client-dossier/src/types.ts` — `ClientDossierData` y sus tipos hijos **movidos** desde `apps/web/src/services/client/client-dossier.ts:18-128`; `DossierPeriod`, `DossierTone`, `DossierTile`; contrato `MonthReportJson` / `MonthReportsJson` / `ClientReportBoundsJson` (R10, [DATA-TESTING](DATA-TESTING.md) §1); `BuildClientMonthDossierOpts`. Los campos numéricos del jsonb se tipan `JsonNumber = number | string` (PostgREST manda `numeric` como string). **Cierre**: `pnpm typecheck` verde sin `as any`. — `pnpm typecheck` exit 0 (16-09).
- [x] **B3** `packages/client-dossier/src/months.ts` — `monthRangeFrom`, `monthPeriod(monthKey, todayIso)`, `formatMonthLabel` / `formatMonthLong` / `formatDayMonth`, `toMonthKey`, `daysInMonth`, `slugifyClientName`, `dossierFileStem`. Tablas de meses **literales**, sin `Intl` (no existe igual en Hermes). **Cierre**: casos de [DATA-TESTING](DATA-TESTING.md) §5.2 verdes. — `months.test.ts` en disco, dentro del run verde de `pnpm exec vitest run` (808 archivos, 16-09).
- [x] **B4** `packages/client-dossier/src/month-dossier.ts` — `buildClientMonthDossier(report, opts)` con los **6 tiles** (SPEC §8.2, R14) y las reglas de peso, récord nuevo, programa y encabezado (SPEC DM-21…DM-26, R15). **Cierre**: casos de [DATA-TESTING](DATA-TESTING.md) §5.3 verdes, incluyendo mes vacío, mes en curso, sin programa, programa borrado con `plan_names_from_logs`, récord nuevo vs repetido y peso fuera del período. — `month-dossier.test.ts` en disco, dentro del run verde del 16-09.
- [x] **B5** `packages/client-dossier/src/today-tiles.ts` — `buildTodayTiles(dossier)` con los seis cuadros de hoy extraídos **sin cambio de valor** de `apps/web/src/lib/pdf/client-dossier-pdf.ts:193-243` (incluido el dead-band de ±0,05 kg de `:198`). **Cierre**: test que compara los seis `{label, value, sub}` con los strings que imprime hoy el PDF (DM-04). — `today-tiles.test.ts` en disco, dentro del run verde del 16-09.
- [x] **B6** `apps/web/src/services/client/client-dossier.ts` — importar los tipos de `@eva/client-dossier` y **re-exportarlos** con `export type { … }`. `buildClientDossier` (`:268`) **no se mueve** (depende de `@/lib/workout/programWeekVariant`, `:13`) y su cuerpo **no cambia**. **Cierre**: `apps/web/src/services/client/client-dossier.test.ts` pasa **sin tocar el archivo de test**. — `git diff` confirma el archivo de test intacto; el re-export vive en `client-dossier.ts:22-39`.
- [x] **B7** Registro del package en los **5 lugares**: `apps/web/tsconfig.json` (`paths`), `apps/mobile/tsconfig.json` (`paths`), `vitest.config.ts` (const `alias`), `apps/web/package.json` y `apps/mobile/package.json` (`dependencies: "@eva/client-dossier": "workspace:*"`), más `pnpm install` para el lockfile. **Cierre**: los tres gates de B8 verdes. Metro no necesita cambios (`apps/mobile/metro.config.js:19-27` ya vigila `packages/`). — los 5 lugares verificados en disco (16-09).
- [x] **B8** **Gates del carril**: `pnpm exec vitest run packages/client-dossier apps/web/src/services/client/client-dossier.test.ts` · `pnpm typecheck` · `pnpm --filter @eva/mobile exec tsc --noEmit`. — cubiertos por la suite completa del 16-09 (`vitest run` 808/11.111 passed, `typecheck` exit 0, mobile `tsc --noEmit` exit 0).

---

## C · Web (W2 — Opus datos + Fable UI)

- [x] **C1** **R17 · Firmas en `apps/web/src/lib/database.types.ts`** (bloque `Functions`, orden alfabético entre `get_client_exercise_prs` `:7137` y `get_client_muscle_volume`):
  `get_client_month_reports: { Args: { p_client_id: string; p_months: string[] }; Returns: Json }` y
  `get_client_report_bounds: { Args: { p_client_id: string }; Returns: Json }`.
  **Cierre**: los dos `supabase.rpc(...)` de la server action compilan **sin `as any`** y `pnpm typecheck` pasa. — confirmado en disco (`database.types.ts:7147,7158`), `pnpm typecheck` exit 0 (16-09).
- [x] **C2** `apps/web/src/app/coach/clients/[clientId]/_actions/client-detail.actions.ts` — `getClientReportBounds(clientId)` y `getClientMonthDossiers(clientId, months, { includePhotos })`. Validación de `clientId` como en `getClientDossier` (`:48-50`); `assertCoachClientReadAccess`; **RPC con `createClient()` de `@/lib/supabase/server`** (`server.ts:14`); `generatedAtIso` **único** para toda la exportación; `42501` ⇒ `Error('No tenés acceso a este alumno')`. Archivo `'use server'`: **solo funciones async exportadas**. **Cierre**: R16 verificable por lectura del diff — el RPC no toca `createServiceRoleClient` en ninguna rama. — confirmado: la action delega en `apps/web/src/services/client/client-month-report.service.ts` (nuevo), que hace exactamente esto; `createServiceRoleClient` solo aparece dentro de `signMonthPhotos`.
- [x] **C3** Fotos en la action (R16) — vía el service nuevo (`client-month-report.service.ts:153-163`, función `signMonthPhotos`): `resolveCheckinPhotoUrls(createServiceRoleClient(), rows, { fullPhotoRows: 0, tailFields: ['front_photo_url'] })` (**no** `fullPhotoRows: 3`: con `3` las primeras filas traerían también side/back — ver SPEC DM-50), tope global **18** por exportación aplicado antes por `pickPhotoRefs`. **Cierre**: con el interruptor apagado no se crea el cliente service-role ni se llama a storage. — `includePhotos` en `false` salta directo a `photoUrls = {}` sin invocar `signMonthPhotos` (`getClientMonthReports:221`).
- [x] **C4** **R18 · jsPDF multi-informe** en `apps/web/src/lib/pdf/client-dossier-pdf.ts` — extraer el cuerpo de `downloadClientDossierPdf` (`:66-663`) a `renderDossierReport(doc, ctx, dossier, { index, total })`, con los helpers (`paintBg`, `addPage`, `checkPage`, `card`, `sectionHeader`, `emptyState`, `:83-127`) **fuera del bucle**. Firma nueva `downloadClientDossierPdf(input: ClientDossierData | ClientDossierData[], opts?: { separate?: boolean })`. Junto: un `doc`, `addPage()` entre informes, footer con página **global**. Los tiles salen de `dossier.tiles ?? buildTodayTiles(dossier)`. **Cierre**: C9 verde. — confirmado en disco, C9 verde (16-09).
- [x] **C5** **R18 · Zip** — separado ⇒ N `doc` y **un** `zipSync` de `fflate`, nombre `dossier-<slug>-<primero>_<último>.zip` (`dossierFileStem`). **Nunca** N `doc.save()`. Declarar `"fflate": "0.8.3"` en `apps/web/package.json` (`dependencies`, junto a `jose`/`xlsx`/`zxing-wasm`, `:12-23`) + `pnpm install`. **Cierre**: el smoke test ve **un** `save()` y un zip con N entradas. — `fflate` declarada, `zipSync` importado dinámico en `client-dossier-pdf.ts:853`; lockfile actualizado (`pnpm-lock.yaml` en el diff).
- [x] **C6** `apps/web/src/app/coach/clients/[clientId]/DossierExportDialog.tsx` (**nuevo**, `[UI · Fable]`) — SPEC §6: `@/components/ui/dialog`, `@/components/ui/segmented-control` (`:22-30`), `@/components/ui/switch`; chips `<button aria-pressed>` con scroll a partir de 12; atajos «Últimos 3» / «Últimos 6»; interruptores «Un solo PDF» (≥ 2 meses) e «Incluir fotos» (encendido, Q4); CTA con contador de archivos; **errores dentro del diálogo**; `bounds` pedidos al abrir. **Cierre**: los puntos 1–14 del QA web (SPEC §17). — archivo en disco, completo; **QA del owner sigue pendiente** (el gate mecánico no reemplaza el QA de E6).
- [x] **C7** `apps/web/src/app/coach/clients/[clientId]/ClientProfileHero.tsx` (`[UI · Fable]`) — `handleExport` abre el diálogo en vez de descargar; el botón mantiene `aria-label`/`title` **«Exportar PDF»** (no «Exportar dossier» — corrección del revisor final, SPEC DM-02a). **Cierre**: el camino «Estado actual» del diálogo produce el mismo archivo que antes (DM-04). — confirmado: `ClientProfileHero.tsx:241-242` sigue con «Exportar PDF»; `DossierExportDialog` importado y montado (`:28,407`).
- [x] **C8** **R18 · Smoke test ampliado** `apps/web/src/lib/pdf/client-dossier-pdf.smoke.test.ts` — el holder pasa de un valor suelto a `captured: ArrayBuffer[]`; casos nuevos: (a) **3 informes junto** ⇒ un `save()`, páginas > 3, nombre `…_2026-09.pdf`; (b) **3 informes separado** ⇒ un zip con 3 entradas y un solo `save()`; (c) un `ClientDossierData` suelto (la firma vieja) sigue funcionando. Detalle en [DATA-TESTING](DATA-TESTING.md) §5.5. **Cierre**: los tres casos verdes. — dentro del run verde de `pnpm exec vitest run` (16-09).
- [x] **C9** **Gates del carril**: `pnpm typecheck` · `pnpm exec vitest run apps/web/src/lib/pdf/client-dossier-pdf.smoke.test.ts apps/web/src/services/client/client-dossier.test.ts packages/client-dossier` · `pnpm exec eslint` sobre los **archivos tocados** de este carril. — cubiertos por la suite completa del 16-09 (`typecheck` exit 0, `vitest run` 808/11.111 passed, `lint` 0 errores).
- [x] **C10** **(ronda de revisión 16-09)** `translateReportRpcError` devuelve mensaje genérico («No se pudo leer el informe del alumno») para todo código que no sea `42501`/`22023` y loguea el original server-side (`console.error('[client-month-report] RPC error', …)`). Test con `vi.spyOn(console, 'error')` y un `42P01`. — hecho, verificado en disco.

---

## D · RN (W3 — Opus datos + Fable UI)

- [x] **D1** `apps/mobile/lib/client-month-reports.ts` (**nuevo** — no en `coach-client-detail.ts`: las lecturas mensuales se separaron en su propio módulo para no engordar el archivo de 1.6k líneas, ver SPEC DM-03g corregido 16-09) — `fetchClientMonthReports(clientId, months)` y `fetchClientReportBounds(clientId)` con `supabase.rpc`. El modelo se arma con `buildClientMonthDossier` de `@eva/client-dossier`: RN **no** reimplementa ni una regla (R22). **Cierre**: los seis tiles de un mes coinciden con los de la web (QA punto 20, pendiente de QA de owner). — archivo en disco, `mobile tsc --noEmit` exit 0 (16-09).
- [x] **D2** **R23 · HTML multi-informe** en `apps/mobile/lib/client-dossier-pdf.ts` — separar `renderDossierHtml(model, photoMap)` de la exportación; nuevo `exportClientDossierPdfs(models[], opts)` que concatena bloques con salto de página entre informes y comparte **un** archivo (Q1 = a). `kpiCard` (`:255`) recibe un `DossierTile`; el stem usa `dossierFileStem` (`:627,656`). **Cierre**: 3 meses ⇒ un PDF con 3 informes. — confirmado en disco (`renderDossierHtml:282`, `exportClientDossierPdfs` con `models.map(...)` en `:654`).
- [x] **D3** **R23 · Fixes de corte** en `STYLES` (`apps/mobile/lib/client-dossier-pdf.ts:231-245`): `tr, .vol-row, .photo-cell { page-break-inside: avoid; break-inside: avoid }`; `.photos-block { break-inside: avoid; page-break-inside: avoid }`; `.vol-track`/`.vol-bar` con `display: block`. **Cierre**: QA punto 19 — ninguna barra huérfana, ninguna foto partida. — presente en disco; **QA de owner en device sigue pendiente** (E6).
- [x] **D4** **R20 · Fotos RN** en `client-dossier-pdf.ts`/`client-dossier-photos.ts` — `ImageManipulator.manipulateAsync(...)` antes de embeber (`client-dossier-pdf.ts:137`); topes `MAX_PHOTOS_PER_MONTH = 3` y `MAX_PHOTOS_PER_EXPORT = 18` (`client-dossier-photos.ts:12,14`); firma por lote con `signCheckinPhotos` (`client-dossier-pdf.ts:177`, `api.ts`). **Cierre**: QA puntos 21–22 (pendiente de device). — código en disco y typecheck verde; **nota de deuda**: `expo-image-manipulator@14.0.8` marca `manipulateAsync` `@deprecated` (sigue funcionando) — ver Backlog.
- [x] **D5** **R20 · Tope de `refs` en la ruta de fotos** — `apps/web/src/app/api/mobile/coach/checkin-photos/route.ts`: `MAX_REFS_PER_REQUEST = 24`; con más de 24 refs responde `400 { error: 'Demasiadas fotos por request' }` y no firma nada (SPEC DM-52a). La validación de dueño queda intacta y corre para todo request válido. — hecho, verificado en disco.
- [x] **D6** `apps/mobile/components/coach/clientDetail/DossierExportSheet.tsx` (**nuevo**, `[UI · Fable]`) — SPEC §7, construido con `apps/mobile/components/Sheet.tsx`, `SegmentedTabs.tsx` y `Switch.tsx`. **No** se monta un `@gorhom/bottom-sheet` nuevo a mano. Chips en `ScrollView` horizontal + atajos; interruptor de fotos solo en «Por meses»; **sin** interruptor junto/separado; CTA «Generar y compartir»; título del sheet **«Exportar dossier»** (`:172`, distinto del botón web «Exportar PDF» — SPEC DM-02a). **Cierre**: QA puntos 15–18 (pendiente de device). — archivo completo en disco; incluye el guard de cierre solo-si-`exported===true` (ronda de revisión ítem 6) y el caché de `bounds` pedido solo al entrar a «Por meses» (ítem 9).
- [x] **D7** `apps/mobile/app/coach/cliente/[clientId].tsx` (`[UI · Fable]`) — `handleExportPdf` (`:686`) abre el sheet vía `onExportCurrent`/`onExportMonths` (`:893-894`); el camino «Estado actual» sigue llamando a `exportClientDossierPdf` con los mismos `scalars`. `ClientHero.tsx` no cambia de contrato. **Cierre**: QA punto 16 (pendiente de device). — confirmado en disco.
- [x] **D8** **Gates del carril**: `pnpm --filter @eva/mobile exec tsc --noEmit` · `pnpm lint:mobile` · `npx expo export --platform android` (desde `apps/mobile`) · `pnpm exec vitest run packages/client-dossier`. — cubiertos por la suite del 16-09: mobile `tsc --noEmit` exit 0, `pnpm lint` 0 errores/572 warnings preexistentes, `vitest run` verde. `npx expo export --platform android` **no está entre las salidas pegadas** por el jefe hoy — si no se corrió aparte, dejarlo pendiente antes del cierre final.
- [x] **D9** **(ronda de revisión 16-09)** `throwRpcError` en `apps/mobile/lib/client-month-reports.ts` aplica la misma allowlist que el service web (DM-42b.1): `42501` ⇒ «No tenés acceso a este alumno», `22023` ⇒ «Selección de meses inválida», cualquier otro ⇒ `console.warn` + mensaje genérico. El sheet nunca pinta el mensaje crudo. — hecho (16-09, tsc mobile y eslint verdes).

---

## E · Cierre (W4 — Fable jefe + owner)

- [x] **E1** Revisión del diff completo contra este documento, carril por carril, con foco en que el camino «Estado actual» **no tenga un solo diff de comportamiento** (DM-04). — hecha por el revisor final el 16-09; resultado en la sección «Ronda de revisión 16-09» más abajo (14 hallazgos, 10 corregidos/anotados, 3 abiertos como C10/D5/D9).
- [x] **E2** **Suite completa, una sola vez**, en este orden: `pnpm docs:check` · `pnpm lint` · `pnpm lint:mobile` · `pnpm typecheck` · `pnpm --filter @eva/mobile exec tsc --noEmit` · `pnpm exec vitest run` · `pnpm check:tokens`. Registrar la **salida real** de cada uno. — `docs:check` OK · `lint` 0 errores/572 warnings preexistentes (el script ya encadena `eslint apps/web/src ...` + `eslint --config eslint.mobile.config.mjs apps/mobile`, cubre `lint:mobile`) · `typecheck` exit 0 · mobile `tsc --noEmit` exit 0 · `vitest run` 808 archivos/11.111 tests passed · `check:tokens` → «EVA DS token parity OK — 86 governed tokens + 5 seal tokens match across web and mobile (light + dark)» (corrido 16-09 en esta pasada). `pnpm build` también exit 0 (no es parte del orden original de E2 pero se corrió igual).
- [x] **E3** **R25 · Spec E2E escrita, NO corrida** — `tests/dossier-export.spec.ts` existe (login de coach, ficha, «Exportar PDF», «Por meses», un chip, rótulo del CTA, Escape) y está anotada como «escrito, no corrido» en `docs/testing/TEST_STATUS.md` §Gates locales. **Se corre solo en el gate final autorizado por el owner.**
- [x] **E4** `docs/status/CURRENT.md` — una línea del tren con enlace a esta spec, midiendo con `wc -c` **antes y después** (tope duro 16 KB, `scripts/check-docs.mjs:117`). Volver a correr `pnpm docs:check`. — línea del tren ya presente (`docs/status/CURRENT.md:32`); tamaño antes (commit previo a `c6297976`) **16.359 B**, después **14.892 B**; `pnpm docs:check` OK.
- [ ] **E5** `docs/status/MOBILE_PARITY.md` si cambia la paridad web ↔ RN; `docs/testing/TEST_STATUS.md` **solo** si cambia un gate obligatorio o por E3. — no tocado en esta pasada (fuera de `docs/specs/dossier-por-meses/`); `MOBILE_PARITY.md` no menciona el tren todavía — pendiente de evaluar si corresponde una línea.
- [ ] **E6** **Owner**: QA de [SPEC](SPEC.md) §17 **completo** — 14 puntos en web, 8 en device (RN) y 2 comunes. Sin los dos lados verdes el SDD **no** pasa a `done`. — sin ejecutar.
- [x] **E7** **Solo a pedido del owner**: commit, push, deploy web y OTA. — autorizado por el owner («Q1 a») y ejecutado el 16-09 ~02:30Z: `master` = `rnmobiledenuevo` = `0748acd3` (`d0e50cf2` SDD/RPC/package → `0748acd3` web + RN), deploy `dpl_HQH52H1T6nMba2Qfzek7QnzpfBjB` READY, migración LIVE `20260916014833`, OTA ios runtime 1.1.3 grupo `a73f71a0` (run 35047893693) + runtime 1.1.2 desde el tag `ota/1.1.2-20260916` = `64ad9615` (android run 35048341298 / ios run 35048343745). Detalle en [MOBILE_RELEASES_OTA](../../operations/MOBILE_RELEASES_OTA.md).

---

## Ronda de revisión 16-09

Hallazgos del revisor final, verificados contra el código el 16-09. Cada uno en una línea, con su estado real (no el que se asumía al escribir el hallazgo).

1. **`useEffect` del diálogo web que se auto-cancelaba** (BLOQUEA) — **corregido**: `DossierExportDialog.tsx:102-136` reescribió el efecto de apertura con deps `[open, clientId]` y un `boundsRef` fuera de las deps (el comentario `:94-97` documenta el bug viejo: `bounds`/`loadingBounds` en las deps + cleanup que cancelaba dejaba «Cargando meses…» para siempre).
2. **Bounds pedidos al abrir + segmento «Por meses» deshabilitado mientras cargan** — **corregido**: `getClientReportBounds` se pide en el mismo efecto de apertura (`:120-135`) y `SegmentedControl` recibe `disabledValues={loadingBounds ? ['months'] : undefined}` (`:230`).
3. **Hint «Elegí al menos un mes»** — **corregido**: presente en `DossierExportDialog.tsx:320-324`, visible cuando `count === 0` y no hay `notice` de tope de meses.
4. **Rótulo del botón del hero web** — **SPEC corregido** (no era un bug de código): el botón siempre se llamó «Exportar PDF» (`ClientProfileHero.tsx:241-242`), nunca cambió a «Exportar dossier». El SPEC (DM-02a) tenía el rótulo equivocado; ya corregido en este documento. El sheet RN sí se titula «Exportar dossier» (`DossierExportSheet.tsx:172`) porque es un componente nuevo.
5. **Paridad de estados vacíos web↔RN** — **corregido en web**: los 4 textos de modo mes (`EMPTY.*.month` en `client-dossier-pdf.ts:77-85` de web) son carácter-por-carácter iguales a los de RN (`apps/mobile/lib/client-dossier-pdf.ts:311,350-351,357,382`) — ver SPEC §8.7 (nuevo).
6. **Guard mudo en RN que cerraba la hoja sin haber generado el PDF** — **corregido**: `DossierExportSheet.tsx:126-142` solo llama `onClose()` cuando `onExportCurrent()`/`onExportMonths()` devuelven `true`; un `false` (nada generado) deja la hoja abierta.
7. **Mensaje crudo de Postgres llegando a la UI** — **corregido en las tres capas**: el service web devuelve genérico + log server-side (C10), el diálogo web filtra con allowlist (`messageFor`), y RN traduce `42501`/`22023` y cae a genérico con `console.warn` (D9).
8. **Ruta de fotos: 400 en vez de truncar** — **corregido**: `checkin-photos/route.ts` responde `400` con más de 24 refs (D5), sin tocar la validación de dueño.
9. **RN cachea `bounds` y los pide solo al entrar a «Por meses»** — **corregido**: `DossierExportSheet.tsx:96-102`, comentario explícito «Los meses se piden la PRIMERA vez que se entra a "Por meses" ... y quedan cacheados mientras la hoja viva para este alumno».
10. **Ruta de las lecturas RN** — **SPEC corregido**: viven en `apps/mobile/lib/client-month-reports.ts` (nuevo), no en `coach-client-detail.ts` como decía el SPEC/PLAN/TASKS originales. Corregido en los tres documentos (DM-03g, tabla de W3, tarea D1).
11. **Fila del tile 3 («Días entrenados»)** — **SPEC corregido**: el valor real es `trained/planned` o solo `trained` sin programa, con sub «N sesiones» (usa `sessions`, no `training_days`) — `packages/client-dossier/src/month-dossier.ts:379-384`. Corregido en SPEC §8.2 y DM-24g.
12. **«null%» → «—» en el tile Nutrición de RN («Estado actual»)** — **anotado para QA, cambio intencional**: `apps/mobile/lib/client-dossier-pdf.ts:598-599`. No es una regresión si el QA del owner venía de una build vieja. Nota agregada en SPEC §17.
13. **Tope de 24 meses replicado en la UI web** — **corregido**: `DossierExportDialog.tsx:45-47,164-167,179-180` (`MAX_MONTHS`, `TOO_MANY_MONTHS`, aviso inline al tocar el chip 25 o pedir «Últimos» más de 24).
14. **`expo-image-manipulator@14.0.8` marca `manipulateAsync` como `@deprecated`** — **deuda, anotada**: sigue funcionando (`apps/mobile/lib/client-dossier-pdf.ts:137`); reemplazo sugerido por el propio paquete es `ImageManipulator.manipulate`/`useImageManipulator` (API contextual nueva). Entra al backlog de este documento, para el próximo bump de SDK de Expo — no bloquea este tren.

---

## Backlog fuera del tren

Deuda declarada, con dueño y motivo. **No entra en este tren.**

- **Nutrición V1 en el dossier RN «de hoy» (R22).** `apps/mobile/lib/client-dossier-pdf.ts` imprime `data.nutritionMonthlyAvgPct`, que sale de `averageNutritionTimelineCompliance` sobre el timeline **V1** (`apps/mobile/lib/coach-client-detail.ts:1157`), mientras el dossier web ya usa la señal **V2** (`apps/web/src/services/client/client-dossier.ts:419-422`). El mismo alumno da dos números distintos según desde dónde se exporte. Es un **bug preexistente**; el informe **mensual** no lo hereda (su nutrición sale del RPC nuevo). Arreglo estimado: chico, pero toca el camino «Estado actual» de RN, que este tren se comprometió a no mover (DM-04).
- **La ficha web cuenta «workouts esta semana» en UTC (R26, D15a).** El fallback parte el ISO con `split('T')[0]`; el informe mensual corta en Santiago. Entre las 21:00 y la medianoche de Chile un entreno puede caer en semanas distintas según quién lo cuente. **Divergencia aceptada**, anotada en [DATA-TESTING](DATA-TESTING.md) §6. Arreglarla toca la ficha entera y sus tests, no este tren.
- **`check_ins.date` sigue escribiéndose en medianoche UTC por el peso rápido.** Este tren lo esquiva usando `created_at` como eje (R5); la columna sigue mintiendo para cualquier otro consumidor que la lea como instante.
- **Los atajos «Últimos 3/6» no recuerdan la última selección.** Cada apertura del diálogo/sheet arranca en «Estado actual».
- **Sin exportación programada ni envío por correo.** El coach descarga y manda él.
- **`expo-image-manipulator@14.0.8` marca `manipulateAsync` como `@deprecated`** (ronda de revisión 16-09, ítem 14). `apps/mobile/lib/client-dossier-pdf.ts:137` lo usa para redimensionar/comprimir fotos antes de embeberlas; sigue funcionando en el SDK actual (`apps/mobile/package.json:66`, `~14.0.8`), pero el paquete ya expone la API contextual nueva (`ImageManipulator.manipulate` / `useImageManipulator`). Migrar cuando toque el próximo bump de Expo SDK; no bloquea este tren.
- **`translateReportRpcError` (web) y `throwRpcError` (RN) devuelven el mensaje crudo de Postgres para cualquier código que no sea `42501`/`22023`** (ronda de revisión 16-09, ítem 7; SPEC DM-42b.1). En la web no se filtra al coach porque `DossierExportDialog.messageFor()` ya tiene una allowlist; en RN sí llega crudo al sheet. Tareas nuevas **C10** y **D9**, sin marcar — cerrarlas es un cambio de una función en cada lado, no bloquea el resto del tren.
- **La ruta de fotos compartida trunca en vez de rechazar** (ronda de revisión 16-09, ítem 8; SPEC DM-52a). `checkin-photos/route.ts:54` sigue con `.slice(0, 24)`; el revisor final pide `400` cuando `refs.length > 24`. Tarea **D5** actualizada, sin marcar.
