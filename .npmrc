# pnpm config
# Strict isolation: packages cannot require undeclared deps (supply chain defense)
shamefully-hoist=false

# Don't fail on peer dep warnings — project has known peer dep mismatches (React 19 ecosystem)
strict-peer-dependencies=false

# Use workspace protocol for local packages
prefer-workspace-packages=true
