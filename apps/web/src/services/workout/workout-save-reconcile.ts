// Compatibilidad local: la fuente única web + mobile vive en @eva/workout-engine.
export {
    diffBlocksByPosition,
    hasProgramOptimisticConflict,
    matchPlans,
} from '@eva/workout-engine'
export type {
    BlockOp,
    DesiredPlanKey,
    ExistingBlock,
    ExistingPlan,
    ProgramOptimisticConflictInput,
} from '@eva/workout-engine'
