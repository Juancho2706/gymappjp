/**
 * Concept A — Kinetic Obsidian
 * Fondo de mesh gradient ambiental fijo (pointer-events:none, z -1).
 * Usa --theme-primary-rgb sutilmente para pintar la atmósfera.
 */
export function AmbientMesh() {
  return <div className="ambient-mesh" aria-hidden />
}
