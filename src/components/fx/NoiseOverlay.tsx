/**
 * Concept A — Kinetic Obsidian
 * Grain cinematográfico global. Se monta una sola vez en el layout raíz.
 */
export function NoiseOverlay() {
  return <div className="noise-overlay" aria-hidden />
}
